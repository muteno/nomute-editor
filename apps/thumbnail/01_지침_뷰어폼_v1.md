# 썸네일 뷰어 폼 기틀 (v1)

> **정본 코드 = `viewer/thumb.html`.** 이 문서 = 그 폼의 **구조·관례 SSOT**. 렌더링(PIL 스크립트)은 `00_지침_썸네일과오버레이`·`MEMORY`가 정본이고, **여긴 입력 UI/UX(탭·goSpec·슬롯·수정복원·자간안내)만** 다룬다.
> ⚠️ **왜 만들었나**: 이 폼이 매번 흔들린 건 *정해둔 게 없어서*다(운영자 260624). 이제 여기 박는다 — 새 세션·압축 후 썸네일 폼 작업이면 **이 문서부터** 읽고, 충돌 시 이 문서가 우선(코드 실측으로 갱신).

---

## §0. 대원칙 (제일 중요 — 이걸로 안 흔들림)

1. **흰 = 고정 → 백엔드 불변.** 폼/goSpec/슬롯 작업은 **`functions/api/thumb.js`·`.github/workflows/thumb-make.yml`·`apps/thumbnail/nomute_*.py` 안 건드린다.** 헤더는 항상 흰칸을 렌더하고, 배경(nobg) 등은 *프론트 옵션*일 뿐. → **폼 UI는 자유롭게 바꿔도 렌더 회귀 위험 0.** (이게 그동안 빠른 반복이 안전했던 이유.)
2. **LLM 토큰 0.** /1~/4 = 순수 PIL 렌더. `effort`·`모델` 레버 **해당없음**. (유일한 외부호출 = 맞춤법 자동교정 `functions/api/spellcheck.js` = 비-LLM 프록시·무료·비차단.)
3. **디자인 = §🎨 토큰 계승.** raw hex/blur/accent 창작 금지 → `var()` 토큰. 새 버튼/컴포넌트는 기존 인벤토리 계승(재설계 금지). 다운로드=`.dlbtn`.
4. **검증 = `check_refs` + node 파싱(자가검증).** 폼 변경은 §✅ⓒ 국소 뷰어 손질 = 자가검증으로 끊는다(동작 로직·`:root` 토큰·파이프라인 끼면 더 엄격). `python3 shared/check_refs.py` + 인라인 `<script>` `new Function()` 파싱.
5. **반영 = §수정 모드.** 백업 → 수정 → check_refs → PR → main 머지 → `git show origin/main` read-back. 머지 후 Cloudflare 1~2분(뷰어 `_headers` no-cache = 하드새로고침 불요).

---

## §1. 탭 구조 (3탭 · 기틀)
> ⚠️ **포스트 탭 재폐지 · 릴스 라벨→"썸네일" · 생성기명 "오버레이 생성기" (운영자 260624)**: 상단 탭 = **썸네일 · 저작권 · 카드뉴스 3개**(`.tabs` `repeat(3,1fr)`). 포스트(app1)는 = 오버레이를 4:5로 뽑는 것 = 썸네일 폼 안 포맷 토글의 비율차일 뿐 → 탭 제거(`#925`로 한 번 복구됐다 다시 폐지). 포스트 폼 HTML/JS(`#opaBox`·`#lines`/`PIMG` 등)·`selApp('1')` 분기는 **도먼트 잔존**(탭 없어 미도달·importToEditor가 레거시 app1→app2 리매핑·후속 정리 가능). **제목**: 페이지 `<title>` = `오버레이 생성기` · **메뉴/모달 헤더** = `viewer/index.html` `.bpop` 링크 텍스트 `오버레이 생성기`(롱프레스 메뉴 → `openTool`이 그 텍스트를 모달 `#toolTitle`에 씀 = 메뉴명·헤더 한 곳서 결정).

| 탭(라벨) | app | 디스패치 | 비고 |
|---|---|---|---|
| 썸네일 | `2` | `/api/thumb` | **단일 폼**(헤더 + 오버레이[포스트/릴스 비율] + 옵션 · 아래 §2) · 옛 라벨 "릴스" |
| 저작권 | `3` | `/api/thumb` | 자연 문구 탭수정 + 안내문(경고/AI/자유문구 = 옛 app4 흡수) · 아래 §7 |
| 카드뉴스 | `5` | `/api/compose` | 인라인 칩 에디터(입력 UI 본체) |
| (경고문) | `4` | `/api/thumb` | 탭 아님 — 저작권 탭의 안내문서 발사되는 내부 app |

- 전역 상태: `APP` · `FMT` · `ovFmt`(썸네일 오버레이 포맷 'post'/'reels') · 이미지 store `CIMG`(썸네일)·`IMG`(카드). 기본 탭 = `selApp('2')`(썸네일). *(옛 포스트 app1은 탭 제거·이력은 importToEditor서 app2로 리매핑.)*

### §2. 썸네일(app2) = 단일 폼 — 헤더 + 오버레이[포스트/릴스] + 옵션 (운영자 260624 통합)
> **헤더 · 오버레이 · 옵션**이 한 폼. **채운 섹션만 발사** · 헤더↔오버레이 **동시 가능**(상호배타·READ-ONLY 폐지 — 헤더형 릴스도 오버레이 동반 위함).
- **• 헤더(부제+제목)** 채우면 → 릴스 흰칸(9:16·배경 ON 시 nobg +1). 부제·제목 **둘 다** 필요(반쪽=안내).
- **• 오버레이 `| 포스트/릴스`** = **포맷 토글**(`#ovFmtTog` · 텍스트 시작 *전* 선택 = 자간 한도 분기). **포스트=시안(4:5·−45 한도·기본)** / **릴스=레몬(9:16·−30 한도)**. 클릭 전환 시 자간 즉시 재적용(초과구간 빨강). **헤더 입력 중이면 릴스 강제**(흰칸 9:16과 짝 · `effOvFmt`). 발사 = effOvFmt 'post'→app1/fmt:post(4:5) / 'reels'→app2/overlay(9:16). 이미지·OPA·축약 공용.
  - 발사 매트릭스: **헤더만 → 흰칸** · **오버레이만 → 포스트 또는 릴스(토글)** · **둘 다 → 흰칸 + 릴스 오버레이**.
- **옵션줄**(검정 배경 패널·`#goSpec`) = `배경 · OPA(60+보조) · 합성본 · 축약`. (포맷은 옵션 아닌 •오버레이 헤더에 = 자간 선반영.)
- **둘 다** 채우면 → 흰칸 + 자막 동시 발사. 아무것도 안 채우면 안내.
- 옵션줄(`#goSpec`·`#rOptHead`) = `배경 OFF/ON · OPA 60 +30 · 합성본(이미지 시 자동) · 축약 ON`. 백엔드 = 기존 `header`·`overlay` 모드 **그대로**(흰=고정 불변 · 렌더 회귀 0).

---

## §3. goSpec = 결과 명세 = 컨트롤 (핵심 패턴 · 기틀)

> **정의**: 제작 버튼 *위* 한 줄. 결과물 명세를 보여주면서 **동시에 거기서 설정을 토글/조절**한다. 산재한 체크박스를 폐지하고 한 줄에서 다 제어 = 일관성.

- **표기 관례(정본)**:
  - 라벨(헤더/오버레이/축약) = **mut 색**(`.gs-lbl`)
  - 활성 값 = **accent 네온그린**(`.gs-v.on`)
  - **옵션 구분 = `|`**(OPA\|합성본) · **그룹 구분 = 공백**(` / ` 안 씀) · **`[ ]` 대괄호 안 씀**
  - **배경·축약 = OFF/ON 단일 토글**(`syncOnOff`·`.gs-tog` 탭) — *흰칸은 고정*, 배경(흰칸 없는 nobg) 추가 여부만(기본 OFF·ON이면 +1장). "기본\|흰"·"[흰칸\|배경]" 식 2값 나열 폐지 = 혼란 → 단일 토글 통일(260624). OPA = `.gs-opa`(휠·상하드래그·조절중 mut→고정 accent)
  - 기본(combo) goSpec = **90%(11.25px) + nowrap** = 모바일 한 줄(`#goSpec`)
- **적용 범위**: 릴스 단일 폼(`#goSpec` — 배경·OPA30·축약 토글 한 줄 · `#rOptHead`=`• 옵션` 머리) + 포스트(`#postSpec` 축약). **저작권·카드뉴스는 입력 UI 본체라 goSpec 제외.** *(옛 `#hdrSpec`·`#ovSpec`은 단일 폼 전환으로 제거 — 260624.)*
- **구현(흰=고정 정신)**: hidden input(`#cBg`·`#rBg`·`#abbr`) 유지 + goSpec span이 그 상태를 토글 → **발사(buildParams·go.onclick) 로직 불변.** `updateGoSpec()`가 모드별 명세를 갱신.

---

## §4. 입력 위젯 패턴 (기틀)

- **텍스트 + 이미지 = covrow**: 텍스트(`.covta`) + 이미지 슬롯(`.covimg`). 데스크톱 2/3:1/3(flex 2:1) · **모바일(≤480px) 3:1(텍스트 75%·슬롯 25%)** — 이미지 옆 둘 때 우측 넘침 방지(슬롯 축소로 해소 · 입력 글자크기는 100% = 옛 85% 복귀 운영자 260624). 적용 = 기본 자막·오버레이·**포스트 배경**(260624). *full-width 이미지 행 금지(한 행 낭비) — 슬롯으로 통일.* ⚠️ 클립(복사/붙여넣기) 버튼과 호환 = `attachCopyPaste`가 `covta`를 wrap에 승계(안 하면 wrap이 flex 먹어 슬롯 찌부 = 260624 버그).
  - **높이 동기 = 이미지 슬롯 높이 = 텍스트칸 높이 (260624)**: `ResizeObserver`가 각 `.covrow`의 `textarea`(클립 wrap 안)를 추적해 형제 `.covimg`에 명시 `height` 부여(`offsetHeight`·border-box) → 수동 리사이즈(`resize:vertical`) 포함 항상 같은 높이(`align-items:stretch`만으론 실측 어긋남 = 운영자 보고). 탭 숨김(offsetHeight 0)이면 스킵·보일 때 RO 재동기.
  - 바인더 = **`bindSlotImg(slotSel, inputSel, store)`** — 기본(`#cImgSlot`/`CIMG`)·오버레이(`#rImgSlot`/`RIMG`)·**포스트(`#pImgSlot`/`PIMG` = 카드뉴스 `IMG`와 분리 = 위젯 desync 방지)** 공용. 슬롯 탭=선택 · X=삭제 · `_set(b64,name)`/`_reset()`=수정복원용.
- **단독 이미지(배경) = `bindFile(input, lab, store)`** — 포스트·카드뉴스 배경(`#file`/`#fileLab`/`IMG`). `_set`/`_reset` 동일 노출.
- **부제/제목** = `.cf-sub`(흰 입력)·`.cf-title`(초록 입력) = **결과물 색 미러**. 라벨 없이 placeholder(부제/제목). 기본·헤더 동일.
- **rfab(빠른 메뉴)** 이미지 버튼 = `imgInput()`이 현재 모드 file input(`#file`/`#rFile`/`#cFile`) 반환 → `.click()`.

---

## §5. OPA·축약·자간 관례

- **OPA**:
  - 포스트(`#opaSeg`): **58 기본** · 30 · 직접입력. 다중선택 = 장수.
  - 릴스(단일 폼 · 옵션줄): **60 토글(`#gsOpa1`·고정값) + 보조 토글(`#gsOpa2`·기본 30·클릭 추가/끄기) · 최소 1개**(둘 다 끄기만 차단 = 보조 ON이면 60 OFF 가능). **보조 값 조절(ON일 때) = `−`/`+` 탭 스테퍼(`#gsOpa2Dn`/`#gsOpa2Up`·±5·모바일 주경로) + 휠/상하드래그(±1·데스크톱 보완)** — 모바일 드래그가 작은 타깃이라 잘 안 먹혀 −/+ 탭 신설(운영자 260624). 상태 = `cOpa1on`·`cOpa2on`·`cOpa2val`(JS 변수) → `getComboOpas`가 켜진 값 배열 반환(중복 제거·최소 [60]). 옛 `COMBO_OPA` 휠·`#cOpa30`·`#rOpaSeg` 폐지(260624).
  - 최소 1개 유지(마지막 1개 못 끔 = 0 방지). 직접입력 = 값 있을 때만.
- **축약(abbr)**: goSpec **ON 토글**(전 탭 통일 · 버튼 안 체크박스 폐지). **포스트·릴스만**(저작권·경고문·카드 제외).
  - **기본 4종(절대규칙·불변)** = `...→⋯ · •→· · 이,→李, · 윤,→尹,`(`ABBR_BUILTIN`). 항상 적용.
  - **사용자 커스텀 등록 (260624)**: 축약 토글(`#gsAbbr`/`#psAbbr`) **길게(550ms·모바일) 또는 우클릭(PC)** → `#abbrDlg` 다이얼로그. 기본 4종은 **잠금 행(`.abfix`·읽기전용·"기본" 태그·삭제불가)으로 리스트 상단에 표시**(운영자 "이,/윤, 도 리스트에 보여야") → 그 아래 `+ 추가`로 `축약 전→후` 커스텀 행(추가/삭제·인덱싱 `abbrRenumber` 전 행 1,2,3…). localStorage `nomute_thumb_abbr`(`ABBR_KEY`)에 **커스텀만**(`abbrSaveRows`가 `.abfix` 제외) 저장 · `abbr()`=`ABBR_BUILTIN`(코드 불변) + 커스텀(`split/join` 리터럴). **맥락 자동감지 불가**(순수 치환) → 흔한 음절(예 군=軍/郡/군것질)은 `축약 전`에 **앞뒤 글자까지** 넣어 스코프(예 `군 →軍 ` · `군,→軍,`). 닫기=X·백드롭·ESC·폰 뒤로가기(pushState/popstate).
- **자간(tracking) 안내**: 텍스트가 좌우 마진 초과하면 안내(`.trk`) — **노랑(`.on`)=자간 좁힘 · 빨강(`.over`)=축약 필요**. *초과할 때만* 표시.
  - `updateTrkHint(taId, hintId, fmt)` = 포스트(post)·오버레이(reels) 자막.
  - `renderHdrTrk(elSel, subSel, titleSel)` = 부제/제목(reels2 `_fit_tr`).
  - **전 탭 동일 적용**(포스트·헤더·오버레이·기본). 기본 = `updateComboTrk()`(헤더+자막 둘 다). 입력 시 실시간.

---

## §6. 수정(편집) = 제작 조건 복원 (기틀 · 260624)

> **목표**: 이전 제작을 작성기로 그대로 되돌려 **그 건에 맞는 탭으로 복귀** + 입력 복원.

- 발사 시 **`snapForm()`** 이 폼 설정(탭·모드·포맷·텍스트·OPA·토글·프리셋·카드칩)을 압축 스냅샷 → `dispatchJob`의 `job.src` → 이력(`histAdd`)·최근결과(`saveLastResult`)에 동봉.
- 이전 제작 카드 **다운로드 옆 연필(`.imgedit`)** → **`importToEditor(src, compUrl)`**: `selApp`로 탭 전환 + 입력 전부 복원 + 맨 위 스크롤. **릴스 레거시 이력(옛 `rmode` header/overlay)도 부제/제목/자막으로 매핑**(12h 전환기 우아 복원).
- **텍스트·설정 = localStorage**(가벼움) · **이미지 b64 = IndexedDB**(`nomute_thumb_img` · 같은 기기 대용량 · 12h prune).
- ✅ **기기 간 '수정' = 문구 항상 복원 + 이미지 빗금 참고본 (260624 · /api/thumb 한정 · 운영자 승인 백엔드 변경):** 제작 시 snapForm 스냅샷을 **서버에도 보존** → 기기 달라도 연필·문구 복원. 경로 = `dispatchJob`이 `payload.src` 주입(**imgName·imgKey 제거** = 공개 레포 신규 노출/노이즈 차단·로컬 src는 원본 유지) → `functions/api/thumb.js`가 `src_json`(6KB 캡) dispatch → `thumb-make.yml`이 `viewer/thumb_out/<id>/_src.json` 커밋(`_meta.json` 자매·렌더 후 best-effort·빈/깨짐=생략) → `build-viewer.mjs`가 `thumb-hist.json` 각 항목에 `e.src` 동봉(`src.app` 있을 때만) → `renderHist` 연필 표시. **이미지 원본 없으면**(타 기기 = IDB miss) `setImportRef`가 합성본(`e.url`)을 **빗금(`.refhatch`)+「참고」 배지**로 슬롯에 표시(`_setRef`) — store는 안 채움 = 재렌더 시 재선택해야 교체(빗금이 그 신호). ⚠️ **흰=고정/렌더 불변**(픽셀 회귀 0 · _src.json은 사이드카 메타). ⚠️ **카드(/api/compose·app5)는 별도 파이프 = 미적용**(로컬만 · 후속 PR로 comp-make에 동일 패턴 이식 가능). ⚠️ **배포 랙(~1분)**: 방금 타 기기서 만든 건 thumb-hist 빌드/배포 후에야 연필 노출(기존 기기간 이력과 동일 특성). ⚠️ 배포 후 12h 윈도 지나면 _src.json 없는 구 항목은 이력서 자연 소멸(연필 수렴). 정본 코드 4파일(thumb.html·api/thumb.js·thumb-make.yml·build-viewer.mjs).
- 릴스는 1 이미지키 공유(헤더+자막 동일).

---

## §7. 이력(이전 제작) · 저작권 미리보기

- **이력**: 12h 보관(`THUMB_HMAX` 240). 로컬(localStorage `nomute_thumb_hist`) + 타 기기(`SRV_HIST` = build-viewer `thumb-hist.json`). 다운로드=`.dlbtn`(✓팝링) · 수정=`.imgedit`(연필).
  - **맨 위 복원 = 자동 폐지 → 완성 알림(`#done=<id>`) 탭 때만 (운영자 260624)**: 평소 로드는 맨 위 결과 **비움**(폼+이력 리스트만 · "최근 게 계속 떠있다" 해소). **완성 푸시 알림**(`thumb-make.yml`이 `push_send.py --url /thumb.html#done=<id>` 발송 · `sw.js` 탭→이동)으로 진입하면 `maybeOpenHist`→**`restoreById(id)`**가 그 id 결과를 맨 위 큰 결과(캐러셀)로 복원·랜딩(바로 보이게). 소스 = `findResultById`(로컬 마지막이 그 id면 `src`까지=수정가능 / 아니면 SRV `thumb-hist.json` 같은 id 그룹). SRV 비동기·빌드랙은 `loadServerHist`+retry 사다리(≤15회)가 재fetch 후 `restoreById` 재시도. 인세션 제작물은 기존 job 흐름대로 표시(restore 무관). `saveLastResult`(THUMB_LAST_KEY)는 `findResultById` 로컬경로용 유지. ⚠️ 옛 라벨버그(릴스 흰칸 `box.jpg`→'포스트')·빌드/배포 랙은 잔존(별개).
- **저작권 = 자연 문구 + 탭 수정 · 포맷 분리 · ON/OFF · 안내문 팝업 (운영자 260624)**: 입력 박스 대신 `ⓒ 2026. [이름](플랫폼). all rights reserved.` **문구 자체를 메인으로** 띄우고 부분만 탭해 고친다(`.cphline`). 4섹션 순서 = **· 포맷 → · 저작권(ON/OFF) → 문구 → · 안내문(팝업) → 제작버튼(공용)**.
  - **· 포맷** = **단일 토글**(`#cpFmtTog`·`.ovfmt` 계승 = 썸네일 `#ovFmtTog`와 동일 패턴 · **한 번에 하나만 표시**·탭=포스트↔릴스 전환·시안#1fd6ee/레몬#e7ff2e·**기본 릴스** `cpFmt='reels'`·저작권↔안내문 **공유**). *도형(세그 알약) 없음 — 운영자 260624.* (옛 `#cpFmtSeg` 2-span 세그[둘 다 보이고 선택만 점등] 폐지.)
  - **· 저작권** = `ON` 토글(`#copyTog`·`.onoff`·**기본 ON** `copyOn=true`·탭=OFF→`.cphline.off` 흐려지고 제작서 빠짐). *도형(알약) 없이 텍스트만(ON=네온그린·OFF=mut) — 운영자 260624.*
  - **연도**(`#cphYear` contenteditable) = **자유 수정**(정수/`-`만·`2025-26` 가능·`getYear()`=숫자만·빈값 2026). *(구 `202` 고정+끝자리 폐지 — 운영자 260624.)*
  - **이름**(`#cphName` contenteditable) = 탭=입력·미입력 `:empty::before` 예시 `nomute`(값엔 안 샘)·`getName()` 빈값+저작권ON이면 발사 차단.
  - **`all rights reserved.`** = `<span class="cph-br">`(flex-basis:100%)로 **편집화면만 다음 줄**(수정 시 안 흔들림)·**출력은 이어짐**(발사는 year/name/platform 파라미터라 영향 0).
  - **플랫폼**(`#cphPlat`) = 탭→`.platpop` 픽토그램 팝업 *촤르륵*(좌→우 스태거)·custom=직접입력칸.
  - **· 안내문** = `OFF` 토글(`#guideTog`·`.onoff`·**기본 OFF**·*도형 없이 텍스트만* — 운영자 260624)·탭→`#guidePop` 팝업(`.gpop`·경고/AI/자유문구 = `.gpbtn` 텍스트 버튼·플랫폼 팝업과 동일 촤르륵)·고르면 ON(라벨=종류명)·재선택=OFF·`caution`/`ai`=프리셋·`free`=빈칸 커서·켜지면 `#warnLines` 등장.
  - **제작버튼 공용**: `copyOn`이면 저작권 발사 + `guideMode`+문구 있으면 안내문 발사 · **둘 다 OFF면 "저작권을 켜거나 안내문을 골라줘"**(아무것도 안 나감). ⚠️ 백엔드(`/api/thumb` year/name/platform·app4 lines)·흰=고정 불변 = 렌더 회귀 0.

---

## §8. 기틀 vs 주변부 (흔들림 방지 — 제일 중요)

- **기틀(바꾸려면 §기틀 보호 = 사용자 확인 먼저)**: 탭 구조·디스패치 경로 · **흰=고정/백엔드 불변** · goSpec 패턴(표기 관례 포함) · covrow/`bindSlotImg` 패턴 · 수정복원(`snapForm`/IDB) · 자간 안내 체계 · 디자인 토큰 계승 · LLM 0.
- **주변부(자유 — 말 안 해도 됨)**: 문구·placeholder·기본값 미세조정(OPA·여백·색 미세) · 라벨 텍스트 · 안내문. **단 이걸 바꿔도 위 기틀 패턴은 따른다**(새 다운로드 버튼=`.dlbtn`, 새 이미지칸=covrow+`bindSlotImg`, 새 설정=goSpec 토글 등).

---

## 변경 이력
- **v2.8 (260626)** = 폼 **겉면 프레임(`.panel`) 제거 = 뉴스 요약 페이지 계승**(운영자 "겉면 라인 구분 불필요 · 뉴스 요약 페이지 UI/UX 그대로 계승"): `.panel`의 `border:1px`·`border-radius:16`·`background`·`box-shadow`·`padding:14`를 전부 제거 → `background:transparent; border:0; border-radius:0; padding:0; box-shadow:none`. 탭 아래 폼 전체를 또 감싸던 외곽 박스(겉 라인)를 없애 **섹션(`.csec`)·입력칸이 페이지 배경 글래스 위에 바로 흐르게** = index.html 요약 페이지 `.nm-wrap`(프레임 없는 흐름 컨테이너) 구조 계승. 깊이는 입력칸 자체 글래스(`rgba(14,15,17,.55)`)가 담당(§🎨 "테두리보다 글래스"). 외곽 여백은 `.wrap`(18px 16px)이 제공·`.tabs margin-bottom:16` + `.csec margin:16` 상단 간격 유지. **동작·발사·백엔드·`:root` 토큰·`.csec`/goSpec 패턴 불변 = 순수 외형(§✅ⓒ 국소 뷰어 손질·자가검증 check_refs+JS구문)**. 가역 = `.panel` 룰 복원. 백업 `_versions/260626_1630_thumb_panel_frameless`.
- **v2.7 (260626)** = 모달(오버레이 생성기) 배경 = 타 메뉴처럼 **글래스로 뒤 피드 비침 복원**(운영자): `html.inmodal, html.inmodal body` `var(--bg)`→**`transparent`** + `html.inmodal body::before { display:none }`(모달 안에선 자체 feed_bg 실크 끔) → iframe(`#toolframe`)이 투명해져 `#tooldlg` 글래스(`rgba(12,18,14,.64)`+blur34)가 **실제 뷰어 피드를 블러로 비침** = `.bpop` 등 타 메뉴와 통일. v2.5의 "모달에서도 평면(불투명)"을 **의도적으로 되돌림**(운영자: 생성기 배경이 불투명해 어색 → 자연스럽게). ⚠️ 스탠드얼론(직접 진입·`#done` 푸시)에선 `body::before` 유지(전체 페이지 배경 불변) — `html.inmodal` 스코프라 모달일 때만 투명. 동작·발사·백엔드·`:root` 토큰 불변 = 순수 시각(§✅ⓔ).
- **v2.5 (260624)** = 생성기 배경 = 노뮤트 문서/보고서와 **통일(평면 `#0b0d0c`)**(운영자): thumb.html `--bg` `#121212`→`#0b0d0c` · body 라디얼 글로우 제거(평면) · `html.inmodal` 투명→`var(--bg)`(모달에서도 동일·옛 글래스 비침 폐지). ⚠️ thumb `--bg`가 index `--bg`(#121212)와 의도적 분기(생성기=문서톤·재동기화 금지). 시안 산출 = `docs/reports/260624_복사붙여넣기버튼_시안.html`(클립버튼 위치·후속). *(260626 v2.7서 모달 투명 부분 되돌림 — 평면 토큰값(`--bg`)은 유지.)*
- **v2.4 (260624)** = 탭/명칭 정리(운영자): 상단 **포스트 탭 제거**(3탭=썸네일·저작권·카드뉴스·`repeat(3,1fr)`) · **릴스 라벨→"썸네일"** · 페이지 `<title>` 및 롱프레스 메뉴/모달 헤더 **"썸네일 생성(기)"→"오버레이 생성기"**(thumb.html + index.html `.bpop`) · rfab 탭순환 `['2','3','5']`. app1 도먼트.
- **v2.6 (260625)** = 저작권 컨트롤 **도형 제거 + 포맷 단일 토글** + 분신술10인 마감(운영자): ① **· 포맷** = 2-span 세그(`#cpFmtSeg`·알약·둘 다 보임) → **단일 토글(`#cpFmtTog`·`.ovfmt` 계승·한 번에 하나만·탭=포스트↔릴스)** = 썸네일 `#ovFmtTog`와 동일 로직(운영자 "옆 썸네일 메뉴 로직 살펴봐"). ② **· 포맷·저작권·안내문 알약(도형) 폐지** → `.onoff`/`.ovfmt` border/bg/radius 제거 = 텍스트만(ON=네온그린·OFF=mut). 기능·발사·백엔드 불변(`cpFmt`/`copyOn`/`guideMode` 그대로) = 렌더 회귀 0. ③ **분신술 마감**: 중복 `.cpfmt` 시안/레몬 hex 2개 제거 → `check_refs` thumb hex baseline **31→29**(§🎨 "raw 줄이면 baseline도") · `.onoff`/`.ovfmt` **탭 패딩(3px 5px) 복원**(도형 없이 폰 탭영역 확보) + **`:active` 눌림 피드백** + **aria-label 동적**(SR이 현재 포스트/릴스 읽게 = 도형 소실 보완). *(분신술7·8.)*
- **v2.3 (260624)** = 저작권 2차(운영자): **· 포맷[포스트|릴스] 맨 위 분리**(기본 릴스·공유) · **· 저작권 ON/OFF 토글**(기본 ON·OFF=제작서 빠짐) · **연도 자유 수정**(202 고정 해제) · **`all rights reserved.` 편집화면만 줄바꿈**(출력 이어짐·`.cph-br`) · **· 안내문 = OFF 알약→팝업(경고/AI/자유문구·플랫폼 팝업식)**(세그 나열 폐지) · 제작버튼 공용(둘 다 OFF=안내). `copyOn` 상태 신설(snapForm/restore). §7 갱신.
- **v2.2 (260624)** = 저작권 = **자연 문구 + 탭 수정**(운영자). 문장 끊던 입력박스 폐지 → `ⓒ 202[끝자리]. [이름](플랫폼). all rights reserved.` 문구를 메인으로, 연도(202 고정+끝자리 정수/`-`)·이름(contenteditable)·플랫폼(픽토그램 팝업 *촤르륵* 스태거)만 탭 수정. 포맷 = `[ 포스트 | 릴스 ]` 세그(둘 다 보이고 선택만 점등). 안내문 = `OFF [ 경고 | AI | 자유문구 ]`(켜진 항목 네온그린=ON). 백엔드·흰=고정 불변. *(구 인라인 input box[전역 `input[type=text]`가 `.cph-in`보다 특이도 높아 통짜 박스로 깨지던 것]도 동시 해소.)* §7 참조.
- **v2.1 (260624)** = 릴스 OPA 정교화(운영자). `60 고정 + +30 토글`(2장 고정) → **`60 토글 + 보조(기본30·휠/드래그 값 조절) 토글 · 최소 1개`**(보조 ON이면 60 OFF 가능 = [60]/[60,v]/[v] 다 됨). 상태 = `cOpa1on`·`cOpa2on`·`cOpa2val`(`#cOpa30` 체크박스 폐지). `getComboOpas` = 켜진 값 배열.
- **v2 (260624)** = **릴스 포맷 분기(기본/헤더/오버레이 세그) 폐지 → 단일 폼**(운영자). 헤더·오버레이·옵션 한 폼 + **채운 섹션만 발사(B)** = 한 폼이 옛 3모드 대체. OPA = 휠(`COMBO_OPA`) → `60 고정 + +30 토글`(`#cOpa30`·`getComboOpas`). `RMODE`·`selRmode`·`#rHeader`·`#rOverlay`·`#rOpaSeg`·`#hdrSpec`·`#ovSpec`·`RIMG` 등 제거. 옵션줄에 `• 옵션`(`#rOptHead`) 머리 추가. 백엔드(`header`/`overlay` 모드·흰=고정) 불변 = 렌더 회귀 0.
- **v1 (260624)** = 신설. 그동안 산발 머지된 폼 결정들을 한 곳에 누적: 릴스 기본(combo) 모드 신설 · goSpec 통일(체크박스 폐지·`[]`/`/` 제거·90% 한 줄) · 이미지 슬롯(covrow `bindSlotImg`) · 수정복원(snapForm+IndexedDB) · 자간 안내 전탭 통일 · 저작권 예시 회색 · OPA 기본 정리. (관련 PR: #872·#884·#885·#886·#889·#890.)
