# NO MUTE 썸네일 — 변경 이력 전체 (CHANGELOG)

> `00_지침_썸네일과오버레이_v22.25.md`에서 분리(260610) — 매 진입 로드 토큰 감량. **여기가 전체 이력 정본**(최신 블록은 지침 본문에도 유지). 명세 SSOT는 지침 본문 "정의 위치" — 이력은 기록용.

## 260619 — 통일 라벨 (잡헤더·캐러셀 캡션·다운로드 파일명 일원화)

**운영자 요청 = "여러 개 만들지 말고 통일 개념".** 결과물 식별 라벨 3곳(잡 헤더·캐러셀 캡션·다운로드 파일명)을 **하나의 규칙**으로 통일 — `{앱}({포맷}) #{N} · {variant}`.
- **variant 태그** = 그 앱의 구별 파라미터: 포스트·릴스 오버레이=`OPA{값}`(직접입력 포함·단일도) / 릴스 헤더=`기본`·`흰칸` / 저작권=`{이름}` / 경고문=없음(잡 라벨로 구분).
- **핵심 버그 픽스**: 단일 OPA·직접입력·저작권이 다운로드 시 `..._out.png`로 떨어지던 것(`o.label` 빈 폴백) 제거 → variant 태그가 캡션·파일명에 항상 찍힘.
- **파일명 규칙**: `{yymmdd_hhmm}_{앱_포맷}_{NNN}_{variant}.png` — `#` 제거(인식 문제)·잡번호 **3자리 zero-pad**(005)·**OPA 대문자**. 예: `260619_0710_포스트_001_OPA80.png` · `260619_0710_저작권_포스트_003_노뮤트.png`.
- 정본 = `functions/api/thumb.js`(outs `label`=variant 태그) + `viewer/thumb.html`(`dlSlug`·`pad3`·`jobVars`·캡션/파일명 빌더). **서버 출력 경로·워크플로·PIL 스크립트 0 변경**(라벨은 클라가 태그로 조립 = 경로와 디커플).

## 변경 이력 (v22.24 → v22.25)

**진입 명령 4분할 — `/1`(포스트)·`/2`(릴스)·`/3`(카피라이트)·`/4`(경고문). 코드 4파일 0 변경.**
- ① **명령어 = 분기 확정**(사용자 설계·실무 피드백 "트리도 번거롭다"): `/1`=post 명시 동치(문구만→분기① 세트 2장 / 이미지 동반→②③) · `/2`=reels 명시 동치(이미지→④ / `*`有→⑤ / `*`無→⑥ 헤더형 2종) · `/3`=F16(빈 진입 시 이름+플랫폼 동시 버튼 1창 — 유일한 질문 분기) · `/4`=F17(질문 없이 즉시 생성). 진입 시 [의도 파악] 생략, 세부 되묻기 전면 폐기 → 안내 한 줄로 대체. 정본 트리거 = `.claude/skills/1·2·3·4`.
- ② **삭제(사용자 버튼 승인)**: `/th` 명령 폐지 + v22.24 빈 진입 버튼 트리 폐기(명령 4분할로 대체). 강조 매핑은 **기존 유지**(`*`有=⑤ 오버레이 / `*`無=⑥ 헤더형 — 뒤집기 기각: 문구만으로 강조 오버레이 제작 불가해짐).
- 절대규칙8 풀 4분류: 추가=진입 명령 4종(SKILL) / 변경=진입 체계·문서 참조(v22.25) / 보존=분기 ①~⑥ 출력 전부(세트 2장·헤더형 2종 포함)·F01~F18 명세·코드 4파일 불변·시그니처 캐시·가드·F16 동시질문·F17 즉시 / **삭제=`/th` 명령·빈 진입 트리(UX 진입로만 — 기능 0 삭제·사용자 승인)**. **C-등급(F16/F17) 출력 명세 보존 확인.**

## 변경 이력 (v22.23 → v22.24)

**분기 ⑥ 헤더형 = 항상 2종 출력 + /th 빈 진입 버튼 트리 — 코드 4파일 0 변경(절대규칙1 유지).**
- ① **분기 ⑥ 출력 변경(사용자 명시 결정)**: 릴스 헤더형 = **항상 2종 동시 렌더** — **1번 `_reels2_nobg.png`(흰 배경 없음 — 새 기본)** + 2번 `_reels2.png`(흰칸 포함). 기존 "기본=흰칸 1장·nobg는 요청 시"에서 변경(한 종만 원하면 말로 지정). nobg 변형 bash를 표준 bash 1블록에 흡수, 보고 ⑥ = `mode=reels2(헤더형·2종)`.
- ② **/th 빈 진입 = AskUserQuestion 2단 분기 트리**(정본=SKILL·라우터 §🔔): 포스트/릴스/카피라이트·경고문 → 세부 → 분기 확정 후 문구 수신 시 **[의도 파악] 생략**(모델 판단=맞춤법·페어·오버플로 B안·합성 케이스·품질만). 카피라이트(F16)는 이름+플랫폼 **한 다이얼로그 동시 질문** 후 즉시 생성(출력 명세 불변). 입력 동반 진입·키워드 타이핑 = 기존 자동 분기 그대로.
- 절대규칙8 풀 4분류: 추가=진입 트리(SKILL)·카피라이트 동시질문 UX / 변경=분기⑥ 출력(1종→2종·nobg가 1번)·§릴스 헤더형·표준 bash·보고⑥·고정 수치 표기 / 보존=F01~F17 명세·F18 렌더 로직(픽셀 동일)·코드 4파일 불변·분기 ①~⑤·시그니처 캐시 / **삭제=0**(흰칸 버전은 2번으로 보존). **C-등급(F16/F17) 출력 명세 보존 확인.**

## 변경 이력 (v22.22 → v22.23)

**진입 지연 개선 — 기능 명세 0 변경(F01~F18 전부 보존·코드 4파일 불변).**
- ① **변경 이력 분리**: v22.14~v22.22 이력 8블록(약 6.4KB·본문의 17%)을 `apps/thumbnail/CHANGELOG.md`로 이동(전체 이력 정본) — 매 /th 진입 로드 토큰 감량. 본문엔 최신 블록만 유지.
- ② **`setup.sh` 2단화**: 무거운 단계(apt 폰트 + pip mediapipe/opencv — 신규 컨테이너에서 분 단위)를 스탬프(`~/.cache/nomute_th_env_ready`)로 단락 + **환경 Setup script 등록 권장**(`bash apps/thumbnail/setup.sh` — ly Whisper prefetch와 동일 방식: 스냅샷 캐시 → /th 진입 즉시). 가벼운 단계(경로·심볼릭)는 매 세션 실행. 미등록·미설치 환경이면 기존대로 자동 설치(폴백 보존 — 동작 동일, 순서만 단계화).
- 절대규칙8 풀 4분류: 추가=`CHANGELOG.md`·setup 스탬프 단락 / 변경=setup.sh 단계 구조·문서 참조(v22.23) / 보존=F01~F18 명세 전부·코드 4파일 불변·출력 분기 ①~⑥·시그니처 캐시·표준 bash / **삭제=0**. **C-등급(F16/F17) 보존 확인.**

## 변경 이력 (v22.21 → v22.22)

**릴스 헤더형(형태2) '흰 배경 제거' 옵션 추가 — 코드 파일 0 변경(절대규칙1 유지·import만), 지침·SKILL 문서만.**
- 신규 옵션: **"흰 배경(흰칸·흰 박스) 제거/빼줘" 요청 시** → 흰칸(y590~1431)을 **그리지 않고** 베이스 그라데이션 그대로 + 헤더 텍스트 2줄만 렌더. 파일명 `_reels2_nobg.png`. **기본값은 기존대로 흰칸 포함**(명시 요청 시에만 nobg — 처음부터 요청해도, 흰칸 버전 받은 뒤 요청해도 동일 문구로 재렌더 제공).
- 구현 = `nomute_reels2.py`의 `_draw_line`·상수를 **import 호출만**으로 사용(render()의 흰칸 단계만 생략) → 텍스트 렌더·자간 sweep 픽셀 동일. **실측: 헤더·하단 픽셀차 0(흰칸 경계 1px 제외)·흰칸 영역=베이스 원본과 동일·tr 일치.** 변형 bash = §릴스 헤더형 > 흰 배경 제거.
- 진입 안내표(SKILL)·출력 분기 ⑥·자가 보고 ⑥·고정 수치에 옵션 병기.
- 절대규칙8 풀 4분류: 추가=F18 옵션(nobg 경로)·변형 bash / 변경=분기⑥·보고⑥·고정수치 표기 / 보존=F01~F18 명세·코드 4파일·기본 출력(흰칸 포함) / **삭제=0**. **C-등급(F16/F17) 보존 확인.**

## 변경 이력 (v22.20 → v22.21)

**릴스 헤더형(형태2) 자간 sweep 추가 — 좌우 마진 유지. F18 보강, `nomute_reels2.py`만 변경.**
- 좌우 마진 60px(가용폭 960) 항상 유지. 텍스트 폭 초과 시 **자간만 0→-45 자동 축소**(폰트 크기 불변, 기존 /th post sweep과 동일 하한). 부제·제목 독립.
- `nomute_reels2.py`: `_line_width`/`_fit_tr`/`_draw_line` 추가, render 반환 `{out,sub_tr,title_tr}`. 자간0(미초과)은 레거시 단일 렌더와 동일(픽셀차 0 유지).
- 실측: 긴 제목 1026px → 자간 -45로 박스 안. 극단 장문은 -45로도 한계(축약 권장).
- 절대규칙8: 추가=자간sweep / 변경=`nomute_reels2.py`·시그니처 / 보존=F01~F17·분기①~⑥·C등급 / 삭제=0.

## 변경 이력 (v22.19 → v22.20)

**릴스 헤더형(형태2) 신규 추가 — `nomute_reels2.py`. 기능 추가(F18), 기존 F01~F17·코드 3파일·분기 ①~⑤ 전부 보존.**
- 신규 **출력분기 ⑥**: 이미지X + reels + 강조(`*`) 없음 → **릴스 헤더형(형태2)**. 상단 로고+부제(흰)+제목(초록) 헤더 + 하단 영상 흰칸(y590~1431). 베이스 `assets/reels2_base.png`. 정의: §릴스 헤더형 (형태2).
- 입력 2줄(1줄=부제 흰 / 2줄=제목 초록 자동). `*강조*` 마크업 불필요 — 쓰면 기존 reels 오버레이(⑤)로 분기.
- `nomute_reels2.py` 절대규칙1 편입(불변·import만). **실측: 첨부 원본과 픽셀차 0.0.**
- **절대규칙8 풀 프로토콜 4분류**: 추가=F18·분기⑥·nomute_reels2.py / 보존=F01~F17·코드3파일·분기①~⑤ / 변경=절대규칙1 파일목록·자가보고·운영자단일진실 표기 / **삭제=0**. **C-등급(F16/F17) 보존 확인.**

## 변경 이력 (v22.18 → v22.19)

**F13(`*` 강조 페어 검증) 강화 — "어느 줄·어느 위치에서 짝이 닫히는지" 실행 전 의무 점검. 코드 파일 0 변경.**
- 배경: 별 1개를 제대로 써도, **한 줄 안에서 여는 별·닫는 별이 1쌍으로 닫히지 않으면**(닫는 별 누락 / 줄별 별 홀수 / 강조 구간이 줄 경계로 갈림 / 강조 안에 또 별) 정규식 `*[^*]+*`가 매칭 실패 → 그 별이 **일반 글자로 새고 강조 스킵**. 스크립트는 정상 동작, **검증 누락**이 "가끔 스킵"의 실제 경로였음. (실측 확인)
- **절대규칙 2번 절차화**: ⓐ 줄별 `*` 개수 짝수 확인 ⓑ 같은 줄 1쌍 완결(줄 경계 금지) ⓒ `parse()` 자가 대조(빈 강조·별 노출 0). 짝 어긋나면 렌더 말고 **어느 줄·어느 위치인지 지목**.
- **표준 bash measure 루프에 페어 가드 출력 추가** — LINES 가드와 동일 철학(실행결과에 박혀 렌더/B안 직전 자동 환기). import에 `parse` 추가, 시그니처 캐시에 `parse` 등록.
- **자가 보고**: 강조 사용 사이클이면 `/ 페어 OK`(또는 `⚠️페어깨짐 Ln`) 항목 추가(F14) — "가끔 스킵"이 보고로 드러나 추적 가능.
- F01~F17 명세·구조 보존, 삭제 0. C-등급(F16/F17) 원래대로 보존(이번 변경과 무관).

## 변경 이력 (v22.17 → v22.18)

**출력 포맷 디폴트 문서 동기화 — `jpg`→`png` 표기 치환만. 기능·구조·명세 0 변경 (코드 파일도 0).**
- 메모리 #17(v22.17에서 PNG 디폴트 확정)을 지침 본문에 반영: 합성 산출물 저장 확장자 `.jpg`→`.png`, 변수명 `out_jpg`→`out_png`, "composed.jpg"→"composed.png" 표기 통일.
- 근거: 메모리는 PNG인데 본문은 jpg로 남아 **drift 씨앗**이었음 → 단일 소스 정합.
- F01~F17 명세·구조 **글자 단위 보존**. `quality=100` 인자 유지(PNG에선 무시됨, 무손실).
- C-등급 보호코어(F16/F17)는 원래부터 PNG 출력 → 무관, 명세 100% 보존.

## 변경 이력 (v22.16 → v22.17)

**규칙/문서 통합 — 기능 100% 보존, 코드 파일 0 변경.**
- 절대규칙 1+2+3 → 1번 단일화(코드 파일 불변). 구 4~10번 → 2~8번 재번호.
- "포맷 미지정 → 둘 다" 공통 원칙 §공통 원칙으로 추출(오버레이 ① reels=OPA30 특칙은 보존).
- §0 매니페스트 포인터-온리화(명세 SSOT). measure()·case3 PYEOF2 정의 권위 단일화 + 인라인 사본 명시.
- 분리 유지(통합 함정): 폰트 스펙(카피라이트 Regular 29 ↔ 오버레이 Bold 76/78), 절대규칙 7 vs 8(d).

## 변경 이력 (v22.15 → v22.16)

**절대규칙 8번(버전 갱신 프로토콜) 2단화 — 과발동 보정.** 코드 변경 없음.
- 풀/경량 2단 분기. 풀=버전 갱신·구조 변경·기능 추가/삭제. 경량=단일 파라미터 in-chat 튜닝(C-등급 보존확인만).

## 변경 이력 (v22.14 → v22.15)

**회귀 방지 메커니즘 도입 + 카피라이트 정합성 패치.** 코드 변경 없음.
- §0 기능 매니페스트 신설, 카피라이트·경고문을 "보호 코어(C-등급)"로 승격.
- 버전 갱신 프로토콜 신설. 카피라이트: 플랫폼 미지정 시 괄호 생략(`--raw`), 포맷 미지정 시 post+reels 둘 다, CLI 호출법 시그니처 캐시 등록.
