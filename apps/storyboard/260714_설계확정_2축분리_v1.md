# 스토리보드→영상 파이프라인 · 설계 확정 — 2축 분리 v1 (운영자 260714 07:44~07:58 확정)

> 정본 위계: 이 문서가 `HANDOFF_이식스펙_v1.md`의 **아키텍처 절(§1·§2)을 노뮤트 레포 실정에 맞게 덮는다**(스킬·비용·도구 매핑 절은 HANDOFF 그대로 유효). 다음 세션은 이 문서부터 읽을 것.

## 0. 역할 3분담 (영화 제작 은유 — 운영자 확정)

| 요소 | 역할 | 실체 |
|---|---|---|
| LLM (오퍼스 4.8 / 페이블 5) | **감독** — 스토리보드에 생략된 카메라 워킹·템포·기승전결을 스스로 파악해 연출 지시 | 세션/헤드리스 `--model` 스위치 |
| 시댄스 2.0 (힉스필드 MCP 경유) | **촬영팀** — 감독 디렉션대로 실제 프레임 생성 | `generate_video` model=`seedance_2_0` |
| 스토리보드 + 캐릭터 시트 | **설계도** — 동작·배경·컷 전후 관계를 정의, 감독이 순수 연출에 집중하게 하는 뼈대 | 스킬 5종 산출물 (`.claude/skills/`) |

**감독 성향 매핑 (운영자 확정 · UI 추천 로직에 사용):**
- `페이블 5` = 액션·서사·디테일 조정이 필요한 **역동적인 영상**의 감독
- `오퍼스 4.8` = 광고처럼 정적이거나 감정선이 중요한 **잔잔한 영상**의 감독으로 충분
- → 화제(주어진 주제) 성격에 따라 감독 추천을 분기하되, 최종 선택은 운영자.

## 1. ⚠️ 기존 클링(/k) 레인은 보존한다 — "대체"의 정확한 뜻

- **클링은 안 날린다**(운영자 260714): 200만원 상당 구독이 무료로 확보돼 있고, 프롬프팅만 잘하면 품질이 잘 나옴. **단 클링은 API/MCP가 없어 자동화 불가** → 수동 복붙 레인으로 유지.
- "기존 영상 프롬프팅 메뉴를 대체"의 뜻 = **영상 탭의 중심 플로우가 스토리보드 파이프라인으로 재편**되는 것이지, 기존 /k 프롬프트 빌더(kling·veo·seedance 문법 산출) 엔진 삭제가 아님.
- 결과 = **촬영 2레인**이 스토리보드 하류에서 분기:

```
감독 선택(오퍼스/페이블) → 화제·이야기 입력
        → [설계도] 마스터시트(V2, Element 락용) + 콘티(storyboard-v1 · 12컷)
        → 운영자 승인 게이트 (텍스트 콘티 = 0크레딧 → 시트 이미지는 선택)
        → 촬영 분기:
             A. 클링 레인(수동·무료) — 기존 /k 엔진으로 Kling 복붙 프롬프트 번들 산출
             B. 시댄스 레인(자동·크레딧) — seedance-continuity-builder → 힉스필드 MCP
                generate_video(seedance_2_0) · 생성 직전 get_cost 프리플라이트 = 필수 게이트
```

## 2. MCP 연결 (운영자 확정 절차)

- **대화형(claude.ai/Claude Code 세션)**: 설정 → 커넥터 → 커스텀 커넥터 추가 → 힉스필드 MCP 주소 붙여넣기. 주소 = `https://mcp.higgsfield.ai/mcp` (힉스필드 웹 상단 MCP 메뉴에서 복사). **본 레포 작업 세션엔 이미 연결·인증 완료** — 260714 실측: `balance`=10크레딧·free, `get_cost`(seedance 4s 기본) = 18크레딧.
- **헤드리스 러너(GitHub Actions `claude -p`)**: 커넥터 방식이 안 통하는 영역 — `--mcp-config`(type:http + 토큰) 실측이 **M2 블로커**. 후보 = 힉스필드 API키/CLI 토큰. M2에서 판정.

## 3. 이 설계가 HANDOFF와 다른 점 (요약)

| 항목 | HANDOFF v1 | 확정본 (이 문서) |
|---|---|---|
| 앱 형태 | Agent SDK 신규 웹앱 | **기존 노뮤트 뷰어 + 헤드리스 러너 확장** (신규 앱 없음) |
| Claude 인증 | API 키 레인 (구독 OAuth 금지 이슈) | 러너 기존 구독 OAuth 그대로 (Claude Code 경로 = 문제 소멸) |
| 촬영 엔진 | 시댄스 단일 | **클링(수동) + 시댄스(MCP 자동) 2레인** |
| 모델 선택 | Claude 모델만 | **감독(클로드 2종) × 촬영(클링/시댄스) 2축 분리** |

## 4. 미결 (다음 유닛)

- [ ] M2: 헤드리스 러너 힉스필드 MCP 인증 실측 (§2 블로커)
- [ ] 영상 탭 대체 UI 시안 — 신규 등장 요소 = 배치 사전 승인 필수(§디자인 3-2) → 플레이그라운드 HTML로 제시 예정
- [ ] 힉스필드 크레딧: 잔액 10 — 시댄스 테스트(4s·480p·fast=6) 이상은 충전 or 3일 무료 Plus 트라이얼 결정 필요
- [ ] 예행연습 1회: 세션 내 화제→콘티(0크레딧)→시트 가안(1k·low ≈1크레딧) 실측
