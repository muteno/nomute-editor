# 🗂 세션 작업 큐·리스크 레지스터 (260620 · 컨텍스트 증발 방어용 SSOT)

> ⚠️ 운영자 지시: "앞에 있는 거 다 리스크화하고 기록해서 놓치지 말고 해결, 해결 후엔 디버깅." 이 파일은 그 *durable 기록*이다(채팅 증발/압축 대비 · CLAUDE.md §🧠). 항목 완료 시 줄 갱신, 전부 닫히면 결론을 `docs/curation-algorithm.md` §7/§8 + CLAUDE.md TODO로 접고 이 파일 archive.
> 작업 브랜치 = `claude/google-search-keyword-method-u2vigy` · PR #565(draft) · **머지는 전 항목·구글검색 마무리까지 홀드**.

## 진행 상태 표
| # | 항목 | 상태 | 정본 위치 |
|---|---|---|---|
| A | **카드별 구글검색어 = AI 디자인싱킹**(풀문장→최소명사) | v1 구현완(PR#565) · **운영자와 프롬프트 추가 튜닝 예정**(아직 부족) | `prompts/card-make.md` §검색어 설계 · `viewer/index.html` parseCards/srchrow |
| B | 타이틀 우상단 복사버튼(우측·높이중앙·`.copy` 계승) | ✅ 완료(PR#565) | `viewer/index.html` open()·`#mdbody h1 .titlecopy` |
| C | 메타줄 기자명 다음 줄바꿈(WRITE·IN 2줄) | ✅ 완료(PR#565) | `viewer/index.html` overviewMeta·`.ov-nl` |
| D | **픽 PICKING 기기간 동기화**(B안=기존 pending 재사용) | ✅ 구현·테스트(9/9 상태머신) — api/pending `key` 노출 + 뷰어 `pickState`(로컬+서버 통합). 매칭=검증된 `_normU`(PFAIL 패턴)·파이프라인 무변경=주변부 | ↓ §D |
| E | **desync 버그 클래스**(요약수정 marker·썸네일재생성 stage·트리아지) | ✅ 트리아지(PASS/👎/확인) 동기화 구현(운영자 승인) · 진행마커는 로컬OK 문서화 | ↓ §E |
| F | 하단 **실패 토스트 → 누르면 실패기사 원문 새탭**(지금은 수집함탭만→또 클릭) | ✅ 구현(이번 커밋) | `viewer/index.html` showFailToast onclick=failOpen |
| G | **긴급(속보) 토스트 → 누르면 수집함+해당 후보로 스크롤**(지금은 눌러도 이동 무·액션 없이 이동만) | ✅ 구현(이번 커밋) | `viewer/index.html` showToast onclick=scGoTo |
| H | **요약 2차수정(revise) Thread 500자 초과 방지** — analyze 컷이 revise에 미적용 | ✅ 구현(이번 커밋) | `.github/scripts/revise.sh` 프롬프트 하드캡 |
| J | **검색이미지 소스무관화** — URL의 og:image 의존 폐지, 분석 Claude가 WebSearch로 관련기사 유추→og:image(품질·속보 필터) | ✅ 구현·테스트(6/6) — paste·차단매체도 관련이미지 채움. 항상(모든 기사). ⚠️ 라이브는 다음 분석런 | ↓ §J |
| I | **썸네일 검색후보 '속보 표지' 재유입** — 이투데이 속보 og가 또 뚫림(전엔 다른 속보표지·로그) | ✅ 구현·단위테스트(7/7) — 기사단위 일반화(제목 `[속보]`면 유사 컷). ⚠️ etoday 403라 라이브 검증은 다음 런/로그 확인 | ↓ §I |
| Z | PR #565 머지 + 머지 후 `unsubscribe_pr_activity` | ✅ 완료(`44819bc` · read-back 검증·웹훅 해제) — **B·C·F·G·H·A v1 라이브** | §수정모드 ④⑤ |

## 🥷 분신술 10인 감사 결론 (desync 버그 클래스 · 260620)
**원리(9번)**: 진행/완료 상태의 진실원본 = *이미 전역인 repo 산출물*에서 유도, localStorage는 낙관적 오버레이. 레포가 이미 3/4 실천(픽완료=`pickInFeed`·수정완료=`a.rev`·썸네일완료=이미지 `?v=` 시그). **빠진 건 중간 진행표시뿐.**

| 표면 | 저장 | desync | 이미 동기화 | 처분 |
|---|---|---|---|---|
| **픽 PICKING/중복발사** | `nm_ratings` localStorage(+`ratings.jsonl` write-only·되읽기 0) | 🔴 중복픽=구독쿼터 출혈·pickedAt 로컬 | PICKED 완료=`pickInFeed` 전역 ✅ | **D 구현** |
| 트리아지(PASS/👎/ack/score) | `nm_ratings` localStorage | 🟡 거른 후보 타기기 재등장·ack는 ledger에도 없음 | score/memo는 취향=ledger누적 본질 | **E: 운영자 확인 후**(개인상태 vs 공유 판단 필요) |
| 썸네일 재생성 stage(THUMB_REDO) | in-memory Map(영속도 X) | 🟡 새로고침·타기기서 증발(운영자 증상 정확) | 완료이미지 `?v=` 전역 ✅ | **E: 데이터손실0 → 표시만·후순위**(서버 redo.json 마커 옵션) |
| 카드 재생성(REPENDING) | in-memory Map | 🟡 동일 | 완료이미지 전역 ✅ | E: 후순위 |
| 요약수정 marker(REVISING) | `nomute_rev_pending` localStorage(영속O) | 🟢 "수정중" 물결만 로컬 | 내용+회차색 `a.rev` 전역 ✅ | E: 로컬OK 허용·문서화만 |
| breaking/pickfail seen | localStorage | 🟡 토스트 재알림(단 웹푸시 dedup은 서버 `push/sent.json` OK) | — | E: 트리아지 동기화되면 대부분 자연해소 |
| **거짓양성(이미 전역 ✅·손대지 마)** | — | — | 카드슛 status.json·대기열팝업(api/pending)·후보목록(api/candidates)·피드/요약내용·카드이미지 | — |

**기틀 판정(10번)**: "뷰어가 서버상태 read-back/유도"로만 하면 전부 **주변부**(즉시 가능). `api/pending`·`ratings.jsonl` **스키마/새 API 손대면 기틀 승격**(§기틀 보호+분신술). B안=기틀 회피 경로.

### §D — 픽 PICKING 동기화 구현 설계 (B안 · 4번 감사 정본)
- **api/pending에 매칭키 노출**: items에 `key = normU(line1)`(paste는 ''), `picked`(파일명 `-pick-` 여부) 추가. `functions/api/pending.js` L47·66.
- **⚠️ 정규화 동치 선결(최대 위험)**: candidate.url=scraper `normalize_link`(쿼리재구성) ⊋ 뷰어 `_normU`(끝슬래시만). 어긋나면 매칭 거짓음성. → `scraper/pick_pending.py` L44가 line1에 `normalize_link(url)` 기록 **또는** api normU=normalize_link 동치화(전 계층 단일 정규화).
- **뷰어 재배선**: `pickingNow`/`pickSettled`/`pfailHas`(L3710·3722·3730)에 `pendStatus(c)`(api/pending 라이브) OR 추가. localStorage=낙관 오버레이 유지. 폴은 `pollPicking`에 `loadPending()` 합치고 pendingNow 가드+visibility로 비용 절제.
- **엣지**: RSS자동 pending(`*-NN.txt`·inputs.analyze=='true' 게이트라 희소)은 `picked:false`로 PICKING 오표시 차단 / 전문픽(`*-pick-*`)·paste 케이스 / GitHub API 폴 비용.
- **문서화**: 머지 시 `docs/curation-algorithm.md` §7에 "픽 cross-device 중복발사·pickedAt 로컬·ratings read-back 부재" 박기(현재 Failed표면만 적힘).

### §E — desync 나머지 처분
- 썸네일/카드 재생성 stage·요약수정 marker = **완료는 이미 전역, 진행표시만 로컬·데이터손실0** → "로컬OK"로 문서화(서버 진행마커는 가치 대비 과함·후순위).
- **✅ 트리아지(PASS/👎/확인) cross-device 동기화 구현(운영자 승인 260620)**: (B)오버레이 방식 = `to_candidates.py`(큐레이션 알고리즘) **무변경** = 주변부.
  - 체인: 뷰어 `toggleAck`→`postRate`(acked 추가) → `api/rate.js`·`rate.yml`(acked 입력) → `rate_record.py`(acked ledger) → `build-viewer.mjs`가 `scraper/ratings.jsonl` url별 최신 축약 → `viewer/triage-state.json`(읽기전용·gitignore·`_headers` no-cache) → 뷰어 `SRV_TRI`/`loadTriage`/`srvTriage`가 `renderScrap`·`makeScItem`·`breakingNow`에서 **로컬 nm_ratings 아래 깔아 병합**(로컬=이 기기 최신 우선·픽은 D 담당 분리).
  - 테스트: 병합 로직 7/7(서버 dismissed/pass 반영·로컬 우선·acked 표시) · build-viewer 실행=triage-state.json 42건 생성 · check_refs·node·py_compile OK.
  - score·memo = 개인 취향(ledger 누적 본질)이라 동기화 제외(로컬OK).

### §I — 썸네일 검색후보 '속보 표지' 재유입 (조사 대기)
- 증상: 썸네일 '🔎 검색 이미지' 후보에 **이투데이 속보 표지**(기사 대표사진 아닌 매체 placeholder/속보 배너)가 또 들어옴. 전에 한 번 막았으나 **다른 속보 표지였고**(로그), 이번 건은 뚫림 = 기존 필터가 *특정 이미지*만 막고 *클래스*를 못 막음.
- 정본 = `.github/scripts/thumb_gen.py` `fetch_article_images`(og:image + 본문 유사사진 추출·엄격필터). 기존 차단 로직(로고/광고/유튜브/작은썸네일/마커매체 컷)의 유효성 실측 → **'속보 표지' 일반 차단**으로 보완(매체 placeholder·고정 배너 패턴: 파일명/경로/해상도/반복사용 시그). 차단매체(SBS·이투데이) 관련기사 폴백 경로도 점검.
- **요구**: 기존 조치 유효성 검증 → 일반화 보완 → 테스트 → 보고(운영자 명시). 실측·로그 기반.
- **✅ 진단(실측)**: 피해 카드 `260620-2234`의 유사 = `etoday.co.kr/news/view/2595584`(label 유사). 그 etoday url은 **alt_urls(운영자 클러스터 멤버)에 있음** → 마커폴백 아니라 **신뢰경로(클러스터)로 들어온 속보기사의 og가 [속보] 배너**. og:image는 `add()`에서 `_PLACEHOLDER`만, 유사는 `_small_dim`+`_BODY_SKIP`만 거치는데 **정상 파일명 배너는 전부 통과**. 기존 조치(본문img 폐지·260620)는 *본문 그래픽*만 막고 **og 선언 배너는 미커버** = 누락 근본.
- **✅ 보완(일반화)**: `_is_breaking_article(html)`(제목 og:title/twitter:title/`<title>`에 `[속보]`·`【속보】`·`(속보)`·`<속보>` 마커) → `fetch_article_images` 유사 루프서 **속보기사 og 컷**(클러스터 멤버여도). URL-특정 차단(다른 커버에 또 뚫림) 대신 **기사단위**. 정본 = `.github/scripts/thumb_gen.py`.
- **✅ 테스트**: 단위 7/7(속보 4변형 컷 · 정상/오탐"속보적"/빈html 보존). py_compile OK.
- **⚠️ 한계(정직)**: etoday가 이 샌드박스서 403(데이터센터 WAF)이라 *그 기사 제목에 실제 [속보]가 있는지* 직접 확인 못 함 → **라이브 검증은 다음 픽/재생성 런 로그로**(또는 운영자가 로그의 og:title 확인). 만약 배너가 *비-속보 제목* 기사에도 뜨면 og:image URL 패턴 정밀차단 추가 필요(로그 URL 필요).

### §J — 검색이미지 소스무관화 (운영자 260620 · "관련 이미지가 소스 무관하게 나와야")
- **요구**: 검색이미지를 *기사 URL의 og:image*에 의존하지 말고, **분석 Claude가 그 사건의 관련 기사 이미지를 유추·수집**(품질 낮거나 속보 대표배너는 컷). paste(전문)·차단매체도 나와야. 범위=**항상(모든 기사)**.
- **메커니즘**: CSE 死라 키워드→이미지 불가 → 실제 이미지=관련기사 og:image뿐. ∴ 분석단계 Claude(WebSearch)가 접근가능·사진좋은 관련기사 URL 2~3개를 frontmatter `image_sources`에 출력 → `thumb_gen`이 거기서 og fetch.
- **구현**: `prompts/news-analysis.md`(image_sources 필드+지시·전 기사·속보/저품질 제외) · `analyze.sh`(awk가 AI frontmatter 보존=자동 흐름·무변경) · `thumb_gen.py`(parse_md image_sources·`fetch_article_images` 소스무관 리팩터[원기사 og→image_sources→alt_urls→마커, paste면 image_sources가 대표부터]·process_one 조건 `art_url or image_sources`).
- **품질 필터**: `_is_breaking_article`(속보 제목 컷)·`_small_dim`·`_BODY_SKIP`·`_PLACEHOLDER`·매직바이트(http_image).
- **테스트**: fetch_article_images 6/6(paste·속보컷·원기사속보→소스·폴백) · py_compile·check_refs·build-viewer OK.
- ⚠️ **비용·정직**: 분석마다 WebSearch 추가(구독 쿼터·시간↑·운영자 '항상' 선택). AI가 좋은 소스를 *실제로* 잘 찾는지는 **다음 분석런 로그/뷰어로 검증**(best-effort·못 찾으면 빈값→AI썸네일 커버). 품질 안 좋으면 지시 튜닝.

## ⚠️ 미해결·후속(이번 커밋 외)
- D 픽 동기화 구현(위 §D 설계대로).
- E 트리아지 동기화 = 운영자 확인 받고.
- 분신술 8번 부가발견: **revise엔 CF 재빌드 훅 없음**(thumb-redo엔 있음) → 수정 반영이 기기별로 더 늦게 뜸. R2 고정키 이미지가 `_headers` no-cache 밖 = 엣지캐시 일시 desync. (후속 검토.)
- A 구글검색 키워드 = 운영자와 프롬프트 톤 추가 튜닝(맨 마지막).
