# 뷰어/모달 UIUX 개선 — 작업 스펙·체크리스트 (260614)

> **이 작업의 단일 진실원본.** 컨텍스트 압축돼도 이 파일 읽으면 그대로 이어간다(사용자 재설명 불필요).
> 작업 중 상태는 ⬜→✅로 되채움. 라이브 검증 후에만 ✅.

## 전역 제약 (G) — 절대 불변
- **G1** `viewer/index.html`만 수정. **본문 지침(`apps/news/*`·`prompts/*`) 무수정.** (예외: 카테고리 표면화에 `build-viewer.mjs` 1곳 — 아래 C 참조. 이것도 *기존 데이터 표면화*지 지침 아님.)
- **G2** **기존 데이터만** 사용 → 새 데이터 생성·역파이프라인 0. 가용 필드:
  `cards.state` · `a.file`(YYMMDD-HHMM = IN시각) · `a.date`+`a.time`(WRITE=보도시각) · `a.category`(옛 큐 frontmatter에만) · `a.bias` · `a.tags` · `a.url` · `a.title` · `a.summary` · `a.body` · `a.guidelines_version` · `cards.images/md`.
- **G3** 품질 저해 0(기존 동작·다른 섹션 안 깸).
- **G4** 누락 0. 검증 = JS `node --check` + `node build-viewer.mjs` + `python3 shared/check_refs.py`. 증빙 = 스크린샷 + main 라이브 + `git show origin/main` 실측.
- **G5** 심미 프리미엄(2026급 UIUX).
- **G6** **60fps 보장**(고주사율 폰 120fps): 모든 애니 = **transform/opacity/filter(GPU 컴포지트)** 기반. main-thread 레이아웃 속성(width/top/left) 애니 금지. ⚠️ shimmer·연결선 흐름은 `background-position`(메인스레드) 대신 **`transform: translateX` 오버레이**로 구현(샘플의 background-position → 본구현서 교체). will-change 적정 사용.

## ⚠️ 데이터 정합 메모 (감사 결과)
- 라이브 자동경로 프롬프트는 현재 `category`(5분기)를 **안 찍음**(#136 폐기). 옛 큐 파일엔 잔존.
- `bias`(편향)·`tags`(민감 통제어휘 — **현행 정본 = `prompts/news-analysis.md`·viewer `SENS_PROTECT`**, 260614 당시 5개에서 현 7개로 확장)는 정상 표면화 가능.
- → 카테고리는 *있는 데이터만* 표면화 = 옛 기사 표시·새 기사 빈칸 가능. 지침 재반영은 사용자 몫(본문 무수정 원칙).

---

## A. 진행바 ⇄ NMT 통합 (메인)
- **A1** ⬜ 대상 = 카드 하단 4단계 진행바(`progGauge`/`.prog` — 실패·요약·프롬프트·카드). 현재 라벨 안 보임·뭉침. (`viewer/index.html` CSS L199–219, JS L527–546)
- **A2** ⬜ 2026급 UIUX로 교체 → ✅ **B안(세그먼트 캡슐) 채택**(사용자 픽 260614). 3칸 막대(요약·프롬프트·카드): 완료=초록 그라데 채움 · 진행=shimmer 빛흐름 · 대기=흐림 · 실패=레드칸. 라벨 위. NMT 점프=각 칸 클릭(N/M/T).
- **A3** ⬜ 라벨 가독(문자열 또렷이 보임).
- **A4** ⬜ 안 뭉치게(여백·간격 충분).
- **A5** ⬜ **NMT 하이퍼링크 nav를 진행바에 통합**(둘 따로 X → 진행바 스텝이 곧 점프 버튼). 별도 `.nmt` 레일 제거.
  - 점프 대상(NoMuTe): **시작**(모달 top) · **N**=인스타 본문(요약) · **M**=카드뉴스 프롬프트 · **T**=카드 이미지(모달 팝업에 리로드된 생성 이미지).
  - 매핑(진행 상태 ⇄ 점프): 요약=N(IG) · 프롬프트=M(text_done) · 카드=T(done). [해석 — 사용자 확인 가능]
- **A6** ⬜ **하이퍼링크 정밀화**(현재 "대충 언저리" = 개짜증 포인트). 각 섹션의 **바로 위 구분선**을 기준점으로, 그 구분선이 **팝업 스크롤 최상단(스티키 헤더 바로 아래)**에 오도록 정확히 점프. (스크롤 컨테이너=`<dialog id=dlg>`, offset=`.dlg-h` 높이 보정.)

## B. 날짜 — IN vs WRITE
- **B1** ⬜ 두 날짜 구분 표기: `IN 6/14(일) 11:00` + `WRITE 6/11(목) 11:00`.
  - IN = 큐 적재시각 = `a.file` 앞 YYMMDD-HHMM 파싱. WRITE = `a.date`+`a.time`(보도시각).
- **B2** ⬜ **강조는 WRITE**(IN 아님).
- **B3** ⬜ IN은 **오늘 일자일 때만** 강조.

## C. 카테고리(5 대분류) — 순수 UI 분류 (지침에 없음 확정)
- **검증결과(260614)**: `apps/news/01`에 사회/경제 분류 스킴 **없음**. 옛 큐 frontmatter `category`엔 사회·정치·경제 잔존(build-viewer 미참조). → **UI 분류 기능**으로 처리(DB화·지침 무수정).
- **5 대분류(고정 UI 리스트)**: **사회**(정치·시사·사회 포함) · **경제** · **문화** · **국제**(외교 포함) · **테크**(과학 포함).
- **C1** ⬜ 옛 큐 `category` 값을 5버킷으로 **뷰어 UI-side 매핑**(정치→사회, 외교→국제, 과학→테크 …). build-viewer가 기존 `category` 필드 1줄 패스스루(=기존 데이터 표면화, 새 DB 아님). 새 기사·미분류=빈칸 graceful·필터서 제외 안 함.
- **C2** ⬜ **기사 타이틀 좌측**에 표기(대충 던지지 말 것).
- **C3** ⬜ 세로정렬: 타이틀 세로영역 16 기준이면 카테고리 글자 ~8, **타이틀 높이 가운데 정렬로 딱 맞게**.
- **C4** ⬜ 타이틀과 적절 간격·심미 미려.
- **C5** ⬜ **도형/박스 X** — 글자색만 구분. **쨍한색 금지**(은은). **이전 세션에서 쓰던 카테고리 색 참조**(리서치 필요 — `_versions/`·#134 카드분류개편).

## D. 편향(bias) 색상
- **D1** ⬜ 편향 텍스트 색 매핑(사용자 명시 — 한국 관례 무관, 이대로):
  - 약우 → 약간 파란색 · 우 → 파란색
  - 약좌 → 약간 빨간색 · (좌 → 빨간색)
  - 중립 → 강조색보다 연한 강조색
  - (※ "우"는 약보수 보정으로 거의 안 나옴)
- **D2** ⬜ `좌우 무관(N/A)` → 그냥 **"무관"** 으로 표기.

## E. 민감 키워드
- **E1** ⬜ 노란색 박스(`.tagchip.warn` 앰버) 제거 → **글자만 흰색** 표기(박스 X).

## F. 모달 섹션 헤더 = 번호 부제 (⚠️ 이모지 없음 · 3 점프 섹션 통일)
- **F1** ⬜ 요약 phase 헤더: **이모지 없음**(현 📄 제거 — ✍️ 아님. 사용자 정정 260614). 텍스트 **"1. 기사 요약 · 시사점"**. 부제 스타일(현재보다 키우되 상단 기사 타이틀보단 약간 작게). 대상 = `#ph-sum > summary`. 옆 지침버전 칩(gvChip) 유지.
- **F2** ⬜ 카드 phase 헤더: **"2. 카드뉴스 — 텍스트·프롬프트"**(가운데점 · 포함, 무이모지), 글씨 키움(부제). 대상 = `cardSec` sumlabel(`text_done`). gvChip 유지.
- **F3** ⬜ 3번째 점프 대상(카드 이미지) 헤더도 **"3. …"** 부제 형태(무이모지)로 통일(NMT의 T 도달점).
- → **3 섹션 번호 부제(무이모지)**: `1.기사 요약·시사점` / `2.카드뉴스—텍스트·프롬프트` / `3.카드 이미지`. A5 NMT가 이 셋으로 점프, A6 정밀 앵커.

## G. 앵글 후보 배지 콜론 제거
- **G1** ⬜ `앵글 후보 ①  :  "…"` → 배지(`.angle`) 뒤 ` : ` **항상 제거**.

## H. 섹션 부제 괄호 → #연회색 해시태그
- **H1** ⬜ `활용 옵션 (콘텐츠화 후보)` → `활용 옵션  #콘텐츠화 후보`(연한 회색).
- **H2** ⬜ `콘텐츠 초안 (카드뉴스 직전 · 복사용)` → `#카드뉴스 직전 · 복사용`(동일 연회색·동일 형태).
- (h2 괄호 부연 = `#`+연회색. 현 `.paren`(흰색) 규칙 교체.)

## I. 복사 버튼 진회색 일괄
- **I1** ⬜ `.copy`(현 강조 초록 오버레이) → **진회색** 일괄(모든 복사 버튼 동일 톤).

## J. 약보수 보정 = 편향과 같은 줄
- **J1** ⬜ 다음 줄로 내려간 `약보수 보정`을 **`편향:`과 동일 라인**으로 올림. (`decorate()` L658 `약보수 보정` 앞 `<br>` 제거)

## M. 편향 줄 불릿 말머리
- **M1** ⬜ 활용옵션 `편향:` 줄에 **• 말머리** 추가(앵글 후보 줄처럼).

## L. ⚠️ PIN 제거 (비용게이트 해제 — 사용자 인지필요)
- **L1** ⬜ '🚀 이미지 생성 & 카드 합성' 버튼 → **PIN(`#pindlg`) 게이트 제거, 바로 생성**(`makeCards` 직접 호출).
- ⚠️ **영향**: 이 PIN은 유료(제미나이/Cloud Run) 발사 암호보호. 제거 시 **누구나 유료 생성 트리거 가능**. CLAUDE.md 파이프라인 불변("슛=암호 게이트")과 충돌 → 사용자 명시 요청으로 구현하되 비용 노출 인지.

## O. ~~날짜 기간 필터(팝오버·달력)~~ — ❌ 폐기 (260614, R로 대체)
- O1~O6, Q2(대주제 선택 팝오버) **전부 제거**: 폰에서 팝오버+달력 = 번거로움. → 검색바 아래 칩(R)으로 단순화.

## R. 검색바 아래 필터 칩 (O·Q 팝오버 대체)
- **R1** ⬜ 기존 필터 팝오버·날짜 달력·대주제 선택 버튼 **제거**.
- **R2** ⬜ 검색바 **바로 아래**에 필터 칩 행 — **연회색 단색 pill**(스샷: 회색 둥근 칩 가로 나열·줄바꿈). 선택 시 톤 강조.
- **R3** ⬜ 칩 = **5 대분류** `[사회][경제][문화][국제][테크]`(C의 5버킷) + `[오늘 기사]` + `[민감 이슈]`.
- **R4** ⬜ 토글 필터: 카테고리 칩 = 서로 **OR**(선택 카테고리 중 아무거나) · `[오늘 기사]`(오늘=IN 적재일, 기존 dateHead 기준)·`[민감 이슈]`(tags 비어있지 않은 것만) = **AND** 조건.
- **R5** ⬜ 민감 이슈 = `a.tags` 있는 것만.

## P. 배너 TV 글로우 브리딩 애니메이션
- **P1** ⬜ 상단 배너(`.banner`=main_background.jpg, TV들+초록 글로우+오웰 문구): **TV 모양 유지**, 뒤 초록 불빛이 **반짝→은은히 어두워졌다(깜깜 X)→다시 밝아짐** 무한 브리딩.
- 구현: 글로우가 JPG에 구워짐 → CSS 시뮬(`.banner filter:brightness/saturate` 미세 진동 + 배너 뒤 초록 halo opacity 펄스). brightness 하한 고정(절대 깜깜 X). ✅ 가능.

## Q. ~~대주제 선택 팝오버~~ — ❌ 폐기 (R 칩으로 흡수, 260614)
- 5 대분류 선택 = R 검색바 아래 칩으로 통합. 팝오버 안 씀.

## S. 하단 네비게이션 바 (noMute 디자인 계승 · 미래 탭 스캐폴드)
- **S1** ⬜ 화면 **하단 고정 네비바** 추가 — 레퍼런스(모바일 bottom nav) 느낌이되 **노뮤트 UIUX 계승**(다크 글래스 + #0FFD02 accent + Pretendard + glassmorphism, safe-area-inset 대응). 활성 탭 = pill/accent 강조.
- **S2** ⬜ **미래 기능 스캐폴드** — 탭 셸 먼저, 내용은 나중에(플레이스홀더 OK).
- **S3** ⬜ 탭: **1 메인**(현 큐레이션 리스트) · **2 스크랩 DB**(자동 스크래핑 결과 — 지금 메인에 모이는 걸 별도 탭으로, 추후) · 나머지 TBD. [탭 구성·개수 확인요]
- 가능? → ✅ (추가 UI 셸, 데이터 무관).

## T. 이스터에그 — 검색바 위 달리는 캐릭터
- **T1** ⬜ 검색바 **바로 위**를 **작고 귀여운 캐릭터**(클로드 캐릭터가 방송용 카메라 든 모습)가 가로질러 뛰어가는 애니. 공식 스프라이트 없음 → **귀여운 SVG 자작/근사**(무리면 보편 뉴스 카메라맨). ≈24~28px, 너무 크지 않게·귀엽게.
- **T2** ⬜ 이스터에그 톤 = 가끔 등장(디폴트: 로드 시 1회 + 주기적 등장 / 시크릿 탭 트리거 고려). translateX run-across + 통통 튀는 보행(bob). [빈도 디폴트로 깔고 정정 가능]
- 가능? → ✅ (CSS 애니 + 자작 SVG 스프라이트).

## U. 검색바 placeholder
- **U1** ⬜ `#q` placeholder `"제목 검색"` → **`"Search"`** 로 변경.

## Y. 모달 헤더 버튼 restyle
- **Y1** ⬜ 우상단 `닫기` → **X 아이콘**(노뮤트 글래스모피즘 계승·텍스트 제거).
- **Y2** ⬜ 좌상단 `원문 ↗` → **검회색 글래스**(현 강조 초록 → de-emphasize, 글래스모피즘 유지). `↗` 유지.

## Z. 모달 요약 헤더 = 기본 개요 메타 (지침칩 제거)
- **Z1** ⬜ "1. 기사 요약·시사점" 우측 **지침버전 칩(gvChip) 제거**(요약 헤더에서). (카드 phase gvChip은 유지 — 재생성 추적용, [확인요].)
- **Z2** ⬜ 그 라인 높이에 맞춰 **기본 개요** 표기: **대분류**(정치 등) · **매체** · **기자명** · **WRITE**(작성일) · **INPUT**(적재일). (B의 IN/WRITE와 동일 데이터원.)
- 🟡 **기자명** = frontmatter에 없음 → 본문 Fact `출처: 매체(기자)` 패턴서 추출 시도, 없으면 생략. [데이터 갭]

## W. 실시간 시계 (날짜 헤더)
- **W1** ⬜ 날짜 헤더(`.dhead`/`#dhDate` 영역, "2026-06-15 월 | Today N")에 **현재 시각 실시간 표기 — 초까지**(`HH:MM:SS`, 로컬=KST). `setInterval` 1s 갱신, 멈추지 않고 흐름. (1s 틱이라 성능 무관.)

## X. 푸터 카피라이트
- **X1** ⬜ 페이지 **맨 아래**에 푸터: **`ⓒ 2025-26. 노뮤트. all rights reserved.`** (muted·중앙 정렬·작게). 콘텐츠 끝, 하단 네비바(S)와 **안 겹치게** 여백(safe-area+nav 높이) 확보.

## V. 프로필 메뉴(pmenu) 개편
- **V1** ⬜ 헤더: 깃 로고(`assets/brand/logo.png` 투명 워드마크) **적당 크기**(안 큼) + 부제 **"News curation"** *기울임 볼드*. (현 "노뮤트"/"뉴스 큐레이션" 대체 — 로고=워드마크라 노뮤트 텍스트 흡수.) **구분선 위 공간 약간 더** 확보.
- **V2** ⬜ **새로고침 = 텍스트 항목 제거 → 헤더 우상단 아이콘**(첨부 주황 동그라미 위치)으로 이동.
- **V3** ⬜ 항목 = SNS 바로가기 확장(노뮤트 prefix 제거·플레인 라벨, `↗` 외부링크):
  - **Home** → https://nomute.kr
  - **Instagram** → https://instagram.com/no_mute (기존)
  - **Thread** → https://www.threads.com/?hl=ko
  - **X** → https://x.com/home
  - **Facebook** → https://www.facebook.com/
  - **메시지** (유지)
- 로고 서빙: build-viewer가 `logo.png`를 `viewer/assets/brand/`로 복사하는지 확인(아니면 복사목록 추가 — 뷰어 빌드 영역).

---

## 🟡 확인요(블록 아님 — 진행하며 사용자 정정 가능)
- **A5 매핑**: NMT 3버튼 ⇄ 진행 3마일스톤(요약=N/IG · 프롬프트=M · 카드=T) 통합. "4분기(시작 포함)"는 시작=모달 top(기본). 이 해석으로 간다.
- **C 카테고리 데이터 갭**: 새 기사 빈칸 가능(프롬프트 미발급). 본문 지침 무수정 → 표면화만.
- **L PIN**: 비용게이트 해제 인지.
- **O6 필터 기준 날짜**: IN(적재일) vs WRITE(보도일)? — 기본 **WRITE(보도일)** 추정.
- **Q 5 대분류 목록**: 정확한 5종(사회·경제·…)? 데이터 distinct 추출 vs 고정 5종 — [확인요]. C와 동일 데이터원.

---

## ✅ 결정 확정 (260614 · ㄱㄱ)
- **L PIN = 제거 확정**(사용자 지출 모니터링·내일 조치·휴무).
- **C 카테고리 = 여기서만 UI 분류**(품질 무관). 확정.
- **Z 기자명** = 항상 표기, 없으면 **`-`**(본문 Fact 출처서 추출).
- **B = 분까지**(초 불요). *(W 실시간 시계는 초까지 유지 — 흐름 보이게.)*
- 아이디어 채택: ① 진행바 세그먼트 **N·M·T 글자 힌트** · **"전체" 리셋칩** · 카테고리 5색 OK · 시계 옆 **KST**.
- **T 이스터에그 = 카메라맨**, **터치 시 리액션(찰칵/플래시)**.
- **S 하단 네비 = 클릭 시 그 주변에 작은 팝업(모달 UIUX 톤)** 뜸(탭 전환 X·플레이스홀더·버튼 여러개·본인만 봄).

## 진행 로그
- 260614 16:0x **빌드 완료 — A~Z 전 항목 구현·7배치 커밋.** Opus 서브 2패널 검증: ① 스펙준수 100%(누락 0·결정확정 8건 반영) ② 런타임 회귀 0(🔴 0 / 🟡 reduced-motion egg 1건 보강 완료). JS·build·check_refs ✅. → main 머지.
- 260614 14:48 백업: `_versions/260614_1448_진행바_스텝퍼/index.html` (작업 전 원본).
- 260614 14:5x 스펙 캡처(A~M + 전역제약). 사용자 brain-dump + 스샷 8장 반영.
- (이후 구현·검증·라이브 기록 여기 누적)
