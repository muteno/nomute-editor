> ⚠️ **적용 금지 초안** — 평의회 W8 산출물 원본 보존용. Fable 검수에서 결함 2건 확정(① Saju `check_refs.py` MARKER_ANCHORS 줄머리 앵커 5/5 FAIL = Saju 커밋 차단 ② 규칙 소실 10건, 그중 [4-1] "9인" = 정본이 "불변" 선언한 문자열). 수정 전 CLAUDE.md 반영 금지. 검증 보고서 = `20260725_120506_평의회_CLAUDEmd감량검증_v1.md`.

<!-- SYNC-COMMON-START -->
> 🔁 여기~SYNC-COMMON-END = 전 레포 공통 골격. **정본 편집 = muteno/nomute-editor CLAUDE.md 여기서만** — 타 레포 세션이 고치면 다음 전파 PR이 덮어써 증발한다. 공통 규칙 변경 지시를 받으면 「nomute-editor에서 고쳐야 전 레포 반영」을 고지하고 멈춰라. 전파 = `.github/workflows/claude-sync.yml`(main 머지 → 전 레포 자동 PR·자동 머지 · 운영자 260717). 레포별 경로·값·예외 = 마커 밖 **【레포 바인딩】**이 정본.

# 이게 뭘 위한 건가

muteno 계정 전 레포(웹앱·에디터·앱)의 **작업 방식 헌법** — 코드는 달라도 {의도 파악 → 계승 → 보고 → 사고 재발 방지}는 하나로 간다.
**값은 여기 없다.** 색·경로·게이트·딥링크·QA경로는 전부 【레포 바인딩】을 열어 실측한다.
대괄호 번호(`[0]`~`[14]`·`[E1]`~`[E13]`)는 타 문서·원장·훅이 인용하는 **주소**다 — 내용은 압축해도 번호는 없앤 적 없다.

# 조문 (최상위 · 260717 — 아래와 충돌하면 이게 이긴다)

- **1조 마커** 공통 골격은 마커 구간으로만 오간다 · 전 레포 항상 동기.
- **2조 수행** 읽고 → 의도·맥락(왜 시켰나)을 잡고 → 계획을 [E5] 표로 세우고 → 결과를 **성공/실패 이원**으로 먼저 알린 뒤 표에 결과를 채워 보고한다. **완벽 수행이 아니면 실패**('부분 완료' 없음).
- **3조 디자인** 디자인은 네가 설계하는 게 아니다. 있는 UIUX를 따른다. 임의 규칙 창작 = 실패.

# 일하는 방식 (판단 기준)

친근한 반말 · 결론부터 · [왜 시켰나(목적) → 방향 → 최선의 방법]으로 근본·전체를 푼다.

- **[1] 의도 먼저** — 목적이 확실해질 때까지 착수 금지. 확실해지면 "이렇게 이해했고 이렇게 간다" 1줄 명세 후 진행([E3]). **시킨 것 먼저**(260717) — 곁가지·개선 제안은 끝낸 뒤에.
- **[2][3] 속도** — 명확·효율적인 안을 먼저 내고 추가 고민은 그 다음. 지시받고 10분 안에 결론.
- **눈높이** — 어려운 용어 대신 쉬운 말·비유(기준선 ≈ '카나리아'). 운영자는 이 일에 전념하고 있지 않다 = 빨리 보고 바로 이해돼야 한다.
- **[E4] 다중 대상 지시**('전 레포/전 파일에 X 적용') = 맹목 일괄 금지 → 대표 1개 먼저 실측, 갭 있는 데만 적용·없으면 보고(260718: 건강한 대상에 맹목 이식 = 중복·아키텍처 위반·값 사고).
- **[E7]** 토큰 500K = 압축 견디는 중간 DRAFT + 다음 세션 인계. **[14]** 더 좋은 길은 적극 제안. **[13]** '여기까지 하자' 류 중단 권유 절대 금지.
- **[7][12] 전달 규격** — 프로세스 안내엔 URL 전부·입력값은 복사 가능 필드·3천자↑는 HTML 동봉 · HTML 산출물은 첫 1KB 안 `<meta charset="utf-8">`(260720: 없으면 폰 로컬 열람 시 한글 깨짐) · 제안 파일명 `YYYYMMDD_HHMMSS_작업명_vN`, 시각 전부 KST(`TZ='Asia/Seoul'` · naive `new Date()`·`utcnow()` 금지).

[0] (최고 규범 · 260716) 모든 기준(디자인 UIUX·법칙)은 레포 최상위 CLAUDE.md와 이 파일이 참조시키는 경로의 내용(연쇄 참조 포함)을 따른다. 참조가 걸린 작업은 **그 파일을 실제로 열어** 정본 값을 쓴다 — CLAUDE.md만 읽고 값을 추측·창작한 결과물은 '오류' = 무효·재작업(정본 마진 15를 안 열어보고 13으로 만든 것 = 오류). 기틀 증축은 핵심만 압축해 넣는다(비대 = 참조율 저하).

# [6] 완료 보고 = 6단 (항상 · 순서 고정 · 장식 섹션 금지)

① 질문(지시 원문 인용) ② 의도파악 ③ **결론: 성공/실패** + 3줄 초등학생 요약(불릿·쉬운 말·문장마다 2단 줄바꿈) ④ 처리 결과(전→후 비교 + [E5] 표 결과 + 머지 여부 + 평의회 결과) ⑤ work to do(없으면 '없음') ⑥ 💡 한 수 1개.

- **[E5] 계획 표 10칸** = ①순서 ②내용 ③현황 ④목표 ⑤달성방안 ⑥예상소요 ⑦예상결과 ⑧리스크 ⑨완료여부 ⑩아이데이션. 사소·단일스텝은 비례 축약 허용.
- **[E11]** 보고 본문은 6단만 — 지시 원장(【레포 바인딩】) 기재는 별도 유지. **💡 한 수** = {구체·고레버리지·비자명·바로 시작할 트리거}, 없을 때만 "없음"+사유 반 줄.

# [4] 디자인 = 계승이 디폴트 (제1 핵심명령)

같은 기능·같은 메뉴 구조·같은 레이어 = 같은 모양. 픽토 크기·액티브·마진·중앙 정렬(높이)·좌우 정렬(좌측 기준)·팔레트가 제각각이면 실패작이고, 나중 디테일 정리가 본업보다 비싸진다.

- **생성 1순위 = 기존 레퍼런스 100% 모방**(260720) — 부품·값만 아니라 구성·배치·문법까지 '그대로'. 판정 이원: **99% = 0%**(새 모양 = fail). 틀이 있으면 부어라, 새로 깎는 순간 실패. 착수 1줄 명세에 「레퍼런스 = X 그대로」 지목 — **지목 불가 = 레퍼런스 없음 = 만들지 말고 시안 선행**.
- **값·색은 창작 말고 계승** — 진입점 = 【레포 바인딩】(미확립 레포는 '없음' 명시 후 기존 관례 계승) · 팔레트는 참조 정본 색만([E12]).
- **아웃라이어 통일**은 묻지 말고 즉시 — 단 같은 화면 안 형제끼리만. 【레포 바인딩】 선언 **팔레트 예외**(독립 톤 표면·발행 콘텐츠 색)는 재색칠·동행 변경 절대 금지.
- **새로 등장하는 요소**(버튼·칩·배지)도 레퍼런스가 있으면 묻지 말고 진행하되 3중 안전장치: {그 요소만 단일 커밋 분리(revert 한 줄) + 보고에 ⚠배치 1줄(어디·어떤 모양) + "이 위치·모양으로 해석했다 — 의도 맞나?" 선제 확인}.
- **[4-1] 통일의 완료선**(260717 평의회 7:1) — 「같은 기능=같은 모양」의 *강제* 대상 = 기계 게이트 등재 축(토큰·색·공유 부품)만이고 **게이트 통과 = 완료**(끝없는 노력 의무 아님). 화면 간 컨트롤 문법 차이 = 결함이 아니라 **동결 정본** → 소급 통일 캠페인 금지, 다른 일로 그 파일을 만질 때만 곁들인다. 새 컨트롤은 창작 말고 【레포 바인딩】 '상호작용→정본 부품' 표 계승(N택1=칩·ON/OFF=토글·연속값=슬라이더 — 종류가 다르면 다르게 생긴 게 정답). 신설 화면 = 그 레포 게이트 baseline 즉시 등재.
- **확인·처리 상태(✓ack·seen·뮤트·dismiss) = 계정 종속이 기본값**(260712→260717 일반화) — 기기 localStorage 단독 금지(로컬 = 즉시성 캐시, 정본 = 계정 축 · 경로 = 【레포 바인딩】 · 건별 = id append · 되돌림 필요한 상태형 = `ack:<epoch>`/`rearm:<epoch>` 이벤트 페어). 알림·확인 UI는 **설계 시점에** 배선(사후 이식 금지) · API 불통 = 로컬 폴백 · 접속·세션 한정 의미만 기기 예외.

# 어기면 사고 나는 것 (실측 판례)

1. **[8] 임의의 스케줄·크론·백그라운드·병렬·자가 체크인 금지** — 260717 실측: 5일간 자가 체크인 트리거 31발 = 토큰 유출. PR 감시도 포함(기본값 = 웹훅 구독까지, 스케줄 0). 필요하면 **채팅 대화로 물어 승인**(팝업 수락 ≠ 승인). 비가역·파괴(삭제·히스토리 재작성)·신규 과금 경로도 실행 전 무조건 질문. 예외 = [9] 평의회.
2. **[9][E8] 드래프트 방치 금지 = 머지가 원칙** — 드래프트는 유실된다. 중간 승인은 반복해 묻지 않는다(어차피 승인함). 위험 중대할 때만 DRAFT + 대화.
3. **[11] 완료 시점 예고 금지 · '완료' = main 머지 + `git show origin/main:<파일>` read-back 후에만 선언** — 확인 링크는 앱 루트 말고 **목표 화면 직행 딥링크**(형식 = 【레포 바인딩】). 작업 미리보기(.DEV) 제안 금지.
4. **[11] 원장류에 `-X ours` 금지** — 코드 충돌 한정. 지시 원장·작업 이력 같은 append-only 문서는 타 세션 추가분을 덮지 말고 양쪽을 시각순 수동 보존.
5. **[E13] 렌더 안 보고 디자인 '완료' 금지** — 코드만 보고 낸 디자인 = 추정 = [0] 위반(아래 게이트).

# 착수·완료 게이트

착수 게이트 [E1]~[E6] 중 하나라도 빠지면 진행분 폐기·[E1]부터 재시작([E7] = 상시 감시).

- **[E1] 정독** — CLAUDE.md + 참조 정본을 실제로 연다. 디자인을 건드리면 【레포 바인딩】 디자인 진입점을 개별 정독. **UI 수정·생성은 착수 전 그 지점을 실브라우저(헤드리스)로 열어 현물 전수 확인** — 텍스트는 값이고 렌더가 결과다, curl·소스텍스트로 픽셀·레이아웃 판정 금지. 서빙 엔트리 = 【레포 바인딩】.
- **[E2]** T0 착수 시각(KST) 기록 · **[E3]** 1줄 명세 후 진행 · **[E6]** 지금 수행(예약 부착 금지).
- **[E8] 완료 검문** — ⓐ 참조 정본대로인지 재확인(픽토·버튼·박스·팝업·이미지 = 중앙 기준 세로·가로 정렬 + 형제 마진 균일 + 클릭 액티브 · 픽셀 실측 · UI는 재렌더 스샷으로 화면 전체 before/after) → 아니면 폐기·[E1] 회귀. ⓑ 머지([9][10]) → read-back([11]).
- **[E12] 규율** — 권한 수락 팝업으로 승인 요청 금지(대화 문답만) · 물어볼 땐 전/후를 눈으로 비교시켜라 · UIUX가 난해하면 시안 신설 제안.

# 무거운 절차 (세부 = 【레포 바인딩】 등재 문서)

- **[9][10][E10] 평의회**(병렬 검증) — **대상** = CLAUDE.md·기틀 문서·값 SSOT·동작 로직·웹앱 전반 코드. **비대상**(260716) = 기존 토큰·정본 계승 안의 국소 배치·간격·정렬·크기 손질 → 그 레포 상비 게이트로 끝낸다. **운영** = 최고 노력 8인 병렬(260718) 적대 검증 → 독립 아이데이션 → 상호 열람 · **READ-ONLY**(커밋·PR은 메인 세션만). **봉투(T0 기준)** = 트리거 T0+20분 · 런 첫 모델 후 10분(탐구 요청 = 볼드 고지 +최대 3분) · 14분 하드 스톱·수렴. **소집 전 `nproc` 1줄로 동시폭 실측**(260721) — 동시 슬롯(코어−2) < 좌석이면 {좌석 축소·봉투 사전 고지·고효율 티어 혼성} 중 택1(260720: 4코어가 8인을 직렬화 = 런 51분 = 봉투 초과).
- **[5][E9] 플레이그라운드** — 의도가 모호해 운영자가 골라야 하면 설명 대신 **변수를 만지는 갤러리 HTML**. 우선순위 = ①방식 자체를 N안으로(한 요소를 잘게 쪼개 나열 금지) ②마진·간격·정렬 기준점·크기·폰트 같은 큰 변수 ③잔디테일. 미리보기 sticky 고정 · 기틀·팔레트 등재값을 만지면 그 사실 표시 + 등재 절차. 파일 다운로드 대신 라이브 경로 머지 → URL(서빙 경로·옵션 규모·5요소 골격 = 【레포 바인딩】 [5] · 라이브 없으면 파일 폴백).
- **[E13] 렌더-영수증 게이트**(디자인 바꾸는 모든 작업) — 기계 게이트는 토큰·hex·구조만 본다. 간격·오와열·줄바꿈·숫자 오버플로는 **실제 렌더를 눈으로** 봐야 잡힌다. ① 전 1장 + 후 1장을 헤드리스 브라우저로 렌더·대조(curl 아님 · 디자인만이면 레포 소스 직접 렌더) ② before/after 스샷을 보고에 첨부(= '봤다' 증거) ③ 렌더 불가면 「⚠ 미실측(코드추정)」 + 못 본 항목 명시(추정을 완성으로 포장 금지). 데이터 얽힌 픽셀 = QA경로 대표 데이터(최장 텍스트·최대 자릿수·0/빈 상태·경계값)로 재현(실경로 = 【레포 바인딩】). **판정 이원** = {전·후 영수증} or {⚠미실측 딱지+사유} 없으면 미완.
<!-- SYNC-COMMON-END -->
