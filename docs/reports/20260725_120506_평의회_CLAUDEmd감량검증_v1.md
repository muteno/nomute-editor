# 평의회 보고서 — CLAUDE.md 감량(Thariq 글 적용) 검증

- 소집 T0 = 2026-07-25 11:42:00 KST · 런 시작 11:46:30 · 8인 회수 11:57:09(**10분 39초** · 10분 봉투 39초 초과 · 14분 하드스톱 이내)
- 좌석 = OPUS 8인 병렬(READ-ONLY) + Fable 5 검수 2인 · 러너 실측 `nproc`=4 → 동시 슬롯 2([E10] 봉투 사전 고지 후 진행)
- 트리거 = 운영자 지시(Anthropic Thariq 「시스템 프롬프트 80% 삭제해도 코딩 평가 손실 없음」 글)
- 산출 = **미적용**. 아래 🔴 결함 때문에 CLAUDE.md 본체 무접촉으로 종료. 적용 = 운영자 GO 대기.

---

## 1. 결론 (수렴)

**감량은 옳다. 단 이 레포군에서 CLAUDE.md는 산문이 아니라 기계의 입력 파일이라, "지우기"가 아니라 "게이트 등재 라인은 남기고 산문만 압축"이어야 한다.**

- 무손실 하한 = **−21.8%**(W3 · 순수 중복 제거만)
- 실제 산출 = **−49.8%**(W8 · 마커 23,672B → 11,890B)
- 안전 상한 = **−60~70%**(W5 · 판례 아카이브 이관 포함)
- 이론 상한 = **−76%**(W4 · 스킬 이관 포함 · 단 전파 배관 확장 선행 필요)

수치는 서로 모순이 아니라 축이 다르다(F1 판정). 단 W8의 **"삭제된 규칙 0건"은 거짓** — 실측 10건 소실.

## 2. 🔴 치명적 결함 (적용 차단 사유)

| # | 결함 | 실측 근거 | 결과 |
|---|---|---|---|
| 1 | **개정안이 Saju 커밋을 죽인다** | `Saju/shared/check_refs.py:27` `MARKER_ANCHORS` + `:45` `re.search(r'^'+num, common, re.M)` = **줄머리 앵커**. 개정안은 `# [4] 디자인`·`- **[5][E9]`·`3. **[11]` 꼴 → `^\[N\]` 매치 실패 **5/5 FAIL** | Saju 전 커밋 rc=1 차단 |
| 2 | **리뷰모드 dispatch로는 자동머지를 못 막는다** | `claude-sync.yml:7-10` push(main, paths=CLAUDE.md) 자동 발화 + `:28` push엔 inputs 없어 `AUTOMERGE`가 무조건 `'true'` | main 머지 순간 6레포 자동 팬아웃 |
| 3 | **평의회가 제안한 "50% 가드"가 이 안을 못 막는다** | 0.5 × 23,671 = 11,835.5B < 개정안 11,890B (**54.5B 차이로 통과**) | 가드 무력 |
| 4 | 소실 10건 | `OPUS 4.8` 모델 지정 · `AskUserQuestion` 버튼 지정 · [E9] 대분류5·소분류10 · 마커 헤더 `automerge=false 리뷰모드` · '완료' 정의(전파 PR 전 레포 머지까지) · 2조5항 6칸 원문 · **[4-1] "9인"(정본이 스스로 "불변"이라 선언한 문자열)** · touch-then-align · 조문 ※각주 | 정본 위반 1건 포함 |

## 3. 채택된 진단 (근거 있는 것만)

### 3-1. 위상 선언 5중 + 자기부정 3건 (W1)
- "내가 최상위"를 5곳이 동시 선언: 기틀 조문 / 최우선 1~4 / [0] "최고 규범" / [4] "제1 핵심명령" / YETA "레포 제1"
- 자기부정: ①[E13] 같은 줄 안에서 「스샷 없으면 무효」 ↔ 「딱지만 붙이면 통과」 ②[8] 3줄 간격으로 「AskUserQuestion으로 물어라」 ↔ 「팝업 수락 ≠ 승인」 = **묻지도 진행하지도 못하는 교착** ③2조5항 6칸 ↔ [E5] 10칸
- 충돌 쌍 16개 실측. 즉시 삭제 TOP5만으로 7개 소멸.

### 3-2. 자기가 죽었다고 선언한 조항이 살아있다 (W3)
- `[10]` = 본문에 "이 봉투가 '10분 캡'을 대체한다"고 자기폐기 명시 + 전문 439B 잔존
- `2조 5항` = 주석에 "6칸 = 구정본"이라 적혀 있는데 6칸 열거 그대로

### 3-3. 비율이 뒤집혀 있다 (W3)
공통 프로세스 골격 23,622B(**73.7%**) vs 레포 고유 함정 8,445B(26.3%). Thariq 글은 "토큰 대부분을 gotcha에"라고 했다 — **정확히 반대**. 감량의 진짜 성과는 바이트가 아니라 이 비율(26%→34%).

### 3-4. 처벌 조항 4건이 손해의 대부분 (W2)
금지 46건 중 **12건은 비가역 방벽(유지)**. 손해는 ①게이트 누락=진행분 폐기·재시작(→**누락을 숨기는 유인**을 만듦) ②99%=0%(판정 방법이 없어 시안으로 도피) ③임의 규칙=실패(경계가 없어 사문화 or 과잉질문) ④오류=무효. 재작성 본보기는 이미 파일 안에 셋 있다: 「게이트 통과 = 완료」·「영수증 or ⚠미실측 딱지」·「금지+대표 1개 실측」.

### 3-5. 삭제 반대 근거 (W5)
- **"80% 일괄 삭제" = [E4] 자기 위반**("다중 대상 지시 = 맹목 일괄 금지 → 대표 1개 먼저 실측")
- 날짜 박힌 사고 이력(31발 토큰 유출·app-deploy 미반영·모바일 뷰포트·PNA 차단·워드프레스 figure 표 소실)은 Thariq 글이 **"여기에 토큰을 쓰라"고 지목한 바로 그 gotcha** — 글을 근거로 지우면 글을 거스르는 것
- 자백 11행(방어 실패 = 삭제 인정): 2조 逐条 · [6] · [2] · [14] · [E7]("세션이 자기 토큰을 못 잰다 = 실행 불가 조항") · [0] 뒷부분("이 줄이 있는데도 32KB까지 비대해짐 = 효력 0 실증") 등

### 3-6. 아직 어디에도 안 적힌 진짜 함정 25개 (W7) — 발췌
1. **clone마다 `git config core.hooksPath .githooks` 1회** — 안 하면 게이트 38개가 **조용히 미실행**(전 레포)
2. nomute-editor: 색은 `viewer/index.html :root` 1곳 → `python3 shared/build_design_mirror.py build`(안 하면 `check_palette_sync` rc=1)
3. nomute-editor: `check_anchor_liveness`(하드) — CLAUDE.md의 `§` 섹션을 지우면 죽은 앵커로 **커밋 차단**(감량 작업 자체의 함정)
4. YETA: `:root` 새 토큰은 `design-tokens.lock`에도 넣어야 커밋됨
5. YETA: 커밋 게이트보다 **Edit 직후 PostToolUse `design_gate.py`(exit 2)가 먼저·더 엄하게** 막는다
6. yeulmaru-promo: `src/index.js` = 앱 모듈 아님, **Cloudflare Worker + 15분 크론**
7. yeulmaru-promo 바인딩 「상비 게이트 = 없음」 = **오기** — pre-commit 3층(check_design→check_refs→smoke_login)이 실제로 커밋을 막는다
8. Saju: 빌드는 **레포 루트에서만**(app/ 안에서 하면 KB 없이 골격만 렌더)

### 3-7. 배관 실측 (W6·F2)
- 5레포 마커 **드리프트 0**(전원 sha 동일 · L1~L101 · 23,672B)
- **TARGETS에 6번째 레포 `muteno/TS`** 존재(이 세션 allowlist 밖 = 상태 미상)
- 마커 밖 무접촉은 **코드 보장**(`text.replace(old, block, 1)`)
- 롤백은 싸다: 콘텐츠 기반 span 교체라 **정본만 revert하면 기계가 6레포 자동 복원 ≈ 5분**

## 4. 실행 순서 (GO 시)

### 오늘 리스크 0
1. `claude_sync.py` `sync_one()` 리뷰모드 분기 직후·머지 PUT 직전에 **대량 감량 가드** 삽입 — 임계 **75% 잔존**(50%는 54.5B 차이로 무력). `.github/scripts/` 변경은 `paths:[CLAUDE.md]` 필터라 **팬아웃 비발화** = 안전
2. yeulmaru-promo 바인딩 [9] 오기 정정(마커 밖 = 전파 무관)
3. nomute_homepage·yeulmaru-promo 바인딩에 「계정 축 = 없음」 키 명시(추가만)
4. `muteno/TS` 마커 상태 확인

### 전면 감량 (선결 조건 충족 후)
1. 개정안 [4]/[5]/[6]/[9]/[11] 다섯 절 **줄머리 복원** → Saju 게이트 시뮬 5/5 PASS 재확인
2. 소실 10건 중 운영값 5건 복원(OPUS 4.8 · AskUserQuestion · automerge 리뷰모드 · '완료' 정의 · [4-1] "9인")
3. 배포: `gh workflow disable claude-sync.yml` → main 머지 → `enable` → `gh workflow run ... -f automerge=false` → 6개 PR 육안 리뷰 후 수동 머지
4. read-back: 6레포 마커 sha 일치 확인 + `python3 shared/check_refs.py`(Saju·YETA) rc=0

## 5. 운영자 결정이 필요한 미해결 1건

**[3] "10분 안에 결론" vs [E10] "T0+20분 트리거·14분 하드스톱" — 산술적으로 준수 불가.** 현행은 [10]에서 얼버무렸고 W8 개정안은 두 조항을 떨어뜨려 **충돌을 안 보이게 했을 뿐(해소 아니라 은폐)**. Saju 바인딩엔 "파도 단위 보고로 갈음"이라는 3번째 시계까지 있다. 선택지:
- (a) [3]을 "평의회·배치 제외 단발 지시 한정 10분"으로 조건화
- (b) [3]을 "첫 응답까지 10분"(응답 지연 금지)으로 재정의하고 완료 시각 제약 삭제

## 6. 첨부

- W8 개정안 초안(11,890B · **⚠ 결함 1·4 미수정 = 적용 금지**) = `docs/reports/20260725_120506_평의회_개정안초안_v1.md`
