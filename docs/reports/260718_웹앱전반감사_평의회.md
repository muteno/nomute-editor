# 웹앱 전반 따로노는·쓰레기 검증 평의회 (Q165 · 260718)

> 지시: 품질 유지·디자인 기틀 유지 방향으로 웹앱 전반의 "따로노는 부분·쓰레기" 검증 — **오퍼스 10 + 페이블 5 병렬 일괄 출발, 중간 재논의 1회, 오퍼스 통제관 1인**(유의미성 지적) · 후속 지시: 5분 제한 가속 + 힘든 표면 페이블 증원 마감.
> 제1 조건: **결론을 반영해도 품질이 떨어지면 안 된다** → 3중 관문(1차 발견 → 통제관 판정 → 2차 적대검증) 통과분만 반영.

## 1. 함대·타임라인
- 1차 탐구 15인: 오퍼스 10(표면 분담 — index CSS/JS · edit+thumb · song+track+sb · nb+ly+conv · k+enneagram+tokens · 공용JS/PWA · 데이터 · 게이트 · 문서) + 페이블 5(렌즈 — 지뢰지도 · 중복드리프트 · 실깨짐 · 고아파일 · 반영리스크). 전원 READ-ONLY(Explore).
- 17:34 T0 → 17:44 일괄 발진 → 17:57 운영자 "5분 제한·페이블 증원" → (세션 사용량 한도 도달로 일시 동결 · 리셋 후 재개) → 23:04 index CSS 페이블 재발진 + 통제관 소환 → 23:12 통제관 판정 → 23:15 2차 적대검증 3인(V1 ly/sb · V2 index 삭제군 · V3 k/track/게이트) → 23:2x 반영 3커밋 + 스모크 전 코어 PASS.
- 통제관 판정: 유의미 12 · 부분유의미 2(o7·o8) · 소음 0. 오탐 적출 = tokens.html '레거시그린' 오라벨(#0FFD02 = 현행 accent-5 정본값) · --press-* 인라인 재정의는 결함 아님(reduced-motion 인라인 필수 패턴).

## 2. 반영 완료 (3커밋 · main 머지)
**① 실깨짐 수리** — 라이브에서 실제 깨져 있던 것
- ly.html :root에 `--modal-glass-anchor`·`--modal-head-bg`·`--line2` 추가(index :root 정본값 계승). `.lypop`·`.lypop .mh`가 폴백 없는 var()로 참조하는데 ly는 tokens.css(구조토큰)만 링크라 **유리 배경·헤더 띠·보더 선언이 통째 무효(IACVT)** 렌더 → qpop 원문 계승 의도 복구. (260717 .lypop 신설 이식 때 무게이트로 출하된 클래스)
- ly.html ESC 핸들러 `stopImmediatePropagation` 추가 — 형제 6뷰어는 전부 '평의회 260713 ③' 주석과 함께 보유, ly만 미계승이라 **팝업 ESC가 부모 브릿지로 이어져 스튜디오가 통째 닫히던 실버그**(V1이 index 브릿지 dialog[open] 가드 구조까지 확인).
- sb.html :root에 `--line2` 추가 — `.geni-sechead` 소머리 밑줄 무효 렌더 복구(sb 전용 소비 = k 동시수정 비대상).
- track svh/vh 순서 역전(o4 발견)은 **신베이스에서 이미 수리돼 있어 무접촉** — 맹목 반영 차단([E4] 실측 우선) 사례.

**② 죽은 코드 정리 51줄** — 전건 참조 0 재실측(정적 DOM + JS 문자열 + smoke 어서션 + 구성도/CII 역참조)
- index: 편향 게이지 CSS 16줄(코드 주석 스스로 '호출부 0=도먼트·후속 정리 가능' 명시 · `--bias-*-rgb` 토큰은 soc-gauge 사용이라 존치) · 구 메시지목록 `.msgitem/.msgempty` 5줄(현행 = `.qrow.msgrow` 체계) · `.scrap-head/-title/-sub` 3줄(260713 설명 헤더 폐지 잔재) · `scGoTo` 죽은 함수 13줄(구 토스트 잔재) · `%SRC%` 치환 무효 블록 9줄(템플릿에 슬롯 없음 · 출처는 %META% 이관 완료).
- ly: `.paid`·`.orsep`·`.lyopt-e` 고아 3줄(conv의 `.orsep`은 실사용 = 무접촉). song: `.sronly` 1줄(track은 실사용 유지).

**③ 게이트 정합·문서 드리프트** 
- check_refs: **viewer JS 구문·아이콘 SSOT 게이트에 nb·sb 편입**(색 baseline엔 등재됐는데 이 두 게이트만 누락 — JS SyntaxError 시 뷰어 무경보 즉사 사각 · 편입 전 현행 rc=0 실측).
- _headers: **sb no-cache 계약 소급**(260714 CAP_TABS 신설분만 형제 열거에서 누락).
- 구식 '4뷰어(comp)' 표기 정정 5곳(comp = 260710 폐지): nm-svg.js 헤더 · 거울 빌더 템플릿(+build 재생성) · check_refs 독스트링 · SSOT §5 · 지도.md — 개수 열거 대신 'grep 실측 정본' 문구(개수 복붙 금지 자기규칙 정합).
- 지도.md: 죽은 `§🔒·§🎨·§📐` 라이브 포인터 2곳 → `[4]·[4-1]+SSOT §0` 재포인팅(내부 모순 해소) · 토큰 '108개' 하드코딩 2곳 → lock 정본 포인터(실측 109개 불일치). 구성도 허브: 부재 파일 링크 제거 + 구 §🎨 앵커 정정.

**검증**: 3커밋 각각 pre-commit check_refs 전 게이트 ✅ · 거울 build/check ✅ · 토큰 락 109 정합 ✅ · **smoke_all 전 코어 PASS(rc=0)** — 랭크 광학정렬 11/11 결정론 일치 · 팝업 글래스 패리티 8멤버 동일.

## 3. 확정·미반영 — 동결 규율([4-1] touch-then-align 대기)
그 파일을 다른 일로 만질 때 곁들여 정리(전용 캠페인 금지):
- k.html: kdd 드롭다운 클러스터(CSS 158-173 · 핸들러 · kddClose 호출)와 `.axchip.sq` — 260717 레일 이행 잔재, **창조부 0 전수 확정**(V3 · .axrow 다른 고객 0, ESC 핸들러도 현재 no-op이라 동반 제거 가능).
- edit.html: ly 복사 잔재 5심볼(lyPendSet·lyPendClear·LY_PEND_KEY·parseCues·lyResultErr) · `#lyOptPv` 참조 2곳 · `.dl-ack/.dl-ring` 고아 · `.poshint` 고아.
- ly.html ESC 이식 후속: `?done=` 파서 2변종(thumb doneReq 정본) · ly esc() 따옴표 미이스케이프(기계 산출 데이터라 실익 낮음 · conf .45).
- index: scGoTo 삭제로 `.sc-flash` CSS(2곳)+`scFlash` keyframes와 `repPar` 선언이 도먼트 전락(무해) — 다음 index 작업 때 곁들임.
- track 축 `?done=` 딥링크 부재(발신 track-make.yml도 bare · push-test는 발신하는데 수신 파서 없음 = 착시) — track을 만질 때 conv 485 블록+발신 URL 세트 계승.

## 4. 운영자 문답 대기 (비가역·스코프 결정 — [8])
- 루트 (N) 중복 zip 3종: `Shared (2).zip`(24K) · `링크 클릭 (5).zip`(20K) · `Screenshot_..._Instagram (2).zip`(1.3M) — 전 레포 0참조(원본만 apps가 참조). 삭제 승인?
- `viewer/ft_out/ftcanary1`·`fturlcanary1` — 260713 카나리아 런 잔재(결과는 작업이력에 텍스트 보존). 삭제 승인?
- 루트 `.skill` 번들 5개(≈21MB) — `.claude/skills/` 추적본과 중복. `_versions` 이동 승인?
- `viewer/love_marquee.webp` — 소비 0(마퀴는 canvas 렌더 + 260705 폐지 display:none). `_versions` 이동 승인?
- index `.fpop-*` 필터팝업 CSS(1140권·1569) — 참조 0이나 **CII 등재**(53·75행 별칭) → CII 행 철거와 세트로만 정리 가능. 승인?
- `viewer/tokens.html`·`viewer/enneagram/` 게이트 스코프 — 둘 다 의도적 독립(index 주석·README 확인)이나 게이트 목록엔 미등재. 통제관 권고 = **baseline seed 등재 또는 smoke_fresh식 '명시적 면제 선언' 중 택1**(암묵 사각으로 두지 않기).
- `.nomedia`(0B·참조 0) — 안드로이드 미디어 스캐너 마커로 추정. 의도 맞으면 README 한 줄.
- CLAUDE.md [15]에 `soc_lean.json` = 운영자 수기 config 예외 1줄 명시(기계 json과 오분류 방지 · o8).

## 5. 오폭 방지로 살린 것 (지뢰 지도 요약 — 이번 감사가 '안 건드린' 이유)
_FWD_UNUSED 40토큰=forward-declared 의도 · viewer/*.json=라이브 데이터(손편집 금지) · BUILD_STAMP=sed 앵커 · comp_out+comp-make.yml=HTML만 폐지·파이프라인 생존 · 확립본 v1/v2=append-only 증거 · vendored(marked/purify) · *_out 12종=워크플로 착지(?done= 이력 fetch) · 데이터 디렉토리(queue/pending/push/…)=functions 라이브 저장소 · _versions=백업 정본 · 마퀴 hidden()=운영자 260705 도먼트 결정 · K_MODELS draft·.seedrow=도먼트 주석 보존 · 5탭 발사문법 차이·dlStamp 동명이함수=동결 정본 · functions/api=URL 파일라우팅(import 0=정상) · check_refs rc=0은 docs/html/js/json엔 면죄부 아님(스캔 = md/py/sh/png).

## 6. 게이트 갭 제안 (통제관 발굴 · 미구현 — 기틀 증축이라 운영자 승인 후)
1. **댕글링 var() 검사 신설** — 뷰어가 자기 :root+tokens.css에 없는 var()를 폴백 없이 참조하면 경보. 이번 ly/sb 깨짐 클래스가 현행 게이트 완전 사각(check_design은 raw만, _new_dead_tokens는 index만). 최고 레버리지.
2. **게이트 뷰어 목록 SSOT 단일화** — 색 baseline·JS 구문·아이콘·tokens_link 튜플이 각각 하드코딩이라 서로 드리프트(nb/sb 증상 = 이번에 봉합). 단일 VIEWERS 상수로 통일하면 신설 뷰어 등재 누락 재발 차단. (tokens_link 715행 튜플의 nb/sb 부재도 이 축 — 이번엔 검증분(160·230)만 편입)

---

## 7. 집행 결과 (운영자 260718 "다 최선의 방법으로 처리해줘" 승인 — Q167 후속 집행)
**§4 문답 8건 처분**: ①(N)zip 3종 = git rm ✅ ②카나리아 2디렉토리 = git rm ✅ ③루트 .skill 5종 = git rm ✅(정본 = .claude/skills/ · HANDOFF 각주) ④love_marquee.webp = git rm ✅(생성기·원본 = _versions 보존) ⑤fpop CSS 8줄 + CII 별칭 = 삭제 ✅(#cardonly 상태 배선은 도먼트 축이라 무접촉 — 비CSS 사용 0 재전수 후) ⑥tokens.html·enneagram = **명시 면제 선언** 채택(check_refs VIEWERS 상수 주석 · 통제관 권고 택1) ⑦.nomedia = 무접촉·용도 추정만 원장 기록(안드로이드 미디어 스캐너 마커) ⑧soc_lean = CLAUDE.md [15] 예외 1줄 등재 ✅
**§3 동결분 처분(승인로 해제)**: k kdd 클러스터+.axchip.sq ✅ · edit 5심볼+#lyOptPv 2곳+.poshint ✅ · ly cpin 5줄 ✅(--bias-l2는 _CC 참조라 존치 — 댕글링 방지) · index sc-flash 3줄+repPar ✅ · **보류 3건(사유)**: edit .dl-ack(핸들러 이식 vs 삭제 양방향 · conf .6 = 확신 미달) · thumb ieReset(배선 여부 판단형) · track ?done= 수신(결과 렌더가 pend 상태기계와 결합 — id 단독 착지 안전 검증 = 별도 세션 규모 · 발신만 고치면 착시라 세트 보류) · ly ?done= 변종·esc()(실익 낮음 · touch-then-align 유지)
**§6 게이트 2건 구현 ✅**: check_dangling_var() 신설(하드 · 10뷰어 위반 0 안착) + VIEWERS_ALL/VIEWERS_TOOLS 상수 단일화(6개 게이트 치환 · nb/sb 전 게이트 동반 편입 = 자동완성·X문자·tokens_link·소머리까지 실측 그린)
**검증**: check_refs 전 게이트 rc=0 · smoke_all 전 코어 PASS · 기틀 변경 적대검증 페이블 2기 — W-A(게이트 코드) 5/5 통과(가짜 토큰 주입 검출 실증 · 개선노트 = 주석 스트립 defs 정밀화 · 실사례 0) · W-B(잔존 스윕) 결함 2건 적중 → 즉시 봉합: CII 3행 미동기(k행 .ksel/.sq 스크럽·vh-acts행 구 팝업 기술 정리·cpin 행 철거) + marquee_pet_gen.py 수동전용·산출 제거 주석
