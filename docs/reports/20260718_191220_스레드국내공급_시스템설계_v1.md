# 스레드(Threads) 국내 공급 시스템 설계 v1 — 운영자 260718 "미리 시스템 설계 ㄱ"

> **한 줄 결론: 파이프는 이미 다 지어져 있다(계정 원장 → 폰 수집 → 90분 채택 → xcard 카드). 막힌 곳은 코드가 아니라 운영 2곳 — ① 폰 크론 중단(2일 스테일) ② Meta 로그인월(쿠키 미설정 → 스레드·인스타 0건).** 이 문서 = 전체 사슬 박제 + 재가동 절차 + 남은 결정 3건.
>
> 방침(운영자 260718 확정 · 원장 Q154): 스레드 = **국내 대상 · 번역 비이식 · 골격은 블스·X와 단일 정본(xcard) 공유**(가상 주입 실렌더 9종 PASS 실증 완료).

## 1. 현행 사슬 실측(260718 19:10 KST — 전부 실제 열람 값)

```
[계정 원장] viewer/sns_accounts.json "threads" 키
    kr 10계정(koreadispatch·sbsnow_insta·mnet_official·newjeans_official·blackpinkofficial
              ·aespa_official·bts.bighitofficial·hm_son7·xxxibgdrgn·jennierubyjane) · gl 0
    상한 = 지역별 10 (3면 대칭 실측: viewer ACC_CAP == functions/api/snsacc.js CAP == scraper _REG_CAP)
       ↓ 편집 UI = 뷰어 설정 모달 스레드 탭(이미 시공됨 · data-p="threads" · /api/snsacc GET/POST)
[수집] scripts/phone_subs.py (+ phone_subs.sh 크론 진입점 · 폰 termux/맥 = 가정 IP)
    threads_subs(): 프로필 HTML data-sjs JSON 재귀 walk · 계정당 최신 포스트 · 콜 간 4s · limit 20
    THREADS_COOKIE env(부계 세션쿠키 · 선택) 있으면 로그인 요청 / 없으면 게스트
    지역 도장 = 원장 kr/gl 그대로 · 산출 = viewer/sns_subs_phone.json 한 파일(타 파이프 무접촉)
    ⚠️ 러너(GitHub Actions)는 Meta 데이터센터 IP 차단이라 이 폰 경로가 유일 공급원(재난문자와 동류)
       ↓
[채택] scraper/sns_trends.py main() — 폰 파일이 신선(기본 90분 · env PHONE_FRESH_MIN)하면
    x·insta·threads 축 채택 · 스테일/파손 = fail-soft(직전분 carry · 절대 비차단)
       ↓
[표시] viewer/index.html — subs.threads → 스레드 그룹 '구독 게시물 · 한국/세계' 접이 2군
    카드 = xcard 단일 골격([순위]@계정[변동] → 본문(링크 mut) → 좋아요·댓글·시각)
    0건 = 그룹 자동 숨김(조용한 공백 — 데이터 유입 즉시 등장 · 코드 변경 불필요)
```

## 2. 병목 실측 — 왜 지금 0건인가

| # | 병목 | 실측 근거 | 성격 |
|---|---|---|---|
| B1 | **폰 크론 중단** | sns_subs_phone.json `updated = 2026-07-16T21:03 KST` = 약 2일 스테일(채택 창 90분 초과) | 운영(폰 단말) |
| B2 | **Meta 축 0건** | 그 마지막 런에서도 x 20건 vs **threads 0 · insta 0** — threads_subs 경고 사인 "포스트 노드 0(로그인월·차단)"에 부합 · THREADS_COOKIE 미설정 추정 | 운영(쿠키) |
| B3 | (관찰) X 구독분이 2018~2022 옛 트윗 | 폰 파일 x[] time 실측 — 게스트 프로필 파싱이 상단 고정/인기 트윗을 채집한 것으로 보임 | 별도 축(이 설계 범위 밖 · 기록만) |

## 3. 재가동 절차(운영자 손순서 — 폰/맥에서)

**절차 A. 폰 크론 생존 확인·재가동** (termux)
```
crontab -l | grep phone_subs        # 등록 확인
bash ~/nomute-editor/scripts/phone_subs.sh   # 수동 1회 실행(수집 건수 stdout 확인)
```
→ stdout `threads N건`이 **0이면 B2(쿠키)로**, 1+이면 커밋 후 30분 내 라이브 반영(다음 sns-trends 런 채택).

**절차 B. 스레드 부계 쿠키 주입** (게스트 수집이 0건일 때만 · 본계 금지 = 자동화 감지 밴 리스크)
1. 폰/맥 브라우저에서 **부계**로 https://www.threads.com 로그인
2. 개발자도구 → Application → Cookies → `sessionid` 포함 쿠키 문자열 복사
3. 폰 env 파일에 주입(레포 커밋 절대 금지 — 기존 설계 그대로):
```
echo 'export THREADS_COOKIE="sessionid=…; ds_user_id=…"' >> ~/.nomute_phone_env
```
4. 절차 A 수동 1회 재실행으로 threads N건 실측.
- 쿠키 수명 = 통상 수개월 · **만료 신호 = threads 0건 회귀**(경고 로그) → 재발급 = 이 절차 반복.

**절차 C. 국내 계정 라인업(설정 모달에서 — 코드 0)**
- 진입: https://apps.nomute.kr/ → 설정(링크 메뉴) → 계정 → **스레드 탭** → 한국 군에 추가/삭제 → 저장(/api/snsacc).
- 현행 kr 10 = K팝·연예 위주. "국내 대상" 방침에 맞춘 **후보(뉴스·미디어 — 스레드 핸들 = 인스타 핸들 승계라 실재 가능성 높음 · 폰 1회 실측으로 확정)**:
```
jtbcnews  kbsnews  ytn_official  sbsnews  yonhap.graphics  netflixkr  wavve.official  kleague
```
- 상한 = kr 10(3면 대칭) — 초과 등재는 저장단에서 컷. 라인업 교체는 운영자 결정(D1).

## 4. 남은 결정(운영자 3건 — 결정 전엔 현행 유지)
- **D1. 계정 라인업**: 현행 K팝 10 유지 / 뉴스·미디어로 교체 / 혼합(예: 뉴스 5 + K팝 5). ※ 상한 10이라 "추가"는 곧 "교체".
- **D2. 쿠키 운용**: 게스트 실측 먼저(절차 A) → 0건일 때만 부계 쿠키(절차 B) 승인 여부.
- **D3. 스테일 가시화(선택 시공)**: 폰 공급이 멈추면 지금은 '조용한 공백'뿐 — 트렌드 수집 죽음 국면의 기존 문법(헤더 픽토 빛멎음 `hdr-stale`)을 스레드 그룹 헤더에도 물릴지(폰 파일 나이 > N시간 = 멎음). 승인 시 국소 시공(신규 컴포넌트 0 · 기존 문법 계승).

## 5. 설계 불변 조항(이미 성립 — 재확인용)
- 번역 파이프(carry·tv·볼드 마커) = **비이식**(국내 콘텐츠 = 원문이 이미 한국어 · 운영자 260718).
- 카드 = xcard 단일 정본 계승 — 스레드 전용 CSS·컴포넌트 신설 금지(Q154 실증: computedStyle 바이트 동일 · Δ0px).
- fail-soft 사슬 유지: 폰 스테일 = 직전분 carry → 그마저 없으면 그룹 숨김. 어떤 실패도 커밋·타 축 비차단.
- 쿠키·키 = 폰 env 전용(레포·Actions 시크릿 포함 금지 — 유일 소비처가 폰이라 옮길 이유 없음).

## 6. 검증 체크리스트(가동 후)
1. `sns_subs_phone.json` threads ≥ 1 · region kr 도장 확인
2. 다음 sns-trends 런 로그 "phone-subs 채택: threads N건"
3. 라이브 트렌드 탭 → 스레드 그룹 등장 · 구독 게시물 · 한국 군 카드 = 블스와 동일 골격(이미 실증)
4. 시각·지표 정상(좋아요·댓글·N시간 전)

— 작성: 260718 19:12 KST · 세션 203d751b · 실측 원본 = scraper/sns_trends.py L47~55·L600~653·L1119~1141 / scripts/phone_subs.py / viewer/sns_accounts.json / viewer/sns_subs_phone.json / viewer/index.html L3301·L10205
