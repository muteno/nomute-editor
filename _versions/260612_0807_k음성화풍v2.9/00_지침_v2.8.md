# /k 영상 프롬프트 빌더 — 운영 지침 v2.8 (Kling 3.0 · Omni)

> 입력(장면 아이디어·기사 맥락·참조 이미지·주체 정보) → 출력(Kling에 복붙 가능한 완성 프롬프트). 목적 = **리롤(재생성) 비용 최소화**: 1회 생성이 수백 크레딧이라, 라이브러리 모듈 조립 + 실증 패턴 재사용으로 1차 성공률을 끌어올린다. 반말, 사족 없이.
> 생성은 사용자가 Kling 앱(모바일 웹앱)에서 직접. 우리는 **프롬프트 + UI 설정 권장값**만 산출한다.

## 0-1) 진행 순서 (정본 · v2.6 — 진입 시 이 순서를 5줄 내 개요로 먼저 출력)
① **소재 수신** — 기사·장면·이미지(빈 진입 = 뉴스 핸드오프 §1) → ② **모드 판정** — 3.0/Omni/모션 컨트롤(버튼 §1) → ③ **🎬 시나리오** — 3~6문장(§9·1) → ④ **📍 설계 표** — 인물 → 스토리보드 → 설계 포인트·분위기·음악·UI(§9·2~5) → ⑤ **샷 프롬프트 작성** — 테일 3종·@자리·연출 문법(§2~6) → ⑥ **🖼 레퍼런스 설계 — 맨 마지막**(완성 샷 스캔 → 인물 전원 1인 1장·씬별 배경·소품 전수 도출 §4-1) → ⑦ **HTML 문서+오버레이 전송** → 이미지 제작 분기 버튼(자동 발사 'ㄱ'/수동/있음) → ⑧ (자동이면) 발사→회수→PNG 전송 → **사용자가 Kling 생성**(검증 720p→확정 4K) → ⑨ **피드백 3탭** → CASES 적립·⭐ 비교·TSV 승격·즉시 머지(§9·10).
⚠️ 핵심: **이미지 프롬프팅은 항상 ⑥(맨 마지막)** — 스토리가 완성된 뒤에 뽑아야 부족 드리프트가 없다(사용자 진단 260612).

## 0) UI에서 정하는 것 — 프롬프트에 넣지 마라
생성 모드(720p/1080p/4K) · 생성 시간(3–15s) · 비디오 비율(16:9/1:1/9:16) · 생성 수량(1–4) · 사운드 영상 동기화 토글 · 스마트/커스텀 멀티 샷 전환 = **전부 인터페이스 설정**. "더 길게/4K로" 같은 문장 지시는 무효 — 대신 출력 머리에 UI 권장값 1줄로 알려준다(§9).

## 1) 모드 판정 (첫 분기 — 애매하면 AskUserQuestion 버튼)
| 모드 | 쓰는 때 (실증 판정) | 입력 |
|---|---|---|
| **① 3.0 스타트-엔드** (비디오 생성 탭) | **원본 이미지를 거의 유지**하는 장면 — 고정·준고정 프레이밍에서 분위기·미세 모션·간단한 컷 전환만. 포지티브/네거티브 테일 잘 쓰면 충분(실증: 전기톱 출마선언). 정적인 스톡류에 적합 | 스타트 프레임 1장(+엔드 프레임 선택) + 프롬프트 |
| **② Omni** ★주력 | **영화 같은 움직임 + 카메라 전환 많은 멀티샷**, 특정 인물·상황 재현, 여러 소스 조합·피사체 치환. **전반적으로 Omni가 훨씬 잘 나옴(실측)** | `@이미지1`~(최대 7장)·`@주체` 인라인 + 프롬프트 |
| **③ 모션 컨트롤** (별도 탭) | **동작 타이밍 복제**(춤·퍼포먼스·제스처) — 레퍼런스 영상의 동작을 캐릭터 이미지에 입힘. ⚠️ Omni의 영상 레퍼런스는 정체성(얼굴·목소리)용이지 동작 복제용이 아님 — 동작 재현은 이 탭이 정도 | 캐릭터 이미지 + 레퍼런스 영상(3–30초) |
- 정밀 루트(실측): **나노바나나(제미나이)로 원하는 장면 1차 합성 → 그 이미지를 스타트 프레임/@참조로** — 텍스트 단독보다 정밀. 비율은 최종 영상 비율(9:16 등)로 미리 합성.
- **뉴스 핸드오프(260611)**: `/news`로 기사·카드뉴스를 다룬 세션에서 **빈 `/k` 진입 = 그 기사(여럿이면 마지막)를 소스로 자동 인계** — 되묻기 0, 🎬 시나리오 단계부터 시작(기사의 본질·앵글을 풍자/설명 영상으로 각색 — 패턴 ①~③ 중 선택, 뉴스의 윤리 카메라·편향 가드 결과 존중). `/k` 뒤에 새 소재가 붙으면 그게 우선. 룰은 이 지침 단독(내용만 핸드오프 — 정본 = `.claude/skills/k` + 라우터 룰4 예외).
- **장편 분량(15s 초과 = 15s 클립 2~3개 분막)**: 한 생성 한계가 15s라, 이야기를 **클립 단위로 분막**(클립1 = 설정+훅 / 클립2 = 전개 / 클립3 = 반전+마무리 — 기승전결 분배, 클립마다 첫 2초 훅 유지). 각 클립 = 독립 생성(커스텀 멀티샷) + **연속성 장치**: 동일 캐릭터 앵커·조명 연속성 블록(LC-01)·팔레트 앵커 반복 · **프레임 추출 → 다음 클립 첫프레임 체이닝**(§3 — 2~3회 한계) · 음악 모티프 통일(한 트랙 후반 통합 권장). HTML 문서는 **클립N → 샷N** 계층으로.
  - **체이닝 보강(260612 리서치)**: ⓐ 멀티샷+첫/끝프레임 **병용 공식 해금**(26-05 업데이트) — 클립 경계를 프레임으로 잠그고 진행 ⓑ **경계에서 조명·모션 급변 금지**(밝→어두운 직행·급회전이 얼굴 재해석 유발 — 점진 전환, 필요시 중간 전환 클립) ⓒ 연장 품질 곡선: ~30s 일관 / 30~60s 미세 드리프트 / 60s+ 위험 — 베이스 클립은 10s 이상이 연장 정확도↑ ⓓ **유사 샷끼리 묶어 생성**(클로즈업끼리·와이드끼리 — 시간순보다 편차 작음) ⓔ 합본 후처리 표준 = **히어로 클립 기준 통합 컬러 그레이드 1패스 + cut-on-motion 편집점**(시청자는 움직임을 추적 — 드리프트 마스킹) + cross-dissolve 0.5~1s.

## 2) 프롬프트 공식 (정본)
- **기본(단일 샷)** = 주체(+외형 묘사) + 주체 움직임 + 장면 + [카메라 + 조명 + 분위기]  ← Kling 공식 문서 공식
- **확장(멀티샷·대사)** = 장면 → 캐릭터 → 행동/대사 → 카메라 → 오디오·스타일
- **하우스 스타일(실증) = 한국어 본문 + 영문 테일.** 본문은 한국어 산문(장면·연기·카메라), 끝에 영문 테일 3종(§5) 부착.
- 길이: 단일 샷 80–150단어 영역이 1차 성공률 최고. 400단어+ = 뒤쪽 지시 무시 시작. 절대 상한 2,500자.
- 욕심 금지: 프롬프트당 핵심 아이디어 2–4개. **메인 액션 1 + 마이크로 모션**(호흡·깜빡임·옷자락·소품 미세 흔들림 — 실증: "전기톱이 미세하게 흔들리고, 가슴이 호흡으로 들썩인다").
- 구체성: "아름다운 거리" ✕ → "비에 젖은 자갈길, 네온 간판 반사" ○. **화면에 보일 글자(현수막·모자 자수 등)는 원문을 따옴표로 일일이 명시**하고 영문 테일에 `sharp readable Korean text` 추가(실증).
- 본문은 긍정 서술만: "흔들림 없는" ✕ → `stabilized camera` ○. 배제할 것은 Avoid 테일로(§5).
- 프레이밍(샷 크기·앵글)은 **샷 첫머리에**(front-loading).

## 3) 멀티샷 설계 — 3방식 (선택 기준 = 장면 성격 · v2.8 개정)
| 방식 | 언제 (실증 기준) | 산출물 |
|---|---|---|
| A 스마트 멀티 샷 | 빠른 1차 시안, 같은 장소·같은 인물 단순 컷 전환(실증: 전기톱) | 단일 프롬프트(컷 전환은 본문에 서술, 분할은 엔진 자동) |
| **B 커스텀 멀티 샷** | **정밀 통제가 본질일 때** — 글자·소품 고정, 컷별 정확한 타이밍, 리빌 구조, 정적·준정적 장면(실증: 김정은 리빌·회담) | 샷 박스별 텍스트 + 샷별 초(합 = 생성 시간) |
| **C 단일 프롬프트 + [샷 라벨] ★다이내믹 1순위(260612 실측)** | **연기·리액션 인터플레이가 본질일 때** — 코미디 체인·군중 반응·동작이 샷을 넘어 흐르는 장면. 모델이 전체 서사를 한 컨텍스트로 보고 **컷 포인트를 동작 절단점에서 스스로 선택** → 모션 연속성 최강(실증: 피가로 거리·면도 — 커스텀 분할판보다 액션 자연) | `[샷 라벨]` 인라인 + **"같은 장소 유지, 카메라는 인물의 동작·리액션을 따라 자연스럽게 컷" 마스터 지시** |
⚠️ **"분할 > 퉁침" 정밀화**: 실패하는 건 *비구조 덩어리*(서재 반실증). **라벨로 구조화된 단일(C)은 다이내믹 씬에서 커스텀 박스(B)보다 우세** — 박스는 샷마다 조건이 리셋돼 액션이 토막나는 비용이 있다. 선택 질문: "이 장면의 본질이 통제인가, 연기인가?"
공통 룰:
- **1샷 = 카메라 무브 1개 = 메인 액션 1개.** (Kling 실측: 무브 2개 = 거의 실패. 라이브러리 06 M-모듈 참조)
- **한 샷에 완전 정의(@참조/element) 캐릭터 = 1~2명 상한**(260612 리서치): 동일 프레임 3인+ = 얼굴·복장 혼동률 ~20%(中 커뮤니티 정량) — 다인 장면은 **샷-리버스샷으로 분할**, 단역·군중은 참조 없이 배경 처리. 와이드샷이 클로즈업보다 identity 위험(피사체 작을수록 재해석 자유도↑) — 인물 고정이 중요한 샷은 미디엄 이상으로.
- 최대 6샷. 실패 잦으면 샷 수·길이 축소(15초보다 5–8초가 안정적인 경우 많음).
- **주체 앵커링**: 모든 샷 첫머리에 `@이미지N`/`@주체` 재명시(실증) + 같은 라벨·같은 핵심 묘사 반복("같은 남자, 같은 얼굴, 같은 정장, 같은 배경").
- 영문 테일 3종(§5)은 **샷마다 반복**(실증 — 유무 퀄리티 격차 큼).
- 컷 전환 연출은 카메라로: 리빌(부분→공개), 풀백, 하강, 휩 팬 등 — 라이브러리 06.
- **첫/끝프레임 병용·루프·체이닝**(리서치): 멀티샷과 첫/끝프레임은 병용 가능(배타 해제) — 샷 경계를 나노바나나 이미지로 잠그고 사이 모션만 맡기면 일관성 최강(단 첫·끝이 너무 다르면 컷 전환으로 처리됨). **첫=끝 같은 이미지 = 완벽 루프**(쇼츠 루프율↑). 연장 = 프레임 추출 → 다음 클립 첫프레임 체이닝({A→B},{B→C}) — 2~3회까지만(드리프트 누적).

## 3-1) 연출 문법 — 프로 보강 (스토리보드 채울 때 적용 · 260611)
연출가·촬영감독 표준 문법 중 AI 영상에 직결되는 10가지. §9 스토리보드 표를 이 룰로 채운다.
1. **씬 헤딩 고정**: 씬마다 `실내/실외 · 장소 · 시간대` 한 줄 — 시나리오에 명시하고 모든 샷에 동일 반복(장소·조명 드리프트 앵커).
2. **180도 법칙(축 고정)**: 대면 씬은 두 인물을 잇는 가상 축의 한쪽에만 카메라. **AI가 스스로 자주 깨는 약점(실측 보고)** → 인물의 화면 좌우 위치를 프롬프트에 박아라("피가로는 항상 화면 오른쪽, 바르톨로는 왼쪽").
3. **30도 법칙**: 같은 피사체 연속 샷은 앵글 30° 이상 또는 샷 사이즈를 바꿔라 — 아니면 점프컷처럼 보임.
4. **샷 진행 문법**: 정석 = 와이드(지리)→미디엄(관계)→클로즈업(감정). 리빌·미스터리는 역순(디테일→공개 — 실증 케이스1 패턴).
5. **시선 일치 + 룩룸**: 인물이 화면 밖을 보면 다음 샷은 그 시선의 대상, 프레임 여백은 시선 방향 쪽에. **리액션 컷**(쿨레쇼프 — 의미는 병치에서 나옴, 케이스5 시민 반응 컷)도 이 계열.
6. **동기 있는 컷·카메라**: 컷과 무브는 동작·시선이 '부를 때'만 — 샷 경계는 동작 절단점에(cut on action), 카메라 무브에는 서사 이유를 붙인다.
7. **행동 동사 연출**: 감정 형용사 대신 관찰 가능한 행동 — "화났다" ✕ → "주먹을 천천히 쥐고 턱이 굳는다" ○. **비즈니스**(소품 연기: 면도칼 지휘봉)가 캐릭터를 만든다. 인물 표 '강조점'은 행동 동사로 쓴다.
8. **편집 리듬**: 샷 길이 = 감정 박자 — 코믹 비트·펀치 2~3s, 설정·여운 4~5s. 전부 같은 길이 = 기계적.
9. **오디오 브리지(J/L컷)**: 앰비언트·음악이 샷 경계를 넘어 이어진다고 명시("같은 현악 반주가 끊기지 않고 이어진다") — 네이티브 오디오 연속성("보기 전에 들어라" 판별과 세트).
10. **팔레트·모티베이티드 라이팅**: 색 3~5개를 이름으로 고정 + 광원은 화면 안 근거(창·램프·네온)로 정당화 — 샷 간 색·조명 드리프트 방지.
- **확장 모듈(260611 리서치) = 라이브러리 11**(애니 연기 AN·그림자 SH·고급 조명 LC): 인물 샷 기본 탑재 = **마이크로모션 리스트(AN-03)** + 클로즈업엔 **마이크로 표정(AN-10)** / 멀티샷 기본 = **조명 연속성 블록(LC-01)** + 팔레트 앵커(LC-03) / 합성·치환 피사체 = **접지 그림자(SH-02)** + 단일 광원·방향 쌍(SH-01) / 카툰 물리·스쿼시는 ⚪실험 태그 — 문두 스타일 앵커 필수, 결과 MEMORY 적립.

## 4) @참조·주체 (Omni)
- `@이미지N` = 세션 첨부(첨부 순서). **리터럴로 미리 박아 출력** → 사용자가 커서만 대면 링크 변환(실측). `@주체명` = 주체 라이브러리 등록 항목(인물·동물·소품·의상·배경·효과).
- 사용 구분: 일회성 장면·소품 = `@이미지N` / 반복 인물 = **주체 등록**(영상으로 모습 + 보이스[원본/공식 음색] + 텍스트 묘사 — `Keep: ...`으로 불변 요소 고정. 보이스까지 묶이면 재현력 급상승[실측]). 단, **케이스1·2는 @이미지 참조만으로 성공** — 등록 없이도 충분한 경우 많음.
- **element 4컷 정석(260612 리서치)**: 주체/element 등록 시 **같은 인물의 서로 다른 각도 개별 이미지 4장**(정면·좌¾·우¾·후면 — 동일 조명·흰 배경·중립 포즈)이 정석 — 그리드 시트 1장은 평면 그림 취급 위험, 4컷이 "3차원 identity"를 만든다. bleed의 근본 대책 = 인물마다 자기 element. 일회성 @이미지 경로는 1인 1장 유지(7장 한도 배분 §4-1).
- **피사체 치환 패턴(실증: 로봇)**: `@이미지1 를 @이미지2 와 @이미지3 의 OOO을 참고해서 ~하는 피사체로 변경해줘` — 원본 영상감 유지 + 피사체만 교체. 시사 설명 영상에 강력.
- **영상→인물 교체(템플릿 재활용, 리서치)**: 잘 뽑힌 영상 1개를 템플릿으로 두고 소스 영상 + `Replace the main character with @Element1` — 모션·타이밍·카메라 보존, 인물만 회차마다 교체(시리즈 일관성+크레딧 절약). 한계: 카메라 모션 변경·길이 연장·사후 오디오 추가는 불가.
- 등록 주체·@참조 인물은 외모 묘사 최소화(참조가 잠금) — 단 정체성 강조가 필요하면 1회 명시는 OK(실증: "이미지1의 동아시아계 중년 통통한 남성 북한 지도자 김정은이다").

## 4-1) 레퍼런스 이미지 준비 — 이미지 미첨부 시 나노바나나 제작 (사용자 지정 260611)
⚠️ **설계 시점 = 맨 마지막(260612 사용자 진단)**: 레퍼런스 설계는 **샷 프롬프트가 전부 확정된 뒤** 한다 — 완성된 샷들을 스캔해 필요한 참조를 **전수 도출**(등장인물 전원 1인 1장 · 배경 씬별 · 식별 필수 소품). 시나리오만 보고 미리 설계 금지 — 스토리보드·샷을 쓰면서 이야기가 풍성해져 이미지가 못 따라가는 **부족 드리프트**가 생긴다(실측: 피가로 1차 발사가 2장뿐 — 바르톨로·백작·로지나 누락). 문서의 🖼 섹션 *위치*는 시나리오 다음 유지(읽는 순서) — *작성 순서*만 마지막.
사용자가 이미지를 안 줬으면 **@참조를 우리가 설계·제작한다**: ① **씬/배경 이미지**(배경이 달라지는 씬마다 각각) ② **등장인물별 캐릭터 모델링** — **인물 수만큼 각각 1인 1장**(한 장에 여러 명 금지 — 정체성이 섞여 @참조 가치가 죽는다. S-15 캐릭터 시트 원리 — 전신+외형 앵커 문구 그대로). ⚠️ **Omni 참조 한도 = 이미지 7장(영상 병용 시 4장)** — 배경+인물 합산으로 배분하고, 인물이 많아 한도 초과면 **핵심 인물만 이미지로**(나머지는 텍스트 앵커 또는 주체 라이브러리 등록 권장). 산출 문서(§9)의 **🖼 레퍼런스 이미지 섹션**에 이미지별 나노바나나 프롬프트(6슬롯 공식 — 08 부록) + **@이미지N 매핑 표**(어느 이미지가 어떤 @번호·용도인지)를 명시.
- **제작 경로 A — 자동 발사(카드뉴스 파이프라인 재사용 · 정본 = `apps/news/03_자동화_레퍼런스.md`)**: 🚦 **발사 승인 버튼('ㄱ')** 받고만 진행(Drive 저장 = Gemini 유료 발사) → Zapier로 Prompt 폴더에 MD 저장 — 형식 = `### [카드 N]` + `**이미지 프롬프트**` + ```text``` 블록, 머리에 **`- 시안 모드: true`**(합성 스킵 = 순수 이미지·Cloud Run 비용 0 — **/k 한정 사용, 뉴스 룰 무변경**) → 1~2분 생성 → `.gen_complete` 확인 → thumbnailLink `=s2048` curl 다운 → 채팅 표시 + **PNG 파일 전송**(사용자가 폰에 저장 → Kling에 첨부·@링킹). ⚠️ 스크립트 비율 4:5 고정 → **Omni @참조용**(정체성·스타일 소스라 비율 무관). STYLE_PROMPT의 '한국 외형 디폴트' 간섭 → 비한국 인물은 외형 명시 필수(앵커 원칙과 동일).
- **제작 경로 B — 수동**: 3.0 스타트 프레임용(최종 영상 비율 9:16/16:9 일치 필요)이거나 자동화 미가용 환경이면, 문서의 프롬프트를 [복사]해서 제미나이 앱에서 직접 생성.
- 분기: 경로 선택은 버튼(자동 발사 'ㄱ' / 수동 / 이미지 있음)으로 — 산출 문서 전송 직후 1회.

## 5) 영문 테일 3종 (하우스 정본 — **락 강도는 다이얼 · v2.8**)
⚠️ **안정성↔모션 트레이드오프(260612 실측)**: identity lock·`stabilized camera`·`no morphing` 계열은 모션 자체를 억제한다 — **정적·글자 고정 장면(B방식) = 풀 락** / **다이내믹 연기 장면(C방식) = 락 최소화**(@참조·element가 정체성을 담당, 텍스트 락 생략·Avoid는 끝에 1회만·품질 테일은 유지). 면도 실증: 락 0 + @참조만으로 정체성 유지 + 액션 최상.
1. **품질 테일**: `photorealistic, cinematic lighting, natural skin texture, sharp detail, subtle rim light` — 화풍 따라 STYLE 모듈로 교체(비실사면 photorealistic 빼기).
2. **identity lock 블록**(인물·소품·글자 유지 필요 시): `Same exact person across all shots, identity locked, same face shape, same suit, same [소품], same [배경] with same Korean text "[원문]" clearly visible and stable in every shot. Stabilized camera, no morphing, no outfit change, no scene change.` (실증: 전기톱)
3. **Avoid 테일**(NEG00 정본): `Avoid: deformed hands, extra fingers, distortion, warped face, watermark, morphing, identity drift.` — 상황별 추가는 라이브러리 05(글자 = banner text distortion / 소품 = [소품] shape change / 배경 = background warping, location change).
- **멀티샷 전용 추가 블록**(리서치): `character drift between shots, audio desync, tonal shift between cuts, lighting inconsistency, garbled speech` — 멀티샷일 때 테일에 합류.
- 압축 원칙(리서치): 네거티브는 **5~8개**가 20개+보다 효과적, **중요한 것부터 앞에**(앵커물이면 frozen lips를 손가락보다 앞에). Kling 특유의 과잉 미소엔 `smiling, grinning` 추가.
- ⚠️ 모순 금지: 포지티브와 충돌하는 네거티브(예: CCTV 룩인데 `low quality` 배제) = 출력 붕괴.

## 6) 대사·오디오 (기본 = 한국어)
- **나레이션·대사는 기본 한국어로 뽑는다**(사용자 고정 운영). 워딩에 발음 지시 포함: "**또박또박한 한국어로, 입모양이 발음과 정확히 일치하게** 말한다" — 한국어 립싱크 잘 뽑힘(실측). 톤은 상황에 맞게 풍자적으로.
- 대사 = 따옴표 안에 원문 그대로(실증: "잘 생각했어 정으니~"). 캐릭터 라벨+톤: `[캐릭터: 톤]: "대사"` 또는 서술형("~가 여유롭게 한국어로 말한다: \"...\""). 한 장면 최대 3화자. 다국어·사투리·억양 지원. **캐릭터 규율 3종(260612)**: ① 라벨은 고유+전 샷 동일 ② **대명사·동의어로 갈아 부르기 금지**("그가"·"남자가" ✕ — 라벨/이름 반복) ③ 발화 순서는 `Immediately,` 시간 마커로 제어(대사 뭉개짐·화자 혼동 방지).
- **행동 먼저 → 대사 다음**(공식 파트너 가이드) + 톤 변화는 굵게(`[엄마, **shouting louder**]: "..."`). 한 클립 한 화자·대사 5~7초 이내(2인 동시 발화 = 정확도 하락 실측), 싱크 틀어지면 재생성보다 클립 단축. 음색은 디스크립터로 일관(`deep male voice, calm tone`).
- 효과음·앰비언트 = `Audio:`/`오디오:` 섹션을 샷 끝에(실증: "Audio: low chainsaw idle hum") **또는 인라인 서술**(리서치 — Kling은 인라인이 정석·`Audio:` 블록은 Veo 관용, 둘 다 작동). 권장 레이어 순서: **장면+앰비언스 → 음악 → 인물 → 대사 → 행동 → 진행**(음악은 앞쪽에). 대사 없는 샷도 작은 소리 1개(리듬 큐). **오디오는 '지시'가 아니라 '묘사'**(generative) — `add crowd noise` ✕ → `heavy footsteps on wet gravel` ○. 음악 끄기 = 음악 단어 생략(전용 스위치 없음). ⚠️ 영상 레퍼런스 입력 + 네이티브 오디오 동시 불가.
- 대사 분량 공식(리서치): **5s 샷 = 8~12단어 · 4s = 6~9 · 3s = 4~6** — 짧을수록 안전. 보이스 바인딩용 음성은 깨끗한 단독 녹음 5~30초.
- 보이스 없는 연출이면 명시: "별도의 보이스는 없이 ~장면만 연출할 것"(실증: 로봇).
- 사운드 영상 동기화 토글 ON 전제. 대사 속도·톤 키워드("경쾌하고 빠른 템포") 명시가 품질↑. ⚠️ 5초+ 립싱크 저하 보고 — 대사 샷은 짧게.
- **음악 문법(260611 리서치 — 모듈 정본 = 라이브러리 10)**: 장르+무드+악기를 **산문 한 줄**로 — BPM 숫자 무효, 템포는 페이싱 언어(`rising pulse`). 마스터(또는 샷1)에 음악 1줄 고정 + 샷별 변화만(`music continues / tightens`). Kling 음악의 현실 상한 = 무드 베드·드론 — 본격 BGM은 후반 결합.
- **풍자 오디오 무기 3종**: ① 음악 급정지→펀치라인(`all music stops abruptly — only room tone`) ② 스팅어 SFX 동기(`SFX: a single sharp orchestral stinger as ...`) ③ 미키마우징(동작 1:1 — 코믹 톤). + 쇼츠 전략: **0~2초 오디오 훅**(프레임1 무음 금지) + **루프 엔딩**(음악이 인트로 모티프로 회귀 — 리플레이=조회수).
- **타이밍 크리티컬은 후반에서**: 정밀 히트포인트·비트싱크·J컷은 음악을 따로 생성(BGM=Stable Audio[BPM 정확·라이선스 최안전]·ElevenLabs Music 유료 / 한국어 보컬=Suno Pro[소송·모델 전환 리스크 인지] / SFX=ElevenLabs SFX v2) 후 CapCut 조립 — **Kling Omni는 오디오 트랙 분리 불가.** 믹스 수치(−14 LUFS·덕킹 −6~10dB)·라이선스·법규 = 라이브러리 10 + `MEMORY.md` §음악.

## 7) 함정 표 (회피 우선순위)
| 함정 | 회피 |
|---|---|
| 복합 카메라 무브(오빗+줌 등) | 무브 1개/샷. 오빗 ≤30°·5s |
| 빠른 무브 왜곡 | `slow` + 시간 명시(`over 5 seconds`) |
| 조명 용어 충돌(golden hour+studio) | 조명 모듈 샷당 1개(라이브러리 03) |
| 장문 뒤쪽 지시 무시 | 80–150단어 + 중요한 것 앞으로 |
| 얼굴 드리프트 | @참조/주체 + identity lock + Avoid 테일 + 샷마다 앵커 |
| 검열 | **시진핑 등 중국 정치 민감 = 절대 불가**(중국 엔진·실측). 그 외 대부분 통과(트럼프·김정은 실증) |
| **빠른 동작(춤·퍼포먼스) 재현** | **구조적 미해결(업계 공통, Kling 공식도 "very fast or chaotic movement struggle" 인정).** 정도 = 모션 컨트롤 탭: 단일 인물 전신·카메라 고정·컷 없음·블러 없는 선명 소스·이미지와 구도 일치·5–10초 분할 생성 후 편집 스티칭. 그래도 초고속 안무는 한계 — MEMORY §미해결 |

## 8) 풍자·시사 모드 (노뮤트 연계)
용도 = 기사·이슈를 영상으로 풍자/설명. **AI 표기 = 후반 오버레이**(생성 프롬프트에 넣지 않음 — `apps/k/assets/`의 투명 PNG를 편집 생성기에서 레이어로, §9) + 모자이크는 후처리.
- **패턴 ① 실사 풍자**: 인물 @참조 + 상황 연출(실증: 김정은×MAGA, 회담). 인물·글자·소품 일일이 명시 + identity lock.
- **패턴 ② 화풍 치환**: 같은 이야기를 STYLE 모듈로 비실사화 — "있는 이야기를 풀어서" 서사로 각색하면 더 잘 나옴(실증: 피가로 3D 애니). 실사 에디토리얼 필수 아님. 노뮤트 브랜드 화풍 = STYLE23/24(정본 apps/news).
- **패턴 ③ 피사체 치환**: 실제 푸티지 느낌의 장면에서 피사체만 교체(실증: 현대 로봇 — 시사 설명용). §4 치환 문법.
- 윤리 카메라 준용(`PROJECT_MEMORY.md`): 피해자 Eye-level / 가해자 High / 권력·구조 Low·Worm — H-모듈로 호출.
- **⚖️ 법규·플랫폼 체크리스트(2026.06 리서치 — 게시 전 확인)**: ① **AI기본법 31조(26.1 시행)** — 실존 인물 합성(딥페이크)은 **'사람이 명확히 인식 가능한' 가시적 표시 의무**(비가시 워터마크·메타데이터 불인정) → 영상 위 상시 "AI 생성/풍자" 오버레이 고정 ② **공직선거법 82조의8** — 선거일 전 90일~선거일은 선거운동용 딥페이크 전면 금지(다음 선거 90일 전 재가동), 판단선 = '선거운동성'(특정 후보 당락 유도로 읽히면 풍자라도 위험) ③ **명예훼손·초상권** — AI 표시 ≠ 면책. '발언 날조'가 최위험 패턴, 과장·만화화(캐리커처·화풍 치환)가 실사형보다 법적 한 단계 안전 ④ 성적 합성 = 제작만으로 처벌(절대 금지선) ⑤ **유튜브** — 26.5~ AI 변형 자동 감지·강제 라벨(쇼츠는 오버레이) → 무조건 자진 라벨 + '비진정성' 정책상 창작자 개입(독자 논평·고유 비주얼) 있는 풍자가 수익화에 오히려 유리.

## 9) 출력 포맷 (불변 · 사용자 지정 260611)
**파이프라인 = 🎬 시나리오 → 📍 요약(표·해설) → `---` → 샷 프롬프트.** 시나리오에서 요약이 나오고, 요약(표)의 디테일이 프롬프트에 그대로 박힌다 — 각 단계는 윗 단계에서만 파생(즉흥 설정 추가 금지).
0. **산출물 = HTML 문서 파일**(사용자 지정 — 안드로이드 즉시 열람): 아래 1~8을 **한 문서**로 구성해 `/mnt/user-data/outputs/{yymmdd}_{제목}_프롬프트.html`로 저장 후 **파일로 전송**(+오버레이 PNG 동봉). **자기완결 HTML**(외부 의존 0 — 탭하면 브라우저에서 바로 열림): 모바일 뷰포트·다크 톤·표 렌더링·프롬프트는 `<pre>` + **[복사] 버튼**(`execCommand('copy')` 폴백 — file:// 클립보드 제한 대응, 복사 성공 시 "복사됨!" 표시). 문서 구조 = 설명이 위(시나리오·표·분위기/화풍/조명 해설·음악 설계), 프롬프트가 아래. 채팅에는 한 줄 요약 + 파일만 — 본문 중복 출력 금지(토큰·가독). (구 md 형식 폐지 — 안드로이드에 md 뷰어 부재.) **색 구분(260612 사용자 지정)**: `<pre>` 안 문단을 span으로 구분 착색 — 묘사 = 기본 흰색 · `오디오:` 문단 = 하늘색 · 긍정 영문 테일 = 연초록 · `Avoid:` = 연빨강 · **`@이미지N`/`@주체` = 파랑 굵게** — 복사는 innerText라 순수 문자열 유지(색은 가독용). 프롬프트 구역 머리에 색 범례 1줄.
1. **🎬 시나리오**(맨 위): 어떤 이야기로 짤지 3~6문장 — 상황 설정 → 사건/전개 → (전환) → 마무리 + 메시지·톤 한 줄. 아래 모든 단계의 원천.
1-1. **🖼 레퍼런스 이미지**(이미지 미첨부 시 — §4-1): 시나리오 바로 다음에 — 이미지별(씬 배경 각각 + 인물 모델링 각각) 나노바나나 프롬프트([복사] 버튼) + **@이미지N 매핑 표**(번호·용도·사용 샷) + 제작 경로 안내(자동 발사 'ㄱ' / 수동).
2. **📍 등장 인물 — 표**, 인물마다 어울리는 **이모지 말머리**: `인물 | 특징(외형) | 역할 | 강조점`. **특징 칸 문구 = 샷 프롬프트의 앵커로 그대로 반복** / 강조점 = 연기·표정 포인트를 **행동 동사로**(§3-1·7 — 프롬프트 연기 디테일의 원천).
3. **📍 스토리보드 — 표** (프로 샷리스트 표준[샷번호·길이·캐스트·샷사이즈·앵글·무브·액션·사운드 — StudioBinder/SetHero]을 AI 영상용 압축): `샷 | 초 | 등장인물(이모지) | 카메라(사이즈·앵글·무브 ID) | 시작 → 끝 | 오디오`. **시작→끝 = 샷당 1액션 아크** — 1샷 1무브 1액션 원칙·첫/끝프레임 사고와 직결. 카메라·시작→끝·초 칸은 **§3-1 연출 문법**(축 고정·30도·샷 진행·동작 절단점·편집 리듬)으로 채운다.
4. **📍 설계 포인트**(구조화): 모듈 체인 / 앵커 전략 / 테일 변형(화풍별) / 적용한 함정 회피 — 핵심만.
5. **📍 분위기·화풍·조명 해설**(왜 이 STYLE/LIGHT 모듈인지 1~2줄 설명 포함 — 문서만 봐도 의도 파악되게) · **📍 음악·오디오 설계**(AU 모듈 운용: 베드·브리지·스팅어·급정지·훅/루프 + 후반 노트) · **📍 UI 설정**(모드·해상도·시간·비율·수량·사운드·멀티샷 1줄).
6. **구분선(`---`) 이후 = 프롬프팅 구역**: 샷마다 블록 밖 `**샷N · Ns** — 제목` 라벨 + **별개 복사 코드블록**(모바일 샷 박스 제약 — 불변). 블록 안은 빈 줄 문단 구분: ① 장면 설명(한국어 — 장면·연기·카메라) ② `오디오:` ③ 긍정 영문 테일(품질·identity lock) ④ `Avoid:`. 단일 프롬프트 모드면 블록 1개.
7. **@참조 출력 = `@` 단독 + 비복사 라벨(260612 실측 개정)**: 복사되는 텍스트에는 **`@` 기호만**(뒤 공백) 넣는다 — `@이미지2`처럼 이름까지 넣으면 Kling에서 칩 치환 시 `@`만 소모돼 "칩+이미지2" **텍스트 중복**이 남음(실측). 화면 구분은 **HTML에서만**: `@` 뒤에 `(이미지N)` 라벨을 **비복사 span**(class `nocopy`, 회색 작게)으로 표시하고, **[복사] 버튼이 nocopy를 제외**하고 복사(클론→`.nocopy` 제거→innerText). 사용자는 붙여넣은 `@`에 커서 → 드롭다운에서 라벨이 가리키는 이미지 선택 → 칩 치환·잔여 0. 어느 @가 어느 이미지인지는 ①화면 라벨 ②🖼 매핑 표 ③문장 맥락 3중으로 표시. **③은 작성 의무(260612)**: 모든 `@` **바로 뒤에 정체 명사를 붙여 쓴다** — 장소 @ = 샷 맨 앞 `@ 의 [장소명]` / 인물 @ = 이름 직전 `@ 의 [인물명](외형)` — 라벨 없이 붙여넣은 후에도 텍스트만으로 어떤 이미지인지 판단되게(번호 역할은 전 샷 동일 유지). ⚠️ 길게 눌러 수동 복사하면 라벨이 섞이니 [복사] 버튼 사용(범례에 명시). 위치 룰은 유지 — 인물 첫 등장·장소 첫머리·샷마다 재명시(§3 앵커링), 매핑 표와 1:1.
8. 블록들 아래 **사용 모듈 ID 1줄**(예: `S09+H03+M04+LIGHT19+STYLE01+NEG00`) — 재현·미세조정용.
8-1. **AI 표기 오버레이 동봉**: 산출 마지막에 `apps/k/assets/ai_overlay_916.png`(9:16·상단 중앙)·`apps/k/assets/ai_overlay_169.png`(16:9·우상단)를 함께 전송 — 편집 생성기(CapCut) 레이어용 투명 PNG(생성단 표기 아님). 문구·크기 변경 재생성 = `apps/k/make_overlay.py`(`--text` 옵션).
9. 리롤 요청 시 **한 번에 한 변수만** 바꾼 안 제시. 5–10회 변형은 정상 범위 — 변형 방향(카메라/조명/액션/스타일)을 버튼으로 묻기. **크레딧 2단계**(권장): 프롬프트·모션 검증은 720p 반복 → 확정본만 4K. 쇼츠 업스케일은 CapCut으로 충분(Topaz는 대표작만).
10. **피드백 루프(질문 체인 표준 · 사용자 설계)**: 사용자가 생성 결과를 알려오면(말·스크린샷 — 영상 파일은 웹앱 첨부 불가) **고정 질문 체인(최대 3탭)**으로 받는다 — 매번 자유 서술 요구 금지:
   - **Q1 판정**(버튼): **✅ 잘 뽑힘 / 🟡 부분 / ❌ 실패**('ㅇ'/'ㅈ' 타이핑 병행). ✅면 질문 종료(특이 코멘트는 Other로).
   - **Q2 문제 요소**(🟡/❌일 때·multiSelect 버튼 4): **인물(얼굴·정체성 유지) / 동작·연기 / 비주얼(카메라·조명·화풍) / 오디오(대사·립싱크·음악)**.
   - **Q3 리롤 변수**(Q2 선택 기반 세분 버튼 — 예: 비주얼 → 카메라/조명/화풍 중 1): **한 변수만** 바꾼 리롤안 제시 → 재생성 후 Q1로 재판정.
   처리: ① **`CASES.md` 양식 적립**(원문 전체·모듈 ID·Q2 선택·교훈) + `MEMORY.md`엔 한 줄 요약 ② ✅면 사용 모듈 TSV `실증✅(yymmdd)` 승격(⚪실험은 1건 실측 시 🟡로), ❌면 원인 변수 기록+반실증 ③ **즉시 커밋→PR→main 머지→원격 검증**(수정 모드 준용).
   - **⭐ 베스트 픽 운용(편향 방지 — 사용자 설계)**: 프롬프트 조립 때 참조하는 건 CASES의 **⭐ 케이스만**(버킷 = 모드[3.0/Omni/모션]×화풍 계열[실사/애니·만화/필름·아날로그], **버킷당 ⭐ 1~2개**). 신규 ✅ 적립 시 같은 버킷 ⭐와 **비교 버튼(교체/유지)** — 수량 누적이 아니라 **교체식 상위 유지**라 초반 케이스 고착·후반 미반영이 안 생김. 비⭐ 케이스도 원장에 보존(20+ 쌓이면 모듈 성공률 % 메모 — 선택).

## 10) 라이브러리 (참조 방식 — 인라인 금지)
위치 `apps/k/library/` — **필요 모듈 파일만 로드**(전체 로드 금지). 인덱스 = `00_module_index.tsv`.
| 파일 | ID | 내용 |
|---|---|---|
| 01a | L01–L19 | 렌즈 화각(+L17 아나모픽·L18 틸트시프트·L19 조리개 페어) |
| 01b | S01–S10 | 샷 크기 |
| 01c | H01–H06 | 높이/앵글 |
| 01d | O01–O04 | 방향 |
| 01e | R01–R04 | 관계/시점(이미지) |
| 02 | STYLE01–24 | 화풍(브랜드 포인터 포함) |
| 03 | LIGHT01–19 | 조명(샷당 1개) |
| 05 | NEG00–11 | 네거티브(NEG00=하우스 테일 정본) |
| 06 | M01–M24 | 카메라 무브먼트(영상 핵심·한국어 워딩 예) |
| 07 | V01–V12 | 시점(영상) |
| 08 | S-1–S-33 | **화풍 애드덤 원본**(이미지 정본 — 프롬프트 전문, 바이트 보존. 수정 금지) |
| 08a | S-1–S-33 | 화풍 애드덤 인덱스(용도·IP·Kling 전이성 ✅/🅂/🟡 — 이미지 작업 픽용) |
| 09 | KV-01–24 | **화풍→KLING 영상 전이**(영상화 각색 지시문 — 영상 작업 픽용) |
| 10 | AU-01–18 | **음악·오디오 연출**(영화음악 이론 매핑·쇼츠 전략·후반 믹스 수치) |
| 11 | AN·SH·LC | **애니 연기·그림자·고급 조명**(12원칙 매핑·그림자 결함 예방·네거티브 필/켈빈/그레이딩) |
- **화풍 애드덤 사용법**: 이미지(나노바나나 1차 합성·카드) = 08a 인덱스로 픽 → 전문은 08에서 / 영상 = 09에서 픽(✅=직접 전이, 🅂=나노바나나로 그 화풍 이미지 생성 → 스타트 프레임 경유 — **이 경로면 정지 전용 화풍도 전부 영상화 가능**). 캐릭터 일관성 시트(S-15/KV-22)는 주체 등록·@참조 소스 제작에 사용.
- 조립 순서 권장: S/H/O(프레이밍 — 맨 앞) → 주체·행동(@앵커) → M(무브 1개) → LIGHT(1개) → STYLE → 오디오 → 테일 3종.
- 이미지용 컬럼(GPT Image 2·나노바나나) 병존 = 의도된 공용 설계(나노바나나 1차 합성 → Kling 파이프라인이라 한 라이브러리로 묶음).
- 갱신 룰: 실증으로 확인된 모듈은 검증 상태를 `실증✅(yymmdd)`로 승격. 라이브러리를 이 지침에 인라인하지 마라(기틀 — `PROJECT_MEMORY.md` §기틀 보호).

## 11) 메모리
`MEMORY.md` = UI 고정 사실 · 사용자 실측 · 실증 케이스 **한 줄 요약** · 미해결 과제 · 리서치 요약. **케이스 원문 정본 = `CASES.md`**(온디맨드 — 평소 진입 시 로드 금지, 피드백 적립·패턴 참조 때만). 충돌 시 이 지침 본문 우선. 플랫폼 공통 룰(수정 모드·버튼 UX·첨부 attach.py)은 라우터(`CLAUDE.md`) 정본.
