# 작업 현황 · 미결 감사 (260614 스냅샷)

> Opus 4.8 감사관 25인이 전체 대화(335턴·62 사용자 메시지)를 5구역 분할로 훑고 **라이브 레포(main)에 실제 반영됐는지 대조**한 결과. *날짜 박힌 스냅샷* — 이후 변경은 git log가 정본.

## ✅ 완료 (라이브 검증됨 — origin/main)
- **폰 큐 파이프라인**: 입구 핸들러 자가치유(`git fetch+reset`), 네이트 EUC-KR 자동 디코딩(`fetch_article.sh`), `phone-setup.sh` 원클릭 복구, 헤드리스 가드레일·무중단, 중복방지(article_id+버전), 실패 가시화 — #112~116
- **지침 SSOT**: `shared/inject_guidelines.sh` 단일 헬퍼로 요약·카드에 *강제 주입*, `guidelines_version`(sha256) 도장 → 지침 바뀌면 재생성. stdin 전달(ARG_MAX 회피). 자동 카드플랜(text_done), 적응형 폴링(~5분·90초·상한15분), 제미나이 이중잠금(Lock A/B) — #118·#120
- **뷰어 3단계 UI**: 요약↑·카드↓ 접이식, 프롬프트 3줄+더보기, 슛 버튼(text_done에서만) — #121·#123
- **뷰어 타이포**: 매체 이모지/편향 색도형 strip, 키워드 강조(약보수 보정·앵글 후보·○○관점)+약보수보정 줄바꿈, 날짜 헤더(검색 위), 복사 아이콘화, N/M/T nav, 말머리 📍·소머리 •, 대주제 h1 통합(상단=카드 동일·복사), h2 괄호 흰색(.paren), 글자수/상한 미강조 — #122·#124·#125·#127·#128
- **썸네일 타이틀 섹션 제거**: 뷰어 + `prompts/news-analysis.md`(맨 위 기사 제목=썸네일)
- **CLAUDE.md 기틀 문서화**: 뉴스 큐 파이프라인 불변 + 머리표·계층 표준

## 🔴 진짜 남은 것 (actionable)
1. **모달 알림/메시지 UI 시스템** — *turn 150에서 승인("응 적용해줘")됐으나 미구현.* 요구: 프로필 둘레 얇은 테두리 + 메시지 수 숫자 배지 / 클릭 시 프로필 자리 팝업 + 닫기 버튼 / 메뉴에 "메시지" 항목 / 메시지 git 누적 / **모달에 "지침 변경으로 이 카드 갱신됨" 이력 알림**. 백엔드는 이미 있음(`articles.json`의 `guidelines_version`) — 뷰어가 표면화만 안 함. → **결정: 지금 만들지.**
2. **NMT nav 위치 겹침** — 디자인은 OK, 위치만. *이번 커밋에서 수정*(거터 밖/우하단 코너로) → 폰에서 확인 부탁.

## 🟡 결정 대기 (네 판단 필요)
3. **apps/news/01 Step 3 썸네일 역참조 제거** — `prompts/news-analysis.md`엔 반영됨. 에디터 본체 지침(01)+썸네일 앱(/1·/2)까지 손댈지. ✅ **반영 완료**(이 PR): 01+00+04+PROJECT_MEMORY+파이프라인 = 맨 제목=썸네일 통일·Step 3 폐지. 썸네일 앱(/1·/2)=직접 입력이라 무영향.
4. **💕 웹앱 아이콘** — 현재 favicon=`main_profile.jpg`. 💕 이모지로 바꿀지 vs 브랜드 이미지 유지(CLAUDE.md 브랜드자산 정본과 충돌하므로 확인).

## ⏸ 네가 보류한 것 (장기 큐)
- 날짜 우측정렬 + 카드 셀 영역 분할(투명영역·하이퍼링크존) — "재수정하고 보자"
- 좌측 버튼존 대안(편향/유형 좌측 이동) — "보류 검토"
- 파일명 계층 정합 리네임(`에디터_뉴스` 등) — CLAUDE.md TODO

## ℹ️ 해소됨 (오해 방지)
- 진행률 % → 가짜 % 거부, 정직한 "프롬프팅 중" 텍스트로 대체
- New 배지 → 구현 후 제거(카드 컴팩트) · 오늘 날짜 강조 → 미적용(요청대로)
- IG/Thread 더보기 → 자유요약만 · 유형 A~H → 유지
- 마커 `[지침반영:01/02-OK]` → 임시 테스트용, 원복 완료
- 𝙉𝙀𝙒𝙎 유니코드 폰트 → 안 씀(모바일 두부 깨짐 위험)
