# PROJECT_MEMORY · 노뮤트 고정 사실·결정 (지침/에디터가 참조하는 메모리)

## 채널
no_mute(노뮤트) = 시사·사회 이슈를 IG/Threads 카드뉴스로 재구성. 2025.11 런칭. **A형(사건 비판)+B형(구조 비판) 주력**(A·B 기대값 최상 / C 중간 / D~H 기본).

## 톤·윤리
- **약 10% 보수 톤 가미**: 좌편향 단정·진보 일변도 회피, 좌·우 양쪽 통하는 구조 비판 우선(진보·노조·좌파도 비판 대상 가능). 적용 = Step 0.5·1·3·시사점 / 면제 = F형·#추모·#자살자해·#아동피해·#재난참사·순수 정보. ⚠️ **사실 무결성·원문 왜곡 금지·강도 증폭 금지가 절대 우선.**
- **편향 기사 가드(v1.15.2·v1.15.4 개정)**: 단일 소스가 과도하게 좌/우 편향(극좌·극우)이라 relay 시 오해 위험 크면 → State1(면책 문구를 **⚡ 출처 바로 위**에 1차 자동 + A형 출처) / 시사점 직후 **볼드·이탤릭 메타 알림 + 3선택지**("면책 조항 삭제"/"OK"/"OK. 카드 뉴스 진행"). 중립화(약보수 재구성+에디터 역)는 고급 옵션 보존. **면책 ↔ 에디터 역 배타적.**
- 윤리 카메라: 피해자 Eye-level / 가해자 High(Low 미화 금지) / 권력·구조 Low·Worm.
- 실존 인물 검열: 비민감(연예인·정치인)은 **과잉 비식별·얼굴삭제 X** — 특정 사실적 닮은꼴·실명·실제 브랜드/기관 로고만 회피. 표정·손·자세·실루엣 살림. 민감(#추모·#아동피해·#성범죄·#재난참사·#자살자해)은 [안전 시각화] 우선.

## 비주얼
- **화풍 2종 고정**: A 한국웹툰 수채화(휴먼·추모·감성) / B 한국웹툰 극화(범죄·법리·권력·구조 고발). 레거시 ART102·질감 50종 제외. 기사 단위 1스타일.
- **악센트**: 단색 + 1색, 기본 네온그린 **#0FFD02** 또는 사건 상징색. film-noir low-key.
- 카드 듀얼: **🍌 Nanobanana 2 Pro**(키워드·자동화) + **✳️ GPT Image 2**(산문·채팅 전용·수동 복붙).

## ★ 기틀 보호 / 열화 진단 (이 프로젝트의 존재 이유)
**왜 라이브러리를 지침에서 분리했나**: 지침에 이미지 모듈·룰을 인라인해 두고 갱신하다 보면 → 갱신 중 *조용한 누락* → §참조가 허공을 가리킴 → Opus 즉흥 메꿈 → '모서리 깎임'. 0.1%씩 깎인 게 복리로 쌓여 *모델은 상향됐는데 출력은 더 나빠지는* 멘붕 상황. 분리(라이브러리=완전한 superset / 지침=코드 참조)가 이 열화를 끊는 구조다.
⚠️ **미래 세션 절대 규칙**: ① 라이브러리를 지침에 **다시 인라인하지 마라.** ② 기틀(분리·3단계·참조 방식·INVARIANTS) 변경은 **모델이 맘대로 하지 말고, 반드시 사용자에게 확실히 묻고 승인 후에만** 한다. (모델마다 맘대로 진행하는 경우가 많음 → 꼭 확인.)

## 결정 로그
- **(260610) Step 2 본문 v1.15.9 통째 롤백 + 카드뉴스 넘김 훅만 보존** (뉴스 v1.15.14): 사용자 판정 — 같은 기사(동아 잠실 투표소 증거보전)로 복원룰(v1.15.9: Thread 316자·📍3 / IG 457자) vs 현행룰(v1.15.13: 442자·📍4 / 678자·📍6) **A/B 실측 비교** 후 버튼 결정("1.15.9랑 카드뉴스 넘김만 반영"). 제거 = v1.15.10 fact_guard 게이트(스크립트 `apps/news/fact_guard.py`는 휴면 보존·재도입은 사용자 결정) + v1.15.13 작성 목표 구간·비트 누락 0·📍 골격 고정(당일 폐기). 보존 = 카드뉴스 넘김 훅 v1.15.11~12 전체(4-A 5원칙 ⑤·[넘김 훅] 섹션·`↪훅:` 줄·RECOMMENDED 항목→25번, 25문). ⚠️ 교훈: **Step 2 요약 룰의 기준선 = v1.15.9 원문** — 재개정은 실기사 A/B 실측 + 사용자 판정 후에만(모델 단독 개정 금지). 운영에서 보인 '소넷 수준' 축약 편차는 룰 외 변수(모델·세션) 의심 — 라우터 §모델(Opus 권장) 확인 병행.
- **(260610) `shared/check_env.sh` 신설 — 환경 캐시 적중 객관 판정**: "진입이 빨라졌나"는 정성 판단 불가(사용자 지적) → 새 세션에서 `bash shared/check_env.sh` 한 줄이면 폰트·패키지·th 스탬프·ffmpeg·whisper 모델 캐시 7항목을 ✅/❌로 실측(읽기 전용·설치 0). ❌면 해당 앱 첫 진입 때 설치 발생 = Setup script 미반영/빌드 실패(`|| true`가 에러 삼킴)/7일 만료 신호. 환경 빌드 로그는 UI 전용이라 모델이 직접 못 봄 — 결과물 검사가 진실원본. (이 세션 실측: 사용자 환경에 셋업 3줄[comp·th·ly] 등록돼 있었으나 세션 컨테이너는 0/7 적중 — 등록≠반영 사례.)
- **(260610) Step 2 요약 퀄리티 복원 — 작성 목표 구간 복원 + 비트 누락 0 + 📍 골격 고정** (뉴스 v1.15.13): 사용자 보고 = "원문 요약 퀄리티가 많이 낮아졌다 — 오늘 수정 이전 기준으로 복원(카드뉴스 제외)" + 세션 중간 요약에서 문장별 📍 소실. 실측 = 현행 룰 데모 4건 198~214자(Thread 상한 450의 44~48%). 원인 = v1.15.7(0606) 하한 폐지 후 '상한 초과만 체크 + 미달 무벌점 + 보강 금지'가 선축소 습관으로 역작용('상한 안 충실'이 모델 재량 판정이라 견인력 상실) + 📍는 개수 규칙뿐 '비트=한 줄 한 📍' 골격 규칙이 역대 부재(깎인 게 아니라 원래 암묵). 조치(A+B 사용자 버튼 승인) ① pre-v1.15.7 작성 목표 구간 복원: IG 700(600–800)/Thread 425(400–450) — 디폴트 목표일 뿐 bash 검증은 상한만(억지 늘리기 금지 = v1.15.7 취지 보존) ② 선충실·후압축: 원문을 사실 비트로 분해→전부 쓰고→넘치면 깎기(선축소 금지) ③ 충실도 판정 = 비트 누락 0(압축·병합 OK·통누락 X·목표 미달 = 누락 의심→복원·비트 적은 기사만 짧음 정답) ④ Thread 📍 3 디폴트→3~5 가변(압박 시 병합 우선·통삭제 최후) ⑤ [📍 골격 고정] 신설(IG·Thread 공통, 비트=한 줄 한 📍·산문 금지·세션 중 타 작업 후에도 유지) ⑥ fact_guard 수치 위축 방지 1줄(수치 회피 신호 아님) + RECOMMENDED 27. 기각 = C안(2~3포스트 체인, 출력 골격 변경 보류). **카드뉴스(넘김 훅 v1.15.11~12)·fact_guard 게이트·상한·면책·파이프라인 무변경.**
- **(260610) /th 진입 지연 개선 — 이력 분리 + setup 2단화** (th v22.23): 지연 원인 실측 = 신규 컨테이너마다 setup.sh가 apt 폰트+pip mediapipe/opencv 설치(분 단위·이 세션 실측 전부 미설치) + 지침 38KB 중 17%가 변경 이력. 조치 ① 이력 8블록 → `apps/thumbnail/CHANGELOG.md` 분리(전체 이력 정본·본문엔 최신 블록만, 38.1→33.1KB) ② setup.sh 2단화 — 무거운 1단을 스탬프(`~/.cache/nomute_th_env_ready`) 단락 + **환경 Setup script 등록 권장**(ly Whisper prefetch와 동일 — 스냅샷 캐시로 /th 진입 즉시), 미등록·미설치면 기존대로 자동 설치(폴백 보존). 기능 명세 0 변경(F01~F18·코드 4파일 불변·분기 ①~⑥·C-등급 보존 확인). 검토 대안 기각: ⓐ분기별 지침 분리(공통 코어[절대규칙·measure·가드]가 본문 대부분 — 감량 미미·누락 위험↑·설치 지연은 그대로) ⓑ구 대화-프로젝트 회귀(진입은 빠르나 단일 원본 원칙 충돌·동기화 드리프트 재발 — 이주 사유 역행). ⚠️ 사용자 버튼 승인('안 C 전체').
- **(260610) 넘김 훅 '매 장 의무' → '배치 설계' 절충 개정** (뉴스 v1.15.12): 낚시 냄새 차단 절충안(사용자 버튼 승인) — 의무는 **카드 1 끝 하나만**(최대 이탈 지점), 나머지는 4-A가 이탈 위험 지점 1~2개 선택 = **기사당 총 2~3개**(장수 늘어도 고정). **질문형 기사당 최대 1회**·단서(숫자·사물)/미완 예고 우선 · **절단 위치 우선**(카드 경계를 긴장 지점에서 자르는 자연 훅 > 훅 문장 덧붙이기) · **감정 피크 무훅 허용**(여운이 당김) · **낚시 자가 테스트**(훅 줄만 이어 읽어 광고 같으면 감축). 즉시 회수·마지막 카드 닫기·완결 문장 유지. 브리프 `↪훅:`은 배치 카드에만. RECOMMENDED 26 문구 갱신(26문 유지). 근거: 매 장 의무는 긴 기사(6~7장)에서 훅 5~6개 = 기계적·예능 자막화 위험 — "당길 자리에서만 당긴다".
- **(260610) 카드 [넘김 훅] 신설** (뉴스 v1.15.11): 마지막 카드 제외 매 카드 **끝 줄 = 다음 장을 봐야 풀리는 한 톨**(질문·단서·미완) + **다음 카드 첫 줄 즉시 회수**(답 미루기·낚시 금지). 절제 — 카드당 1개·질문형 연속 2장 금지·마지막 카드는 훅 없이 메시지 닫기·완결 문장 유지·감정 피크는 긴장 유지형. 4-A 브리프 `↪훅:` 설계(5원칙 ⑤) + 4-B 실행 + RECOMMENDED 26. 목적 = 캐러셀 완주율(마지막 카드 사회 메시지 도달률). ⚠️ 실기사(MBC 빌라왕 1심 징역 10년) A/B 전후 비교 데모로 사용자 버튼 승인. 출력 포맷 골격·파이프라인 3단계·분량·면책 무변경. + check_refs 주석 예시 버전 무관 표기.
- **(260609) [수치 대조 — fact_guard] 소프트 게이트 신설** (뉴스 v1.15.10): 원문(또는 0.5 보도자료+보강 검색 메모)에 없는 수치·날짜가 IG·Thread·카드 텍스트에 있으면 ⚠️ 플래그 — `apps/news/fact_guard.py`를 Step 2 글자수·Step 4 카드 가중폭 bash에 합산. 반올림 변환(조·억·만 ±5%) 자동 인정, `⚡`/`ⓔ` 출처·면책 줄 제외, **차단 아님**(플래그 시 본문 대조: 근거 없으면 수정·있으면 유지). RECOMMENDED 25 추가. 실측: 정상 요약 통과 / 오염 요약(17년·1.2억·14억·5명·6월9일) 5건 전부 탐지. 출력 포맷·품질 룰·파이프라인 3단계 무변경. ⚠️ 사용자 버튼 승인('틀린 숫자 경보').
- **(260609) /th 릴스 헤더형 '흰 배경 제거' 옵션 신설** (th v22.22): "흰 배경(흰칸·흰 박스) 제거" 요청 시 흰칸(y590~1431)을 안 그리고 베이스 그라데이션 그대로 + 헤더 텍스트 2줄만(`_reels2_nobg.png`). **기본값은 흰칸 포함**(명시 요청 시에만·사후 요청도 재렌더 제공). 코드 4파일 불변(절대규칙1) — `nomute_reels2.py` import 변형 호출로 구현, **실측: 텍스트 픽셀차 0·흰칸 영역=베이스 원본 동일**. 진입 안내표(SKILL)·분기⑥·보고⑥·고정 수치 병기. + 마이너 정리: 원천 백업 섹션 현행화(OneDrive 잔재→역사 표기), `attach.py` 동시 세션 한계 주석, 라우터 썸네일 스크립트 표기 명시화(4파일). ⚠️ 사용자 명시 요청.
- **(260609) 드리프트 동기화 + 00 포인터화 + 참조 점검 자동화** (기능·품질 룰 무변경·사용자 승인): ① 옛 사본 수치·포인터를 정본 기준으로 동기화 — 분량(00 §6·메모리 검증 룰)을 v1.15.7 현행(상한 IG 800/Thread 450·하한 폐지)으로, 분리 이전 "CLAUDE.md §3/§4/§7/§8" 포인터(04·03·메모리)를 `apps/news/00` 기준으로, ly 지침 헤더 v2.4→v2.5 정정. ② `apps/news/00`의 플랫폼 공통 사본(기틀 보호·수정 모드·저장소·모델)을 제거하고 **"정본=라우터 CLAUDE.md" 포인터로 대체**(사본 드리프트 원천 차단) — 뉴스 전용 룰(§0·§2~§8)은 전부 보존·무변경. ③ 뉴스 지침 파일명 `v1.15.2`→`v1.15.9` 정정(내용 무변경·참조 2곳 갱신) — **파일명 정책 = rename 방식 유지**(버전업마다 파일명 변경·사용자 결정). ④ `shared/check_refs.py` 신설 + 수정 모드 ③에 실행 추가 — 참조 경로 실존·파일명↔내부 버전 일치를 커밋 전 기계 점검(v1.15.2류 드리프트 재발 방지). ⑤ README 플랫폼 현행화(5앱).
- **(260606) [⚡ 출처 분기] 'ⓔ 노뮤트'(자체 기획 주력) 신설** (v1.15.9): 노뮤트 자체 제보·직접취재·기획이 본문 70%+ 주력 + 나머지 외부 검색 보완 시 출처를 `ⓔ 노뮤트 / yyyy.mm.dd.`(IG·Thread 동일). ⚠️ 이 케이스만 출처 줄 마커가 `⚡`→`ⓔ`(출력 포맷 골격 일부 변경 → **사용자 명시 승인** 후 적용, 버튼 'ⓔ 노뮤트 (⚡ 제거)' 선택). 다른 4종은 ⚡ 유지, 4종→5종. 외부 자료가 본문 주력(50%↑)이면 C/B. 사실 무결성·강도 증폭 금지 절대우선(제보 수치는 '제보된 내용에 따르면' 명시). 이미지 듀얼·카드·파이프라인 3단계·면책 무변경.
- **(260606) [개념어 정밀도] 신설 + [소스 보강 게이트] 트리거 확장** (v1.15.8): ① **개념어 정밀도** — 사회·법·제도·경제 등 '개념'은 독자 공유 표준어가 기본값(*개념=공유 표준어 / 장면·앵글=새 표현*), 운율·압축 위해 개념을 즉흥 비유·사적 신조어로 갈아끼우지 마라. 판정=특정 단어 금지목록 대신 '낯선 독자 테스트'(맥락 모르는 사람이 그 단어만으로 뜻 잡나)+'앵커 규칙'(짧은 포맷=표준어 직접). 자가검증 6번·§8 자물쇠 Step2·3 반영. ② **소스 보강 게이트 두 트리거** — ⓐ약한 단일 매체(축을 '지역'→'정보력·신뢰도'로 재정의: 지역지 + 전문지·업계지·군소 전국·신생 인터넷 등 지명 없는 무명지 포함, 예 디지털데일리) ⓑ정보 충분성(신설·매체 크기 무관: 앵글이 기댄 핵심 개념을 원문이 한 줄로만 던져 뭉개질 때 Step 1 전 개념 표준어·사실관계만 가볍게 보강). 절대우선 룰(사실 무결성·날조·강도증폭 금지) 그대로. 이미지 듀얼·카드·출력 포맷·파이프라인 3단계 무변경(주변부 튜닝). ⚠️ 사용자 명시 결정(버튼 '둘 다 반영' + 트리거 ① '정보력 축' 선택).
- **(260606) 반영 보장 = 불변·최우선 원칙 확정**: 사용자 변경 요청 = 무조건 main 반영 + `git show origin/main` **실측 검증**까지가 '완료'. ① 검증으로 확인 안 된 완료 선언 금지(화면·로컬로 '됐다' X) ② 머지 막히면(force 차단·non-ff·충돌) `-X ours` 흡수 등으로 **끝까지 반영** ③ 그래도 안 되면 STOP+보고. CLAUDE.md §수정모드 ⑤ 강화 + 불변 원칙 명문화. ⚠️ 사용자 명시 결정 — force 허용보다 '반영 보장'이 본질(force는 방법, 검증이 보장).
- **(260606) IG·Thread 분량 = 상한만 유지·하한 폐지** (v1.15.7): 미달 시 억지 보강으로 퀄 저하되던 문제 해소. ① 하한(IG 600/Thread 400) 강제 폐지 — 미달 보강 압박 제거, 대신 '상한 안 충실도'(독자 관점→📍→시사점 랜딩 전반 요약점 누락0·군더더기0, 400자로 담길 걸 300자로 깎기 금지)가 기준. ② 상한 IG 800 / Thread 450(기존 500→450) 유지. ③ 카운트에 ⚡ 출처 포함·면책 줄 제외(기존 IG ⚡ 제외에서 변경), bash는 상한 초과만 체크. 이미지·카드·출력 포맷 골격 무변경. ⚠️ 사용자 명시 결정.
- **(260606) [편향 기사 가드] '면책 삭제' = ⚡ 출처 보존 명확화** (v1.15.6): '면책 조항 삭제'는 **면책 한 줄만** 제거하고 바로 아래 `⚡` 출처 줄·요약 원문은 **전부 보존**(IG·Thread 동일). 면책과 인접한 출처를 함께 지우지 않도록 01 지침 5곳(변경점·State 1·면책 삭제 분기·트리거 표·RECOMMENDED 24)에 명시. ⚠️ 실제 실행 누락(면책 삭제 시 Thread ⚡ 출처 동반 삭제) 1회 발생 → 재발 방지. 사용자 명시 결정.
- **(260606) 선택 분기 = 버튼+알림 UX 확정** (v1.15.5 / 라우터 §🔔): 에디터가 사용자 선택을 요구하는 분기(편향 가드 3선택지·🚦STOP 후 분기·베스트 픽·앱 되묻기)를 텍스트 나열 대신 `AskUserQuestion` 버튼+알림으로 띄운다(폰 탭 응답·알림). 단축 트리거 타이핑('4'·'ㄱ') 흐름은 병행 유지(버튼=대체 아닌 추가). 텍스트 파이프라인·이미지 듀얼·출력 포맷·면책 무변경. ⚠️ 사용자 명시 결정(버튼으로 "지침에 영구 반영" 선택).
- **(260605) 수정 모드 = main 즉시 머지 워크플로 확정**: CLAUDE.md 🗂 절차에 ⓪편집 전 `git fetch origin main` 최신화 + ④PR 생성 즉시 main 머지(draft 방치 금지) + ⑤origin/main 검증 추가. 이유 = 클라우드는 세션마다 main을 새로 복제 → 브랜치/draft에 방치하면 새 세션이 옛 룰을 봄(실제로 한 번 데임). 동시 세션 stale-머지 방지(fetch·rebase, 충돌이면 STOP, `force` 금지). ⚠️ 사용자 명시 결정.
- **(260605) [편향 기사 가드] 개정** (v1.15.4): 단일 소스 과편향(극좌·극우) 시 면책 문구를 **⚡ 출처 바로 위**에 삽입 + 시사점 직후 볼드·이탤릭 메타 알림 + **3선택지**(면책 조항 삭제 / OK / OK. 카드 뉴스 진행). 기존 중립화(약보수 재구성)는 고급 옵션으로 보존. ⚠️ 사용자 명시 결정.
- **(260605) [소스 보강 게이트] + [⚡ B-간소형] 신설** (v1.15.3): 사용자 직접취재 아닌 **지역지/군소 단일지** 기사면 Step 1 *전*에 조선·중앙·동아 등 **주요 보수 전국지** 교차 확인·보강(진보 편향 보정 우선 + 지역지 정보력·전달력·문체 보완). 사실 무결성 절대우선·날조 금지. / 출처 다매체 多 시 대표 3~4곳·가운뎃점·'등'·기자명 생략(B-간소). ⚠️ 사용자 명시 결정(원안 강조 + 지침·CLAUDE.md 둘 다).
- **(260604) 코드화 확정** ★: 단일 품질은 코드(라이브러리 첨부)≈대화(Opus 4.8이 알아서)지만, *검수 축소→딸깍·무인·확장*의 전제가 "100번 돌려도 안 흔들리는 신뢰성"이라 품질 동일하면 코드가 맞다. → 이 폴더(nomute_editor)가 그 실체화.
- **(260604) 모델**: Opus 4.8 최대사고 디폴트(4.6보다 지시이행·일관성 우수, 창의 폭보다 구조·윤리·일관성이 중요). 우연성(그 순간 편차)은 알려진 한계로 감안 — 추후 과제.
- **(260604) 실증**: gwangju_edu 기사 지침 풀런 → 라이브러리 풀호출 MD → 자동화 6장 생성 성공(.gen_complete).
- 표현 품질은 정성·비정량 → 단일 검수 무의미, **분산(variance)으로 본다.** 취향은 *고른 결과물 few-shot*으로 누적 가능(추후 하드닝 항목).
- (260604) 지침 하드닝 '자물쇠' = **`apps/news/00` §8 "단계별 자물쇠"로 구현 완료**(뉴스 분리 전엔 CLAUDE.md §8) — 각 단계 끝 재점검 게이트(특히 4-B 카드마다 라이브러리 §1-6 실제 호출 강제). 참조 누락 차단.
- 미진행(추후): few-shot 취향 예시(코퍼스 ~10-15픽 쌓인 뒤) + N케이스 분산 검증.
- 미진행(추후): **생성 페이싱 단축** — 현재 Gemini 합성을 의도적으로 띄엄띄엄 돌려 ~15분 걸림(그래서 쇼잉 전 15분 고정 대기). Apps Script 폴링·생성 간격을 줄이면 대기 단축 가능 = **기존 시스템(Apps Script) 손대는 거라 사용자 확인 후.** 지금은 15분 고정.
- 프리뷰 쇼잉(확정 260604): 발사 후 15분 → `files.list` 한 콜로 전 카드 썸네일(thumbnailLink, s2048) → PowerShell 다운 → Read로 **전 카드 합성본 + 첫 질문 한꺼번에** 채팅 출력. Zapier 1콜(2task)·카드수 무관·GCP 0. (상세 03_자동화.)

## 검증 룰 (수치 · bash 측정)
- 카드 텍스트: 줄 ≤4(3 권장), **weight≤19.5 AND hangul≤18**. 썸네일 줄 가중≤12. IG·Thread = **상한만**(IG 800 / Thread 450 — `⚡` 출처 포함·면책 제외)·하한 폐지, 기준은 상한 안 충실도(v1.15.7).
- 출처 ⚡: A 단일매체·단일기자 `⚡ {매체} / {기자} 기자 / yyyy.mm.dd.`(에디터 역 금지) / 중립화 예외 `⚡ {매체}({기자}) 기사 참조 / 에디터 역 / yyyy.mm.dd.` / B·C 다수·자체 → 에디터 역. **다매체 多 시 대표 3~4곳·가운뎃점·'등'·기자명 생략(B-간소, v1.15.3).** / **F 자체 기획 주력**(노뮤트 자체 제보·직접취재·기획 70%+ 주력 + 나머지 외부 검색 보완) → `ⓔ 노뮤트 / yyyy.mm.dd.`(⚠️ 이 케이스만 `⚡` 대신 `ⓔ`로 시작·IG·Thread 동일, v1.15.9).

## 원천 백업
- **현재 원천 = 이 레포의 git 히스토리 + `_versions/` 스냅샷** (단일 원본 원칙 — 모든 복구·롤백은 여기서).
- (역사) 레포 이전 최초 복구본은 OneDrive 폴더(`…\Sewoong Hwang\`의 260603 지침 v1.15.2·라이브러리 v1.1 복구본, 260604 인계서)였고 본 레포 01·02가 거기서 출발 — **폐기 원칙(260604)에 따라 더는 참조·동기화하지 않는다**(현재 정본은 레포).
