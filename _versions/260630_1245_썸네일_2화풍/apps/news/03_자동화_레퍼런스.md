# 03 · 노뮤트 자동화 레퍼런스 (매 세션 재학습 방지용)

> ⚠️ **직영 전환됨 (260619) — 아래 Apps Script/Cloud Run 파이프라인은 이제 레거시 폴백이다.** 카드 '슛'(렌더)의 1차 경로 = **레포 안 직영 `.github/scripts/gen_cards.py`**: GitHub Actions가 Gemini로 텍스트-free 장면 직접생성 + `apps/comp/card_news.py` 로컬 합성 + R2 저장(없으면 git 폴백). 게이트 `GEMINI_API_KEY`. **이 문서의 Drive/Apps Script/Cloud Run 서술은 `GEMINI_API_KEY` 없고 `GDRIVE_SA_JSON`만 있을 때의 폴백**(전환기 안전망). 직영 STYLE_PROMPT 등가물 = gen_cards.py `CARD_STYLE`. 상세 전면갱신은 후속 TODO.

> Drive 저장(4-C) 단계에서만 열어보면 된다(레거시). 평소엔 00_운영 §7 한 줄로 충분.

## 파이프라인 (레거시 폴백 — Apps Script v5 `code.gs` 소스로 검증)
```
지침(에디터) → 카드 MD(🍌만) → Drive Prompt/{yymmdd_hhmmss_주제}/동명.md
 → Apps Script mainTrigger(1분 폴링) → parsePrompts 추출 → STYLE_PROMPT 전역 prepend
 → Gemini Flash(gemini-3.1-flash-image-preview, 4:5 / 1K) → {시각}_{주제}_장면NN.png
 → (시안 모드 아니면) Cloud Run nomute-compose /compose 텍스트 합성 → _final_*.jpg
```

## parsePrompts 포맷 (★ 엄수 — 이게 진짜 운영 포맷)
카드 헤더 `### [카드 N]` … `**이미지 프롬프트**` + ` ```text ` 블록. (`**텍스트**` 블록 = 카드 문구, 합성용.)
- 운영 정규식: `/###\s*\[카드\s*(\d+)\][\s\S]*?\*\*이미지\s*프롬프트\*\*\s*\n+([\s\S]*?)(?=\n###\s*\[카드|\n##\s|$)/g`
- ` ```text … ``` ` 펜스는 자동 제거. **MD엔 🍌 Nano만(✳️ GPT 제외).**
- ⚠️ 로컬의 `parser_v2.py`/`[장면]`/`Scene core` 변형은 부차 파이프라인 — 운영 아님. 헷갈리지 말 것.

## 합성 = 항상 (에디터 로직엔 토글 없음)
**Drive에 저장하면 무조건 합성까지 된다.** 에디터는 합성 여부를 결정하지 않고, MD에 합성을 끄는 플래그를 넣지 않으며, 카드 텍스트를 항상 채운다.
(※ 시스템 사실 — 디버그용 참고만: Apps Script는 `- 시안 모드: true`이거나 모든 `**텍스트**`가 비면 합성을 건너뛴다. **단 에디터 로직은 이걸 절대 사용하지 않는다.** 순수 이미지만 뽑을 일은 운영자가 콘솔/수동으로 직접 처리.)
- 완료 마커 `.gen_complete`(fail=0 = 전 카드 성공). MD 24h+ 미수정이면 스킵.

## 과금 (Google Cloud — Drive 저장 = 유료 발사)
- **Gemini API 💰**(이미지 생성) + **Cloud Run 💰**(합성; 시안 모드면 미발생).
- ⚠️ 그래서 지침이 4-B 후 🚦STOP을 강제 → GO('ㄱ') 받고만 4-C 저장. 텍스트 미확정 저장 금지.
- `GEMINI_API_KEY` · `GDRIVE_SA_JSON`(Drive 접근 서비스계정 키) = **운영자 보유, 여기 미보관(민감).**

## Zapier 발사 (Claude Code에서 직접 — 챗앱보다 강한 부분)
1. `google_drive_create_folder` (parent = Prompt 폴더 ID) → 폴더 ID 획득
2. `google_drive_create_file_from_text` (folder=그 ID, title=`{yymmdd_hhmmss}_{주제}.md`, convert=false, file=MD)
→ Apps Script가 1분 내 잡아 이미지 생성. 생성 후 `retrieve_files`로 장면NN.png + .gen_complete 확인.
- KST 타임스탬프 (클라우드=리눅스 bash): `TZ='Asia/Seoul' date '+%y%m%d_%H%M%S'`

## GPT Image 2 (✳️) — 수동, API 미연결
- ✳️ GPT 프롬프트는 **자동화에 안 들어간다.** API 키 미연결 상태. 운영자가 ChatGPT의 **GPT Image 2에 수동 복붙**해 생성.
- 그래서 ✳️는 **채팅 출력 전용**, Drive MD에 미저장(자동화 무손상). 🍌(Nanobanana 2 Pro)만 Drive→자동화.

## 프리뷰 — 이미지를 이 채팅에 직접 띄우기 (실측 확정 260604 · 한 방에)
운영자가 드라이브 안 가고 이 창에서 `_final_`을 보는 법. **base64 풀다운 금지**(토큰 폭증). **썸네일 URL을 한 콜로 다 받아 bash(curl)로 다운:**
> ⏱ **대기 = 적응형 폴링 (15분 고정 폐지 · 260613)**: 발사 후 **~5–6분**에 첫 `files.list` → `.gen_complete`·`_final_` 떴는지 확인, 안 떴으면 **1–2분 간격 재확인**, **상한 15분**(생성 지연 fallback). 실측: 6장 업로드→합성 ~6분(픽업 1분+생성·합성 5분).
1. **전 카드 주소 한 방에** — `google_drive_make_api_get_request`(Zapier, **1콜뿐**): url=`https://www.googleapis.com/drive/v3/files`, querystring `q`=`'{폴더ID}' in parents and trashed=false`, `fields`=`files(name,thumbnailLink)` → 그 폴더 전 파일의 thumbnailLink를 한 번에. *(⚠️ a24c2baa `get_file_metadata`는 thumbnailLink 안 줌 → 이 cheap 경로는 Zapier만. 실측.)*
2. 응답에서 `_final_*.jpg`만 골라, 각 thumbnailLink 끝 `=s220` → **`=s2048`** (거의 원본; s1024=105KB·s2048≈원본 317KB).
3. bash `curl -sL "{url}" -o {temp.jpg}` 로 각각 **디스크 직행**(컨텍스트·Zapier·GCP 0). 클라우드=리눅스 샌드박스라 curl·date·python3 다 됨.
4. **`SendUserFile`로 운영자에게 전부 전송** (전 카드 한꺼번에). ⚠️ **누락 금지 — `Read`는 모델 검수 전용이라 운영자 화면엔 안 뜬다(이 하네스 실측 260613).** 쇼잉은 반드시 `SendUserFile`로 **실제 전송**(Read만 하고 끝내면 운영자에겐 안 보임 → '이미지 안 떠' 사고 1회). 픽셀 검수는 Read, 보여주기는 SendUserFile.
**비용/속도**: GCP **0** / **Zapier = 1콜(2task), 카드 수 무관**(한 방에라서) / 1장 다운 ~1-2s. 픽셀완벽 최종검수만 base64. 완전 Zapier·GCP 0 원하면 로컬 동기화(Drive for Desktop).

## 주요 ID / URL
- Prompt 폴더 `1jQBoDqnDk5-fw51tCdDLD_cuDBAJp3kf` / nomute_imagen 루트 `1eu4EDm895SeFf7IxQOBwJgyO6YYmo6XK`
- Cloud Run 합성 `https://nomute-compose-57080751290.asia-northeast3.run.app` (`/compose`, asia-northeast3)
- **STYLE_PROMPT(v3.1)** = Apps Script ScriptProperties. 각 🍌 프롬프트 앞 **전역 prepend**(단일프레임·4:5 full-bleed·하단40% 안전영역·텍스트 전면금지·한국 외형 디폴트·미성년 안전망 담당). → 카드 프롬프트는 그만큼 린해도 되지만, 지침은 6중 안전망도 MD에 넣음(belt & suspenders).

## 🖼 AI 썸네일 후보 (3화풍 · 픽한 기사 자동 · 카드와 분리 · 260619·260620·260621)
뷰어 기사 제목 아래 「썸네일 후보」 = 검색이미지(기사 og:image+유사) + AI생성 3(각 4:5). **정본 = `.github/scripts/thumb_gen.py`**(검색이미지 추출 + AI 프롬프트 SSOT).
- **카드 vs 썸네일 차이**: 카드(직영 gen_cards)는 Gemini가 **텍스트-free 장면**만 생성하고 글자는 `card_news.py` 로컬 합성(정밀 폰트). 썸네일은 **레포 내 GitHub Actions가 Gemini(`gemini-3.1-flash-image-preview`·4:5·**1K**) 직접 호출** → **기사 타이틀/헤드라인 오버레이 문구는 안 박음**(260619 정정 · 전면 텍스트 금지 *아님* — 촬영 현장에 자연스러운 간판·표지판 글자는 장면 일부로 허용). 화풍 라벨은 이미지 밖 뷰어 캡션으로 따로 노출. **해상도**: 공용 `gemini_image(prompt, image_size)` — 썸네일·카드 모두 `"1K"`(토큰 절감 260621 운영자 · 폰 피드용 충분). (둘 다 레포 내 직영 · R2 저장 공용 · 레거시 Apps Script 경로는 카드 폴백시만.)
- **트리거**: `news-analyze.yml`의 `thumb_gen` 잡(`needs: analyze` · concurrency `thumb-make`). 픽한 기사(queue/*.md)당 3화풍(포토 에디토리얼·웹툰 극화·시사만평 · sid=photo/webtoon/cartoon · 순서=포토→극화→만평 · `photo` sid 유지=재과금0·수채화·포토클로즈업(photo_close) 폐지·260621) → `gen.json`([{sid,img/file,label}]) → `build-viewer.mjs`가 `a.cards.thumb_gen[]`로 투영(뷰어 `thumbStatic` slice 0,4).
- **장면(무엇을 그릴지) = 분석이 정한 충돌장면 우선 (260621 · 기사 분석→썸네일 역배선)**: `thumb_gen`이 AI 3화풍에 넘기는 장면 = `thumb_scene or image_query or 한줄요약`(`thumb_gen.py` build_prompt). **`thumb_scene`(queue md frontmatter) = analyze(Claude)가 기사 분석(Fact·추론·다각도·💡시사점)을 끝낸 뒤 정한 '독자가 가장 분노·충격할 충돌의 순간' 장면 묘사**(명사 아님·기사 근거 내·텍스트-free) — 검색용 `image_query`(상징 명사)와 **분리**. 옛 방식(검색 명사만 그림)이 '주제 일러스트(예: 밥 먹는 그림)'로 도피하던 걸 차단(운영자 260621). 미기입(`<…>`)·예시문 베낌이면 무시→`image_query`→한줄요약 폴백. 정본 = `prompts/news-analysis.md` thumb_scene 필드 + `thumb_gen.py`. ⚠️ thumb_scene은 GVER 미포함 → **기존 큐 기사는 옛 폴백 썸네일 유지**, 소급하려면 수동(md에 thumb_scene 기입 + thumb-redo 재발사).
- **다시 만들기(재생성 · 260620)**: 뷰어 🍌 AI생성 헤더 새로고침 버튼(`thumbRedoBtn`: 새로고침픽토→'Go?'→게이지→전송) → `functions/api/thumbredo.js`(GH_TOKEN·**게이트 없음**, 암호게이트는 추후 앱전체 일괄) → `thumb-redo.yml` 디스패치(`THUMB_ONLY=stem`) → `thumb_gen`이 그 기사 `gen.json`+`search.json` **둘 다 비우고 3화풍 + 검색이미지 전부 재생성**(=전체 새로고침 · 검색은 md `alt_urls` 있으면 유사까지·없으면 대표 og 재fetch) → 커밋·재빌드 후 라이브폴 교체. R2 URL `?v=`캐시버스트(같은 키 덮어써도 새 이미지 반영). ⚠️ 유료(Gemini 3장).
- **뷰어 썸네일 UX(260620 · §🎨 계승)**: 각 후보 우상단 복사·저장(`.sbtn`·`copyImg`/`downloadImg`) · 탭=라이트박스(`openLightbox` `<dialog>`·좌우 스와이프·확대뷰 복사/저장 유지·폰 뒤로가기=닫기 `pushState`/`popstate`).
- **저장 = Cloudflare R2**(설정 시): `aws s3 cp`(S3 호환·러너 기본설치)로 R2 버킷 업로드 → 공개 URL(`R2_PUBLIC_BASE/thumbs/<stem>/gen-<sid>.png`)을 gen.json `img`에 기록(레포 미저장·egress 0). **R2 5 시크릿 없으면 git 폴백**(`cards/<stem>/thumbs/gen-<sid>.png` 로컬 커밋·gen.json `file`). 둘 다 build-viewer가 처리.
- ⚠️ **비용잠금(Lock A) 예외**: 카드 자동경로는 제미나이 0(GDRIVE 부재)이지만, **썸네일은 의도된 자동과금**(픽=3장·3화풍). 게이트 = `GEMINI_API_KEY` GitHub Secret(없으면 no-op 스캐폴드). `THUMB_MAX_BATCH`(기본 3=기사 수)로 1런 상한(=최대 3기사×3장). 프롬프트 = `GOVERNING`(사건 결정적 순간·시선 머묾·스톡/포즈모델 도피 금지·맨 앞 위계)+`COMPOSITION`(**full-bleed=4:5 가장자리까지 장면으로 꽉 채움·빈/검정/단색 띠·여백·레터박스 금지** · 핵심요소 상단 2/3 단일초점 · 안전 최우선[선정·시신·실존인물 닮기 금지] — 구 '하단 1/3 비움=자막 자리'는 썸네일 자막 다운스트림 부재로 폐기[검정 띠만 유발·260621 분신술 10인]·gen_cards 카드 하단여백은 별개 유지)+`NO_TITLE`(타이틀 전면금지·현장 글자도 **최소·작게·흐릿**) + 화풍 photo = 잡지화보 톤 폐기·보도/르포 사실감(anti-stock) 전용 · 1K · 구도어휘는 apps/k 라이브러리(카메라·거리·앵글·조명) **인라인 증류**(빌드주입X=재과금폭탄 회피) — Apps Script `STYLE_PROMPT`는 **안 씀**.
  - **📊 제미나이 토큰 사용량 기록(260620)**: `gemini_image`가 응답 `usageMetadata`를 `_USAGE`에 누적 → `process_one`이 기사별로 `cards/<stem>/thumbs/usage.json`(`calls·prompt/output/total_tokens·cumulative_total_tokens`) 기록 + Actions 로그 `📊 제미나이 토큰…` 출력. **분석(다이제스트)=Claude 구독이라 제미나이 0**·검색이미지=HTTP fetch(제미나이 0)·비전훅 OFF(0) → 현재 제미나이 과금은 **이미지 생성뿐**. 토큰=정확/$=AI Studio 단가표 기준 추정. 집계 전체는 Google AI Studio 콘솔.
- **검색이미지** = 기사 본인 **대표사진(og:image)+유사** 추출(`search.json`=`[{url,link,label}]`, label=''(대표)/'유사'). ⚠️ **Google CSE JSON API 폐기(死) — 2025 신규고객 차단**(신규 프로젝트 "사용 설정"해도 403 `this project does not have access`·우회 불가·구글 직원 확인 · CSE 시크릿 미사용). 대체 = `fetch_article_images(art_url)`: 기사 URL fetch → og:image(발행사 선언=신뢰·크기/크롭 면제) + 본문 유사사진(같은 매체 도메인·같은 사진 디렉터리·로고/광고/유튜브/작은썸네일 컷=엄격필터) 최대 3장 → R2 재호스팅(핫링크·리퍼러 차단 0, 매직바이트 검증). **신뢰 분리**(대표=관대 / 본문=엄격)가 핵심 — 분신술 10인 감사 반영(Daum `/thumb/` og·OG카드 1200×630·네이버 pstatic 본문 전부 보존). 차단매체(403·데이터센터 IP WAF)·paste(url無)는 `search.json=[]` 1회 기록(좀비 재fetch 차단) → **뷰어는 검색섹션을 안 띄움**(빈 안내·Wikimedia 임시·구글검색 링크 폴백 제거 · 260620 운영자 "요약 위 잡음 제거" — thumbSec는 검색·AI 이미지가 실제로 있을 때만 표시) → AI 썸네일(thumb_gen)이 커버. 보안 = SSRF 게이트(http(s)·사설IP/메타데이터 차단). **✅ 교차매체 유사 보강 구현(260620)**: 단일사진 기사 캐러셀 비던 것 → 픽이 심은 `cluster_members`(같은 사건 타매체)를 `pick.js #alt:` → **`analyze.sh`가 queue md `alt_urls:` frontmatter로 보존** → `thumb_gen.fetch_article_images(alt_urls=)`가 그 og:image를 '유사'로 fetch(대표와 중복/소형/잡것 컷·최대 want). 마커매체(SBS·이투데이) 관련기사는 폴백. ⚠️ **paste(url無)는 여전히 0장**(불가피). 🌱 잔여: PIL 품질게이트·R2 lifecycle.
