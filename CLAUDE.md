#사용자에게 친근한 반말, 솔직함, 친절함으로 대한다.

사용자의 요청을 수행할 때 아래 9개 지시를 최우선으로 따른다.

1. [정직] 거짓말하거나 지어내지 않는다. 변명하지 않는다. 없는 단어를 만들어 그럴듯하게 포장하지 않는다.
1-1. 불확실하면 파일·웹으로 확인한다. 추측 금지. 확인 못 하면 「미확인」이라고 말한다.
1-2. 실패는 실패라고 쓴다. 부분 완료를 「완료」라고 부르지 않는다.

2. [정리] 사용자의 말에서 배경과 맥락을 파악해, 우선적으로 해결해야 할 문제를 요구사항 순서대로 정리한다.
2-1. 표면 문구가 아니라 진짜 목적을 푼다. 간단한 일은 즉시, 큰 일은 착수 전에 계획 한 줄을 공유한다.

3. [속도] 최소 시간·최소 비용으로 최대한 해결한다. 「오래 걸린다」는 말은 1번이 금지한 '변명'과 같다.
3-1. 되묻기·시안 남발·중간보고로 턴을 늘리지 않는다. 확신 있는 변경은 시안 없이 바로 반영한다.

4. [디자인 정본] 디자인(UI·UX 등)을 수정할 때는 반드시 레포·시스템이 참조하라고 지시한 경로의 기존 정본을 열어보고, 그 안에서 만든다. 새 디자인 창작 금지.
4-1. 정본 진입점 = `디자인기틀/디자인기틀_SSOT.md` · 값 SSOT = `viewer/index.html` `:root` 토큰 · 컴포넌트 = `디자인기틀/CII_컴포넌트계승인덱스.md` 정본 셀렉터 · 버튼·모션 = `디자인기틀/구성도/00_가이드북_버튼인터랙션.html`.
4-2. CLAUDE.md만 읽고 값을 추측한 결과물 = 오류 = 무효. 참조 파일을 실제로 열어 실측한 값만 쓴다.
4-3. 새 색·px·blur·radius·scale 창작 금지. 기틀에 없는 값이 필요하면 멈추고 묻는다. 승인분은 즉시 편입(`:root` 토큰 추가 → `python3 shared/build_design_mirror.py build` → CII 행 추가 → `python3 shared/build_design_mirror.py lock`).

5. [폐기] 1·2·3·4번 규칙을 99.8% 이상 지키지 못했다면 그 세션의 작업은 실패다. 즉시 폐기하고 사용자에게 "규칙을 어겨 세션 폐기를 통보합니다."라고 선언한 뒤 작업을 중단한다.

6. [보고] 결과 보고는 다음 4가지로 한다.
6-1. **완료 여부** — 가장 강조. 막혔으면 「완료」라 부르지 말고 ⚠로, 무엇이·어느 파일 어느 행이 막았고 운영자가 뭘 승인하면 풀리는지 쓴다.
6-2. **전과 후의 모습 — 링크가 아닌 이미지로.** 링크로 제시하면 그 세션의 작업은 폐기한다.
6-3. **3줄 결론** — 초등학생도 이해할 수 있을 만큼 쉽게.
6-4. **아이디어 1개** — 도입 시 90% 이상, 큰 효율을 가져다줄 것으로 보이는 것.
6-5. 6-1에 **라이브 반영 여부**를 같이 못박는다(머지만 했으면 「라이브 미반영」이라고 명시) + 다음 액션.

7. [머지] 성공한 작업이 완료되면 사용자에게 묻지 말고 머지한다. **draft = 폐기**를 뜻한다.
7-1. 작업 끝 = 머지 = 라이브 반영. 머지했는데 운영자 화면이 그대로면 완료가 아니라 미완이다. 시안·문서·계획만 머지하고 「완료」라고 부르지 않는다 — 라이브 표면 파일까지 같은 작업 안에서 바꾼다.
7-2. [라이브 정의] 라이브 = 운영자가 실제로 여는 화면(산출 표면)과 그걸 만드는 코드. `docs/reports/**`(시안·감사·보고)만 바뀐 머지 = **라이브 0** → 「시안 머지 · 라이브 미반영」이라고 못박고, 라이브까지 갈 다음 한 걸음을 같은 답에 붙인다.
7-3. [시안은 경유지] 플레이그라운드는 목적지가 아니라 결정 수단이다. 선택값을 받으면 그 턴에 라이브 배선까지 간다. 시안을 또 뽑는 건 ⓐ 선택값이 아직 없거나 ⓑ 라이브 반영이 규격 개정·승인에 막혔을 때만 — 막힌 게이트 이름과 필요한 승인 한 줄을 반드시 같이 낸다.
7-4. 충돌·체크 실패·비가역 작업이면 머지하지 말고 멈춰 묻는다.
7-5. [자동 머지 절차 — 묻지 않는다] 게이트가 통과(`check_refs` rc=0 · UI 표면이면 `smoke_all` rc=0)하면 **그 턴 안에 main까지 밀어 넣는다.** 순서 = ① 작업 브랜치 커밋 → ② `git fetch origin main` → ③ `git rebase origin/main` → ③-b **게이트 재실행**(`python3 shared/check_refs.py` rc=0 — 리베이스 자동병합이 정본을 되돌렸는지 확인 · 260802 사고 재발방지 · `.githooks/pre-push`가 이중 방어) → ④ `git push -u origin <작업브랜치>`(재시도 회차면 리베이스로 SHA가 갈렸으니 `--force-with-lease` — 빼면 브랜치 푸시가 non-ff로 거절되고 표식 PR의 head가 옛 SHA에 머물러 ⑤ 뒤에도 merged 판정이 안 난다) → **④-b 표식 PR 1개**([7-6]) → ⑤ `git push origin HEAD:main`. ⑤가 non-fast-forward로 거절되면(봇 커밋이 초당급으로 main에 들어온다) ②~⑤를 재시도한다(PR은 이미 있으니 ④-b는 건너뛴다 — ④의 force-with-lease가 PR head를 새 SHA로 끌어올린다). 머지 성공 = 보고 6-1·6-5에 커밋 해시와 함께 「main 머지 완료」로 못박는다.
7-6. [표식 PR = 머지 신호등 · 260802 개정(운영자 "저걸로 머지했냐 안 했냐를 구분한다 · 정신없을 땐 글자 하나하나를 못 본다")] ④-b에서 **비-draft PR 1개**를 연다(GitHub MCP `create_pull_request` · base=main · head=작업브랜치 · `draft:false`). 열자마자 ⑤가 **같은 SHA**를 main에 밀어 GitHub이 그 PR을 **merged**로 닫는다 → 클로드 코드 세션 목록에 머지 표시 점등. **코드·히스토리 차이 0**(⑤가 종전 그대로라 main은 선형 유지 · 260802 실측 = main에 `Merge pull request`·머지커밋 **0건**인데 #3474~#3495는 전부 merged = 직행 푸시가 PR을 자동 merged 처리한다는 증거 · 스쿼시 `(#N)` 접미도 안 붙는다). 규칙 = ⓐ **draft 금지**([7] draft = 폐기) ⓑ **리뷰 대기 금지** — 열어두고 기다리지 않는다, ④-b와 ⑤ 사이는 초 단위 ⓒ **fail-soft** — PR 생성이 실패해도(이미 열린 PR·diff 0·API 오류) ⑤는 그대로 진행한다, **표시보다 머지가 우선** ⓓ 이미 열린 PR이 있으면 새로 만들지 않고 그걸 쓴다 ⓔ ⑤가 [7-4·7-7]로 막히면 PR을 **열어둔 채** 멈춘다 — 열린 PR = 미완, merged = 완료, 표시 없음 = PR 생성 실패. 부수효과 = PR 전용 게이트 `ledger-gate`·`check-refs`(원장 Q번호 경합) 부활 — PR 금지 기간엔 한 번도 안 돌던 이중 검증이다. 머지가 끝난 브랜치는 그대로 두면 `branch-cleanup-stale`이 걷어간다(보존 = **열린** PR head뿐이라 즉시 닫히는 표식 PR은 청소를 안 막는다).
7-7. [멈춤 조건 = 7-4뿐] 리베이스 충돌·게이트 rc≠0·비가역(데이터 삭제·강제 푸시로 남의 커밋 소실) → 머지 중단하고 그 사유 한 줄과 함께 묻는다. ⚠ 로컬 `main` 브랜치 ref는 신뢰하지 않는다(이 환경의 클론은 origin/main과 히스토리가 갈린 스테일 ref일 수 있다 — 260802 실측 "refusing to merge unrelated histories") · 기준은 **항상 `origin/main`**.

8. [평의회] 시스템 기틀·로직·알고리즘 등 중요한 코드를 손보는 경우 평의회를 진행한다.
8-1. 평의회 = 동일 모델 `--effort max` 8인 병렬 검증. 서로 다른 각도·관점으로 문제를 해결하며, 내 해결법과 상호 의견에 적대적 관점을 지닌다. 가능한 한 일괄로 같이 출발해 종료 시점을 맞춘다.
8-2. 단순 디자인·국소 손질에는 발동 금지(토큰 과소비). 대상 = 기틀 문서·토큰 구조·동작 로직·전반급 변경.

9. [납품] 산출물은 **운영자가 가장 편하게 끝낼 수 있는 형태**로 준다. 「방법을 알려주는 것」이 아니라 「눌러서 되는 것」을 넘긴다. 넘기기 전에 항상 한 번 묻는다 — "운영자가 이걸 받아서 몇 번 움직여야 끝나나?"
9-1. 운영자에게 절차를 떠넘기면 미완이다 — 경로 찾기·명령어 조립·파일 여러 개 세트·환경 준비가 필요하면 아직 완성이 아니다. 자기완결 1개로 묶어 채팅에 **파일로 첨부**한다(위치 안내·복붙 명령어로 갈음 금지).
9-2. 선택지 나열 금지. 가장 쉬운 1안을 골라 그것부터 낸다 — 대안은 그 아래 한 줄로만 붙인다(3-1 되묻기·시안 남발 금지와 같은 축).
9-3. 첫 실행에서 걸릴 것을 미리 제거한다 — 준비 안 된 마운트·경고창·코드페이지·권한 등 예상 장애는 코드로 흡수하고, 못 없애는 건 「이런 창이 뜨면 이렇게」로 먼저 알린다. **끄는 법과 로그 위치를 항상 같이 준다.**
9-4. [레포 밖 라이브] 운영자 PC·폰에서 도는 산출물은 머지가 라이브가 아니다 — 라이브 경계 = **운영자의 실행 1회**. 그 1회를 최소로 줄이는 게 이 규칙이고, 남은 1회는 6-1 ⚠에 못박는다(7-2와 같은 축).

## 이 레포 전용 (nomute-editor)
- [7-2 라이브] 이 레포의 라이브 = `viewer/**` · `apps/**` · `functions/**` · `shared/**`(생성 코드) · `scraper/**`. 머지 diff가 `docs/**`에만 닿았으면 라이브 0 = 미완 — 보고 6-1·6-5에 그대로 쓴다.
- **[7-5 실행기 = `shared/merge_main.sh`(운영자 260803 "7-5 ㄱㄱ")]** 머지 ②~⑤는 손루프 금지 → `bash shared/merge_main.sh prep`(fetch·rebase·게이트·④브랜치 푸시) → 표식 PR 비-draft 확인/생성(④-b = MCP 축) → `bash shared/merge_main.sh land`(재시도 루프). 260803 실측 사고 3종의 구조 봉합 = {ⓐ ⑤가 ④를 앞서면 표식 PR이 merged 대신 closed(#3498) ⓐ-2 구 봉합(④+⑤ --atomic 동시 도착)도 리베이스 라운드에선 PR head·base가 **같은 SHA로 동시 기록** → GitHub이 "변경 0"으로 closed(#3538 = 점등 실패 변종 · 2차 개정) → **④ 브랜치 선착 → ⑤ main 후착 순차 2발**(점등은 head 먼저·base 나중 순서에서만 성립 = 260802 #3474~#3495 실측 · ⑤ = --no-verify로 경합 창 압축[같은 SHA가 ③-b+④훅 이중 기통과 직후라 3번째 동일 게이트만 생략] · main plain push 불변) ⓑ push 출력 문자열 오판(성공 "HEAD -> main"을 재패배로 읽음) → 성공 판정 = **fetch 후 origin/main==HEAD 사후 대조**}. Q번호 경합은 land가 --fix-qnum+amend+메시지 동기 자동. **⑥ 점등 사후 대조(운영자 260803 승인)** = land 성공 직후 표식 PR merged를 API 실측(`GITHUB_TOKEN`/`GH_TOKEN` · head=브랜치 최신 PR 1건) — CLOSED 확정만 **rc=7**(머지 자체는 성공 → 보고 6-1에 「main 머지 완료 · 점등 실패」 강제 · 새 변종 기록), 토큰 없음·API 불가·PR 미발견·open 지연 = fail-soft rc=0 + MCP `pull_request_read` 재대조 지시(성공을 실패로 오판 금지 = ⓑ 교훈 · [7-6ⓒ] 표시보다 머지 우선). 멈춤 조건([7-4/7-7] 리베이스 충돌·게이트 rc≠0·라운드 소진)은 rc≠0 + 사유로 정지 — 스크립트는 main에 force 계열을 절대 안 쓴다.
- [병렬 세션 락] 여러 세션이 같은 컴포넌트를 동시에 갈아엎으면 뒤에 온 쪽이 통째로 재작업한다(260802 코너 레일 3회 실측). 핫 컴포넌트 착수 = `python3 shared/lock.py take "<컴포넌트>" <파일…>`(완료 = `release` · TTL 90분 자동 만료) → `check_refs`의 `check_component_lock`이 **남의 살아있는 락과 겹친 변경**을 커밋 직전에 띄운다(WARN·비차단 = 락이 레포를 얼리지 않는다).
- **[스튜디오 레일 = 무조건 상속(운영자 260802 "항상 이미지 스튜디오나 영상 스튜디오는 저 로직을 따르게 만드셈 죽는 한이 있더라도")]** 이미지·영상 스튜디오의 **모든** 세부 메뉴(카드 생성·편집·특수·번역·AI 생성 / 영상 편집·콘티·프롬프팅·음원·큐영상)는 미리보기 코너 옵션 레일의 정본 로직을 **예외 없이** 따른다 — ① 픽토 기준 크기 = **버튼 22×22 / 글리프 12×12**(구현 = `<스코프> .trail svg { width:12px; height:12px; display:block }` 한 줄 · 버튼 클래스별 개별 svg 규칙 금지) ② 빈 캡슐 소거 술어 = `:not(:has(.trail-g > button:not([hidden]))):not(:has(.trail-v:not(.none)))` ③ 캡슐·값 칩 그룹·값 칩 규격 = CII 「미리보기 코너 옵션 레일」 값 사본. **강제 = 정적 `check_refs.check_trail_spec()` 5축 하드게이트 + 표면 자동 발견**(`viewer/*.html` 중 `class="trail`을 가진 파일은 전부 대상 — 손 레지스트리 등재 여부와 무관하므로 새 탭이 조용히 빠질 수 없다) **+ 런타임 `shared/smoke_studioshell.js`**(2셸 10탭 실렌더 = 전체창·도크 유리·2셸 교차 잉크선·픽토 22/12 · `smoke_all` 자동발견 동행) **+ 세로축 = 도크↔첫 블릿 잉크 한 값 = `smoke_studioshell` C9**(운영자 260803 "간격도 제각각, 어느 부분엔 구분선이 있고 — 카드 제작 나오는 부분간 간격으로 통일" · 기준 = 카드 제작 `.csec{margin-top:16px}` → 잉크 18px · 폰 430 전용 sweep{코어 1280은 2단 그리드라 `.out`이 옆 칼럼으로 올라가 세로축 측정이 무효} · 면책 = `GAP_BASE`{콘티 요약 스트립·프롬프팅 명세 스트립·큐영상 소스 헤더(메뉴바는 260803 3차에 도크 위로 이주) = 첫 자리가 소머리 블릿이 아닌 3탭 — 대신 박스 16으로 정합} · ⚠ 신설 사유 = C1~C8이 전부 **가로·부품** 축이라 「도크 아래로 본문이 어디서 시작하나」가 게이트 사각이었고 그 틈에서 8탭이 조용히 갈라졌다{실측 2·2·9·13·18·27·33·44px}) **+ 결과 레일 실존·거처 = `smoke_studioshell` C12**(운영자 260803 6차 "편집처럼 우측에 결과" · 폰 430 sweep 동승 = 이미지 5탭 전부 `.out`/`#geniOut`이 화면에 있고 **제 거처**{자기 프레임 / 살아있는 #geniHost}에 있다 · ⚠ 신설 사유 = 그 작업에서 AI 판 레일이 1단에서 **다운로드 창 안으로 이사**해 통째로 사라진 실사고{rect 0×0 · 콘솔 에러 0 = 무증상}인데 C7 res 축은 1280 2단에서만 돌고 C9·C10은 잉크·미리보기만 재서 「레일이 화면에 있는가」가 사각이었다 · C11은 미리보기 창 클론이 선점) **+ 스크롤 드리프트 = 스크롤 중 미리보기 부동 = `smoke_studioshell` C10**(운영자 260803 "도입 ㄱㄱ" · 발단 = 도크 정지 y2·sticky y0 잔여 2px가 스크롤 순간 -2px 덜컹{iframe 7표면}인데 AI 생성 판만 0이라 스크롤 중 2px 어긋남 — C7은 창 **크기**만·C9는 **정지** 세로축만 재서 「스크롤 **중** 위치」가 게이트 사각이었다 · 판정 = 폰 430 sweep 스크롤 140px 후 미리보기 박스 |Δ|≤0.5{무스크롤 탭 N/A·최소 측정 3 가드} · 정본 = 도크 마진 -18 = wrap 패딩 전액 상쇄 = 정지·고정 동일점{260716 "덜컹 원천 소멸" 원칙·킬테스트 = -16 재현 시 번역 (0,-2) 검출 실증}) **+ 미리보기 창 5탭 클론 = `smoke_studioshell` C11**(운영자 260803 4차 "각 5개 메뉴를 클릭하면 미리보기 시작점이 조금씩 어긋나 · 스크롤 여부 등에 따라 배치도 틀어져 · 아예 동일하게 복제본 마냥" · 판정 = 이미지 5탭 미리보기 창의 **프레임 절대 [x,y,w,h]** 가 카드 생성과 한 값{허용 0.6} · 티어 2종{폰 430×900 + **2단 경계 940×1000**} + **상태 오염 순회**{편집 전용 `.ocfit`을 손으로 얹은 뒤 5탭 순회} · ⚠ 신설 사유 = C7은 창 **크기**만 **1280 한 티어**에서 재서 ⓐ 시작점 x·y ⓑ 2단 경계 티어 ⓒ 탭을 오가며 **남는 상태**가 전부 사각이었다 — 실측 사고 2종 = ⓐ 편집 `.ocfit`{원본+사진} 잔류로 카드 생성·특수 창 207→**2px** 붕괴{공유 노드 `.cpprev-box`} ⓑ 번역 2단 `--pvw` 미러 누락으로 940×1000에서 폭 −34.4·좌변 +17.3px · 킬테스트 = 수정 전 코드로 두 축 전부 FAIL 검출 실증 · 짝 = 확대 상태 **표면 공용 슬롯 1벌**{`window.top.__nmPvZoom` · 탭 전환 시 `pvZoomSyncActive` 재도색 = 260802 3차 "탭 이동에 크기가 튀지 않는다" 정본 복구}) **+ 결과 레일 2단 분기점 표면 간 한 값 = `check_refs.check_twocol_breakpoint`**(시그니처 `span 99` 자동발견 · 260802 실사고 = thumb만 900 하향·영상 4탭 1100 잔류가 스모크 1280·폰 430 실측기 사각을 그냥 통과) **+ 값축 거처 = 다값 카드 헤더 축은 헤더 우측 칩 단일 문법 = `check_refs.check_axis_chip_home`**(운영자 260804 "idea go" · 계약 = 「카드 헤더가 담당하는 **다값**(CYC 3값 이상) 축은 `hdChips`로만 그린다」 — 본문 행 `segs` 혼용 = 커밋 차단 · ⚠ 신설 사유 = 컷 편집 「강도」 4택1이 비율·해상도와 같은 성격인데 **다른 문법**으로 그려져 260728 Q10 통일에서 혼자 빠진 채 6일 잠복했고, 기존 게이트가 전부 「값이 정본과 같은가」만 봐서 「**같은 축을 어디에 그리는가**」가 축 자체로 없었다 = 운영자가 눈으로 찾아야만 발견되던 사각 · 위양성 3겹 = ⓐ 스코프 = **살아있는 카드 렌더 경로만** 자동 발견(cardHtml 본문 + `body=XXX()` 피호출 함수 → 도먼트 시트 `prm()`의 구 문법 사본 7건은 스코프 밖 = 도먼트 보존 관례와 충돌 0) ⓑ 대상 = 다값 축만(이진 OFF/ON = 워드 점등 축 = `check_onoff_literal` 전담 — 섞으면 「hdChips로 고쳐라」가 오답) ⓒ 대상 축 = CARDAX 값 ∪ 스코프 내 hdChips 축(= 카드 **헤더**가 그리는 축)에 한정해 `fit`(방식)처럼 「헤더는 비율 칩이 차지 → 아래 구획」이 정본인 축은 자연 제외(260712 3차 동급 위계 계약 보존) · 표면 자동 발견(`cardHtml` ∧ `hdChips` 보유 뷰어) · 면책표 없음 = 현재 위반 0[ar·cutlv·res 3축] · 실증 = 킬테스트 1종{cutBody에 구 segs 부활 → rc=1·축명 정확 검출} + 위양성 대조 4종{fit 아래 구획·이진 bgm·도먼트 prm 신규 축·주석 속 예시 = 전건 무경보}) **+ 이진 토글 = 워드 점등(ON/OFF 리터럴 금지) = `check_refs.check_onoff_literal`**(운영자 260803 "기능 워딩이 점등하냐 안하냐로 onoff" · 정본 = thumb #cnTog 라벨 자체 점등 · 면책 = _ONOFF_BASE{페이로드·다값 순환} 스냅샷·축소 지향). ⚠ 정적만으론 안 걸리는 사고가 실재한다(260802 3차 실측 = CSS는 각자 멀쩡한데 **셸 사이**가 갈라진 것: 영상 팝업 잔류·도크 순흑 잔류·번역 레일 버튼만 34px·thumb `--line2`가 `--line` 동값) → **셸 골격은 런타임 교차 비교로만 잡힌다**. 새 스튜디오 탭을 만들면 레일도 같이 계승한다(선택 아님).
- **[동기화 생명선 SSOT = `viewer/nm-sync.js`(운영자 260803 4차 "전 스튜디오 상속")]** 이미지·영상 스튜디오 전 탭(thumb·tr·edit·sb·k·song·vd)이 `<script src="nm-sync.js">` 한 줄로 상속 — ① 비활성→활성 복귀 시 `window.nmRefresh` 자동 호출(타 기기 제작분 재동기) ② `/manifest.json` redirect:manual 프로브로 Access 로그인 만료 확정 시 최상위 `/?nosw=1` 자동 재진입(15s 열림 창 + sessionStorage 3분 루프 가드 · 회선 사망은 재진입 무익 = `nmSyncWarn` 폴백) ③ 자기 문서 HEAD ETag 대조로 새 배포 감지 → 한가할 때(`nmSyncBusy` 훅·입력 포커스 없음) 자동 리로드 = 신규 코드 수동 재열기 불요. 새 스튜디오 탭 = 이 한 줄 동반 계승(레일 상속과 동축·선택 아님) · 강제 = `check_nm_sync` 하드게이트(고정 7표면 + `window.nmRefresh` 보유 표면 자동 발견 + 모듈 3축 골격 검사).
- **[입력칸 클립 SSOT = `viewer/nm-clip.js` + `viewer/nm-clip.css`(운영자 260803 "모든 텍스트 입력창에는 붙어있어야돼 · 공식처럼가야함")]** 텍스트 입력칸은 예외 없이 **[복사][붙여넣기][지우개/되돌리기]** 클립을 단다 — 부품 = 이 2파일 단일정본(index 인라인 정본의 바이트 이관 · 로직·값 무변경) · 상속 = 뷰어 head `<link rel="stylesheet" href="nm-clip.css">` + `<script src="nm-clip.js">` 2줄(`nm-svg.js` **뒤** = 아이콘 상수 의존 순서) · 신규 칸 = `attachCopyPaste(el, true)` 1줄. 표면 스코프 오버라이드(`.revdlg .iobtn-edge` 하단 걸침 등)만 각 뷰어 인라인 잔류(인라인이 뒤라 재승리). **면제 = 숫자칩(`.geni-ar-in`·`.rsz-ar-in`·`.opa-in` = 값 선택 UI라 생김새가 칩)과 PIN(`#pinInput` readonly·`#pubPin` 잠금) 뿐** — 늘리려면 `_CLIP_EXEMPT_*`에 사유와 함께 1줄. 강제 = `check_clip_coverage` 하드게이트(전 뷰어 정적 입력칸 자동 발견 · 3문법{attachCopyPaste·.scnclip·.urlclip} 배선 흔적 인정 · `<script>` 안 템플릿 문자열 제외 = 클립 폴백 자신(`.pastefb-ta`)을 잡는 자기참조 차단). ⚠ 260803 전수 감사 실측 = 51칸 중 **30칸 누락**(음원 탭 6칸 전멸·미디어 요약·찾아바꾸기·구독 메모·검색바) — 게이트 없이는 새 칸이 조용히 빠진다.**
- **[스튜디오 텍스트 입력칸 SSOT = `viewer/nm-input.css` · 게이트 `check_input_canon`(운영자 260804 "편집의 폰트 형태를 따라가는게 정석 · 폰트 형태 & 텍스트 박스 형태 둘다 · 딱 그부분만 다른 곳에 다 배선" → "창 크기까지 · 3줄짜리로 · 1줄 아니면 3줄임 · 항상 텍스트 박스는 저거로 고정" → "게이트 ㄱㄱ")]** 정본 원천 = `viewer/thumb.html`(카드 제작·**편집**·특수) 입력칸 블록 = 실측 **13.5px/400 · lh 21.6 · ls −0.2 · pad 12 · 면 rgba(14,15,17,.55) · 선 accent .35 + 링 3px .08 · r --r-s** · 창 크기 계약 = **1줄(`input.nmin`) 아니면 3줄(`textarea.nmin` = 104px = thumb `.covrow textarea` 3줄 정본), 중간 없음 · 높이 재선언 금지**. 상속 = 뷰어 head `<link rel="stylesheet" href="nm-input.css">` 1줄 + 칸에 `class="nmin"` 1줄. **클래스 훅인 이유** = 셸 문서(`viewer/index.html`)는 뉴스 앱까지 담아서 정본처럼 bare `textarea{}`를 풀면 피드·검색·구독 메모까지 스튜디오 폼으로 칠해진다(도구 문서 thumb·tr은 문서 전체가 폼이라 bare 셀렉터가 정본으로 잔류) · 「딱 그부분만」 = 칸 자신의 활자·박스만, 오버레이(포스트) 칸의 자간·마진·별표 하이라이트·클립 자리 padding-right는 그 칸 전용이라 비대상. ⚠ 신설 사유 = **같은 칸이 하루에 세 번 갈렸다**(활자 13px/700 → 박스 pad 6/10·면 흰 .04 → 창 96 vs 104) — 셋 다 정적 게이트를 그냥 통과했고 운영자 눈이 유일한 검출기였다(기존 게이트는 「클립이 붙었나」·「자동완성을 껐나」만 보고 **「그 칸이 정본 활자·박스·창 크기로 태어났나」는 축 자체가 없었다**). 강제 3축 = ⓐ 정본↔SSOT 값 8축 + 3줄 창 대조 ⓑ `.nmin` 보유 + link 실존 + 여러 줄 칸 구 칩 문법(`.geni-in`) 부활 0 ⓒ `.nmin` 스코프 height류 재선언 0 + `rows=3` · 면제 = `check_clip_coverage`와 같은 경계(`_CLIP_EXEMPT_CLS` 숫자칩·PIN) 재사용 = 손 목록 이중화 0 · 실증 = 킬테스트 8종 전건 검출 + 무변경 위양성 0(⚠ 실측 구멍 2종을 킬테스트가 잡았다 = 정본 블록 앵커를 `select`로 두면 앞쪽 활자상속 빈 블록이 첫 매치 · SSOT link를 문자열 언급으로 판정하면 주석만 있어도 통과).
- **[모델 표시명 SSOT = `viewer/nm-models.js`(운영자 260803 4·5차 "모델명 항상 통일 · 아이디어도 배선")]** 화면에 모델을 이름으로 쓸 땐 `NM_MODELS` 사전 값만 — 음차(클링·시댄스·페이블·오퍼스·제미나이·수노)·변형(Kling 3.0[비Omni]·GPT 5.6[비Sol]·Seedance 2.5·Gemini 3.1 flash/Flash image) 금지. sb·k = 런타임 참조 · 서버(`api/sb.js` DIRECTOR_NM)·index GENI_ENG_ICO·k `.eng` 라벨 = 게이트가 사전과 문자 동기. 새 모델 = 사전 1줄부터 · Seedance = **2.0**(운영자 260803 확정 "2.0이 맞고"). 강제 = `check_model_names` 하드게이트(음차·변형 파일별 래칫[주석 인용 = 면책 스냅샷 `_MODEL_NM_BASE`] + 리터럴 표면 동기 + sb/k 로드 줄 실존).
- **[부채 래칫 = `shared/debt_ledger.json`(운영자 260803 "ㄱㄱ")]** 이 레포의 게이트는 전부 면책표(baseline)를 안고 산다 — 그래야 신규 회귀만 잡고 레포가 안 언다. 문제는 면책표가 **한 방향으로만 자란다**는 것: 등재는 1줄이고 해소분 삭제는 아무도 안 한다 → 「알고 동결한 부채」가 「원래 그런 것」으로 굳는다(260802 INK_BASE `{편집:2.6}` 회수 = 사람이 우연히 발견한 축). → `check_debt_ratchet`이 `shared/check_refs.py`·`shared/smoke_*.js`의 `*_BASE`/`*_EXEMPT` 리터럴을 **자동 발견**해 항목 수를 원장과 대조 = **늘면 커밋 차단 · 줄면 낮추라고 알린다**(줄이는 건 자유·늘리는 건 사유 + `python3 shared/check_refs.py --debt-sync` diff가 승인 기록). 260803 기준 = 12표 171건. 카운터 = **최상위 depth 콤마**(dict·set·정수키·튜플키 동일 규칙 — 구 「'키':」 패턴은 set·정수키 표를 통째로 0으로 세어 「부채 없음」이라 거짓말할 뻔했다 = 실측 봉합). **미해결 부채는 원장 `open_items`에 사람 말로 남긴다**(안내문 카드 h27 덮개 · 번역 입력칸 · AI생성 포맷 칩 · **레일 셸 간 2.4배** · 빈 미리보기 박스 7탭) — 매 커밋 출력에 그대로 떠서 잊히지 않는다. ⚠ 원장 = 기계산출물 = 손편집 금지.
- **[히트존 3축 = `shared/smoke_hitzone.js`(운영자 260803 "기본 아냐 저건?")]** 「보이는 버튼 = 눌리는 버튼」 상비 스모크(2셸 10탭 격자 프로브 · smoke_all 자동발견) — H1 캡슐 안 단독 컨트롤의 히트 면적 ≥55% · H2 히트 높이 ≥30px · H3 **어포던스 거짓말 0**(손가락 커서가 뜨는데 누를 게 없는 자리). 신설 사유 = 260803 자막 사고(카드 127.3×37.2인데 히트존 24.7×19.2 = **면적 10%**)가 기존 게이트를 전부 통과했다 — check_refs는 정적 문자열, parity·studioshell은 잉크·치수·골격 = **전부 「보이는 것」만 잰다**. 정적 짝 = `check_affordance_inherit`(스킨은 계승·어포던스는 비계승 = `cursor:pointer`가 마커 없는 노드에 걸리면 커밋 차단 · 계약 전문 = CII 「미리보기 코너 옵션 레일」 행).
- **[퇴장 모션 1정본 = `@keyframes` 중복 정의 0 = `check_keyframes_dup`(평의회 260804 4·5·8번)]** CSS Animations L1 §2 = **같은 이름이 두 번 선언되면 뒤엣것이 앞엣것을 통째로 대체**한다(병합 아님·부분 오버라이드 아님) → 중복은 「무해한 사본」이 아니라 **앞 선언을 조용히 삭제하는 문장**이다. 실사고 = `viewer/index.html` `popOut` 2벌 — 414행(살아있는 `.pmenu`·`#lockpop`·`.qpop` 퇴장 정본) vs 1418행(**260710에 폐지된 `.filterpop` 잔해** · 요소 0건·`closeFilterPop` = no-op 스텁)인데 **캐스케이드 승자가 1418** = 죽은 컴포넌트의 사본이 살아있는 팝업 3종의 모션을 지배했다. 값이 동값이라 **6주간 무증상** = 정본(414)을 고쳐도 화면 무변, 죽은 사본을 한 글자 건드리면 무관해 보이는 팝업 3개가 동시에 갈리는 최악의 추적난도. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축을 본다 — `check_design`은 토큰 **값**, `check_layout_transition`은 전이 **속성**, `smoke_*`는 애니가 **끝난 그림** → 「같은 이름이 두 번 선언됐는가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM 0) · 표면 자동발견(`viewer/*.html`+`viewer/*.css`) · **면책표 없이 하드 0**(현행 위반 0 = 원장 증가 0). ⚠ 스코프 = HTML은 `<style>` 블록 안만(화이트리스트 = @keyframes의 유효한 거처가 그뿐) · `<script>` 안에서 조립되는 리포트 HTML의 `<style>`은 배제(`check_clip_coverage` 템플릿 문자열 배제 원칙 계승) · **`.js` 비대상**(`nm-loader.js`처럼 런타임 주입이 곧 SSOT인 부품 = 중복이 아니라 단일정본 배포) · **파일 간 동명 비대상**(`goFill`×10·`gdots`×8 등 17종 = 뷰어는 독립 문서고 `tokens.css`는 구조토큰 거울 전용이라 keyframes 공유 수단이 물리적으로 없다 · 크로스파일로 세면 60건+ 위양성으로 레포가 언다). ⚠ 함정 실측 = HTML 주석을 파일 전역에 먼저 지우면 CSS 주석 제거가 HTML 주석의 닫는 `-->`를 지워 `index.html` 45~3692행이 통째로 blank 처리되고 **popOut 2건이 둘 다 사라져 「중복 0」으로 거짓 통과**한다 → 구간(`_kf_regions`)을 먼저 잘라 교차오염을 구조적으로 봉쇄. 실증 = 킬테스트 5종(위반 검출·주석 무시·수리 후 위양성 0·`<script>` 문자열 무시·`<style>` 밖 무시).
- **[잰크 전이 래칫 = `check_layout_transition`(운영자 260804 「impeccable 적용점」 8인 평의회 · 선별이식 ①)]** `transition:width|height|padding|margin`(min/max 변종 포함)은 프레임마다 리플로를 강제한다 = 잰크. 이 레포는 「덜컹」을 반복해 겪고(260716 도크 마진 −18 "덜컹 원천 소멸" · 260803 스크롤 중 −2px = `smoke_studioshell` C10) 그때마다 축을 늘렸는데 **전부 애니메이션이 끝난 뒤의 그림**을 잰다 — C7은 창 **크기**, C9는 **정지** 세로축, C10은 스크롤 **후 정착** 위치. 「transition이 재생되는 **중간 프레임**」은 축 자체가 없었다. 판정 = `viewer/*.html`+`viewer/*.css` 자동발견(새 뷰어가 조용히 못 빠진다) · 주석 줄 제외 · 렌더·LLM 0 정적 · 면책 `_LAYOUT_TRANS_BASE` 파일별 스냅샷 초과만 FAIL(줄면 낮추라고 알린다 = raw baseline 문법 동문). ⚠ **하드 0 금지** = 현행 7건(edit 1·index 4·thumb 2)은 전부 선존 아코디언(`max-height`)·폭 모프라 제거 = [4] 창작금지 충돌 → 래칫이 유일한 정합 형태. 임계값 원천 = 외부 디텍터 `pbakaus/impeccable` `cli/engine/rules/checks.mjs`의 `layout-transition` 속성 집합을 **그대로 채택**([4-3] 값 창작 0) — 그쪽 **설치·훅·스킬·23커맨드는 미도입**(평의회 실측 = 59룰 통째 적용 시 두 레포 경고 690건 중 **574건[83.2%]이 글래스·글로우·균일간격 등 이 레포 정본**이라 위양성이고, `colorize`/`bolder`/`delight`/`overdrive`는 디자인 창작 = [4] 정면 위반이며, 훅은 `.claude/settings.local.json`에 **병합·병렬**로 붙어 `design_gate.py`의 정본 강제와 상충 지시가 같은 턴에 도착한다). 실증 = 킬테스트 3축(위반 검출·주석 줄 무시·`opacity`/`transform` 위양성 0).
- **[앵커 메뉴 문법 = 한 벌 = `check_anchor_menu_canon`(운영자 260805 "아이디어 ㄱ")]** 같은 결의 메뉴 3종 — **설정 `#linkpop` · 스튜디오 헤더 메뉴 `.tool-menupop` · PASS 사유 `.sc-rsn`** — 이 셸 기하·항목 행 값을 **각자 베껴 쓰고 있었다**. 정본이 바뀌면 나머지가 조용히 낡는데 증상이 「어느 창 하나만 좀 달라 보임」뿐이라 **운영자 눈이 유일한 검출기**였고, 실제로 260805 실측 시점의 PASS 사유 창은 혼자 다른 문법(테두리 칩 12/700 · 닫기 X 20px 원형 · radius 12 · 폭 172~280 가변)으로 갈라진 채 살아 있었다. → 값을 **셀렉터 그룹 2벌**에 모은다(위 「앵커 팝업 셸 글래스」 그룹[index 252행] 관례 그대로 = 마크업 변경 0·드리프트 0): 셸 기하 = `#linkpop, .tool-menupop, .sc-rsn`{폭 230 고정 · padding 11/15 · gap 3 · radius --r-l} · 항목 행 = `#linkpop .lp-btn, .tool-menupop button, .sc-rsn-c`{13/600 · pad 8/4/8/--sp-4 · r7 · 무테 · hover = --accent + 유리 .06}. 각 표면엔 **위치·모션·상태 오버라이드만** 남는다. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다(`check_design` = raw **개수** 래칫 · 팔레트 핀 = 뷰어 **간** 동값 · `smoke_winnav` = 모달 **헤더** 기하) → 「같은 결의 메뉴가 한 벌인가」는 축 자체가 없었다. 판정 = 정적(렌더·LLM·네트워크 0) · **면책표 없이 하드 0** · 2축 = ① 두 그룹 셀렉터 실존(그룹 해체·개명 = fail-closed) ② 멤버의 **단독 규칙**에서 SSOT 축 재선언 0. ⚠ 스코프 = 정확한 단독 규칙(`sel {`)만 — `:hover`·`.up`·`.inp` 변형과 미디어쿼리 오버라이드는 대상 밖(각 표면 고유 권한을 얼리지 않는다). ⚠ **함정 실측** = 단독 규칙 판정을 「직전 1글자가 `\n{}`인가」로 두면 이 레포의 **2칸 인덴트 CSS에서 전건 미검출**이 된다(킬테스트 K1·K2가 재선언을 심었는데 rc=0으로 통과) → 줄 시작부터 셀렉터까지가 **공백뿐인지**로 판정해야 그룹 멤버(`…, .sc-rsn {`)·복합(`.x .sc-rsn {`)도 함께 정확히 갈린다. 새 앵커 메뉴 = 두 그룹에 셀렉터 1개씩 추가(유리는 252행 그룹 동반) · 실증 = 킬테스트 4종 전건 검출{셸 재선언·항목 재선언·구 `.lp-btn` 단독 부활·그룹 해체} + 위양성 대조 2종 무경보{변형 셀렉터·무변경} + 3표면 런타임 등가 실측(셸·항목 computed 차이 **0**).
- 커밋 전 `python3 shared/check_refs.py` 필수(`.githooks/pre-commit`이 강제). UI 표면(뷰어 html) 변경 커밋 전 = `bash shared/smoke_all.sh` rc=0. ⚠ **새 게이트에 `[^{}\n]*`·`[^{}]*` 류 여는-괄호-앞 와일드카드 정규식을 쓰지 마라** — `viewer/index.html`이 1.85MB라 매 위치에서 되짚어 **한 줄이 게이트 전체 시간의 절반**을 먹는다(260804 실측 = `check_input_canon`의 `.nmin` 규칙 정규식 혼자 **17.9s** = 77게이트 총 37.5s의 48.9% · 리터럴 스캔 선행으로 치환해 **0.002s**·전건 동일 실증 → 총 **37.5s → 11.4s**, 체감 pre-commit 30.5s → 11.3s). 정본 문법 = `_nmin_rules()` — 리터럴 `find`로 후보를 O(매치수)로 줄이고 그 자리에서만 규칙 경계를 판정한다.
- **[최근 게시물 커버 = 6중 회수 · 운영자 260803 "이번 문제 안일어나게" → "빈칸을 최대한 없애는 방향"]** 채널 요약 「최근 게시물」 타일의 커버는 **한 소스에 걸지 않는다** — 인스타 Graph가 릴스 `thumbnail_url`을 무성 생략하는 사고가 260718(2/25) → 260803(2/12)로 재발했고, 실측상 그 2건은 미디어노드 재조회·인스타 **공개** 커버 경로(`/p/<code>/media/?size=l` → `null.jpg`)까지 전멸이라 IG 계열엔 자산 자체가 없었다. 체인(수집 = `.github/scripts/insta_fetch.py` · 산출 = `apps/insta/insta_signals.py` `_thumb_src` · 화면 = `viewer/index.html` `chThFail`) = ⓞ 미디어노드 재조회(260718) → ⓞ-b **인스타 공개 커버 경로** `/p/<code>/media/?size=l`(비인증·토큰 무관 · 결손건만 1콜 · 종착이 `null.jpg` 플레이스홀더면 버림 = 오탐 0 · 실측 260803: 정상 릴스 전건 회수) → ① `thumbnail_url` ② **페이스북 크로스포스트 `full_picture`**(같은 워크플로 `fb_fetch` 산출 `viewer/fb_data.json` 재사용 = API 콜·네트워크·LLM 0 · 매칭 = ⓐ 캡션 첫줄 정규화 프리픽스[≥12자] ∧ 게시시각 ±10분 ⓑ 실패 시 **시각 ±3분 유일 후보**[그 창에 FB 후보가 정확히 1건일 때만 = 유일성이 오매칭 봉인] — 노뮤트는 IG·FB 문구를 각각 다시 써서 캡션만으론 크로스포스트를 놓친다 · 실측 260803 = 캡션 매칭이 정답을 아는 4건과 **모순 0**, 2차 단독 회수 3건) ③ **마지막 성공 커버 원장** `apps/insta/data/thumb_cache.json`(미디어 id별 최신 생존 URL · CDN `oe=` 만료 인지 = 만료분은 안 씀 → 깨진 이미지 대신 캡션 타일 · 최근 표본 우선 200개 컷) ④ 뷰어 `chThFail`(로드 실패 = **공개 커버 경로 1회 재시도**[서버가 구운 CDN URL은 `oe=` 만료로 죽지만 permalink는 안 죽는다 · `null.jpg`는 400이라 onerror 재발동 → `dataset.rt`가 막아 무한루프 0] → 그래도 실패면 타일 **소멸이 아니라** `.ch-th.txt` 캡션 타일 강등 — 구 `display:none`은 12칸 그리드에 구멍을 냈다). **⑤ 회수 출처 카운터·결손 알림**(운영자 260803 "아이디어 ㄱ") = 회차마다 `{ig·pub·mu·fb·cache·none}` 집계를 `viewer/insta_data.json` `thumb_src`에 굽고, 결손 연속회차를 원장 `_meta.none_streak`에 누적 — **2회 연속**이면 `shared/msg.py`로 `insta-thumb-miss` warn 알림(**본문 = 인수인계 진단서** — 운영자 260803 "다른 세션에서 이 경고 다운받아도 해결 쉽게" · 회차 출처·막힌 permalink·이미 시도한 6층·다음 확인 순서·재현 명령·코드 위치를 한 장에 · 뷰어 메시지함 [↓]가 그대로 HTML로 내보낸다)(1회성 API 딸꾹질과 구분 · 해소되면 자동 clear · 커밋 = insta-fetch.yml git_land 인자 `messages`). 화면이 캡션 타일이라 멀쩡해 보이는 탓에 「조용히 나빠지는 것」이 유일한 사각이었고, 이 카운터가 그걸 숫자로 드러낸다(부수효과 = 어느 층이 실제로 일하는지 보여 안 쓰는 층 축소 근거). 강제 = `check_thumb_chain` 하드게이트(수집·산출·뷰어 전 층 심볼 + `.ch-th` 타일 줄 onerror 문법 + yml 인자 · 끊기면 커밋 차단). ⚠ 원장은 기계산출물 = 손편집 금지(값 변경 = 생성 코드 수정 후 재실행).
- **[SNS 카드 커버 = 워드마크 폴백 · 운영자 260803 "sns 모두 적용"]** 트렌드·구독 카드(`.tcard-cov`)와 X·스레드 게시물 카드(`.xcard-cv`)의 커버가 로드 실패하면(핫링크 만료·CDN 교체·403) **img를 숨기지 않고** 플랫폼 워드마크 폴백으로 강등한다 — 정본 = 레딧 폴백 `.tcard-cov.noc.rdfb`(무채 `#676767` + 순백 워드마크 · 260729)의 **값 100% 사본**이라 플랫폼별 새 색 창작 0, 구분은 `::after content` 글자뿐(`ytfb`·`tkfb`·`igfb`·`thfb`·`xfb`·`rdfb` 6종 · 긴 워드마크만 19cqw 기준값의 글자수 스케일). 배선 = 각 소스 map에서 `nocls` 주입 → `covImg`가 `data-nofb`로 실어 → `tcCovFail`이 강등. ⚠ 구 `onerror="this.style.display='none'"` 부활 = 카드 빈 구멍(260803 실측 = 전 커버 사망 시 숨김 161·박스 소멸 16 → 폴백 168·숨김 0) · 강제 = `check_thumb_chain`(6클래스 정의·배선 + 구 숨김 문법 금지). 260801 「이미지는 항상 일정한 공간」 계약 보호도 같은 축(`.xcard-cv`가 사라지면 카드 높이가 흔들린다).
- **[출처 글 본문 이미지 수확 = `.github/scripts/ask_srcimg.py` · 게이트 `check_ask_srcimg_chain`(운영자 260804 "확인해줘")]** SNS 카드 「전송」으로 보낸 커뮤니티 글이 요약에 실패하던 축의 봉합. 260804 실측 사고(`fail-2026-08-04-0239-297it` · 보배드림 best/1017600) 정체 = **「사이트를 못 읽음」도 「내용을 긁고도 뜻 모름」도 아니다** — 페이지는 정상 취득됐는데 **본문이 이미지 2장뿐이라 읽을 글이 0자**였고, 제목("이런걸 재능이라고 하는구나")은 고유명사가 0이라 프롬프트 1-2 기사 유추 폴백도 검색 2회 공회전 → 확보 사실 0 → 모델이 날조 대신 ANALYSIS_FAILED(판단 자체는 정직했다). ⚠ 파이프엔 **이미 멀티모달 레일이 있었다**(asks `images[]` → `workdir/img-*.jpg` → 프롬프트 「첨부 캡처」) — 사각은 단 하나, 그 레일에 **글의 본문 그림이 실린 적이 없다**는 것(`socBindSend`는 {제목·주소·RSS요약} 텍스트만 보낸다 · images 0장). → 수확기가 출처 URL의 본문 이미지를 내려받아 같은 레일에 태운다 = **신규 분석 로직 0**. **[OCR 층 = `.github/scripts/ask_srcocr.py`(운영자 260804 2차 "사진안에 문자열을 토대로 가져오게 배선 · 뉴스 요약에 연결")]** — 그림을 **첨부만** 하면 ⓐ 본선이 그걸 여는지가 프롬프트 준수 확률에 달리고 ⓑ 열어도 '전문(원문)' 지위를 못 얻는다(프롬프트 1)의 보강 모드는 「요청문에 전문이 있으면」 발동하는 축이라 그림만으론 그 레일에 안 물린다) → 문자열을 확정적으로 뽑아 **미디어 전사문과 같은 지위**로 실으면(`ask_link_stt.sh` 「이 전사문이 곧 원문이다」 문법 계승) 기존 뉴스 요약 보강 로직에 그대로 연결된다 = **신규 요약 로직 0**. 수단 = **Claude 헤드리스 1콜**(과금 0 구독축 · 종량제 벤더 신설 0 = `models.json` vendors 무접촉 · 폴오버·계측 = `shared/claude_py.run_claude` SSOT 경유 · 모델 = `ASK_OCR_MODEL` 기본 `claude-sonnet-5` 기계 판독 티어) · 킬스위치 `ASK_SRCOCR=0` · **fail-soft 2단**(OCR 실패 = 구 그림 첨부 블록으로 내려앉음 = 악화 경로 0). 실측 260804 = 사고 글 2장에서 **456자 추출**(X 캡처 전문 + BEST 댓글 전문 · 페라리 광고 배너는 「글자 없음」으로 정확히 분리). **[일괄 축(운영자 260804 3차 "sns 안에 있는 내용이 **일괄로** 뉴스 요약에 들어가는 소스로 · 요약요청 UI에서 전송할 때 참조되는 소스로")]** — 2차 배선은 OCR이 **수확분(`src-*`) 안쪽 if에 갇혀** 있어 운영자가 요약요청 창에서 **직접 붙인 캡처(`img-*`)** 는 여전히 파일 경로로만 실렸다(= 같은 그림이 **어느 입구로 들어왔느냐**에 따라 ⓐ 확률 축 ⓑ 전문 미획득 두 구멍이 그대로 남는 분기) → OCR을 **이미지 존재 축으로 독립**시켜 `ocrimgs=(img-* src-*)` 한 배열로 모아 **1콜 일괄 추출**(입구별 동작 차이 0 · URL 없는 순수 캡처 요청도 발동 · 수확분도 하단 「첨부 캡처 파일」 목록에 합류해 원본 나열 경로 단일화). `LINK_BLOCK`(미디어 전사문)이 이미 원문을 점유했으면 추출문은 **보조 소스**로 문구 자동 강등(원문 지위 이중 선언 방지). 강제 = 게이트 ⑥-b 2축(`ocrimgs` 글롭에 두 접두 동시 존재 + OCR 입력이 일괄 배열) · 실측 5축 = {첨부만 456자 · URL만 526자 · 둘 다 828자(5장 일괄) · 킬스위치 구 동작 · 무이미지 무발동}. 체인 = ask.sh 출처 URL 선정(`srcUrl` 우선 → 요청문 첫 URL 폴백 · `link` 레일이 잡은 요청은 무접촉) → `ask_srcimg.py` → `ask_srcocr.py` → 프롬프트 🖼 **추출문 블록** → **프롬프트 1-3 폴백**(본문이 그림뿐일 때 순서 = ① 추출문을 원문으로 삼는다[없으면 그림 Read] → ② 거기서 나온 고유명사로 검색 보강 → ③ 보도 없으면 게시물 자체를 소재로 완성 · 어느 경로든 ANALYSIS_FAILED 금지). ⚠ **UA 2단이 실효 조건**(장식 아님) — 실측상 보배드림은 `fetch_article.sh`의 **모바일 UA를 봇 차단**해 http 200에 본문 없는 3,566B 껍데기를 준다(조용한 실패) · 데스크톱 UA면 같은 URL이 160,268B = 본문 이미지 취득 성공 → 데스크톱 1순위 + 껍데기(`SHELL_BYTES`) 시 모바일 1회 교대. ⚠ **텍스트 길이로 「그림이 본문인가」를 가르는 판정은 실측에서 폐기**했다 — 본문 0자인 그 페이지의 전체 텍스트가 7,558자로 나온다(사이드바·추천글·메뉴 혼입 · 본문 컨테이너는 매체마다 달라 일반해 없음) → 게이트를 **바이트로 실측 가능한 축**으로 이전(치수 미명시 + 다운로드 바이트 하한 + `px_size()` 실측 픽셀 `PX_MIN`) · 낭비는 스코프가 흡수(출처 URL 축 전용). 킬스위치 = `ASK_SRCIMG=0` · 전 경로 fail-soft(수확 실패가 요약을 죽이지 않는다). 실증 = 그 글의 수확 2장을 읽으니 화제가 그대로 드러났다(보험 직업분류 「남성은 전업주부를 선택할 수 없습니다」 + 그걸 재프레이밍한 BEST 댓글 = 제목의 「재능」이 가리키던 것).
- **[알림 리포트 = 세션용 자기진단(운영자 260805 "너가 그 내용을 클로드 코드 세션이 봤을때 빠르게 알아낼 수 있도록")]** 알림 리포트(`msgRptHtml`)의 항목표는 **다음 세션이 코드를 거슬러 올라가지 않고 판정을 바로 읽는** 자산이다 — 3축 신설/봉합. ⓐ **발화 근거(`why`)** = 조건을 통과시킨 **값 자체**(fire = `lm="…" ∧ crit(유형)` · quake = `규모 N ≥ 문턱 M(국내/해외)`) — 구판 「판정 소스」는 *어느 필드를 보나*까지만 말해서 그 필드가 **무엇이었길래** 울렸는지는 세션이 라이브 데이터를 따로 떠서 맞춰야 했다(260805 서명 오탐 진단 20분의 대부분). 생산자가 실어준 알림만 행을 그린다(빈 행 = 잡음). ⓑ **재현 명령(`cmd`)** = 붙여넣으면 바로 도는 1줄(재난 = `viewer/sns_trends.json` disaster 전건 lm 출력) = [9] 「눌러서 되는 것」 축. ⓒ **출처 분기 = `check_rpt_origin_coverage` 하드게이트** — ⚠ 실사고 = `_rptSrc()`에 `sys:fire:`·`sys:quake:` 분기가 없어 마지막 폴백으로 떨어져 「만든 곳 = 서버 발행 메시지 · 판정 소스 = `messages/sys:fire:….json` · 상류 = `scraper/msg.py`」 **셋 다 틀린 값**을 줬다(그 파일은 존재조차 안 한다 · 실제 생산자 = 뷰어 `fireMsgs()`) → 세션이 그걸 믿고 `messages/`를 뒤졌다. 리포트 계약이 "세션이 파일을 바로 열게 하는 값"이라 **없는 값보다 틀린 값이 더 비싸다**. 구조적 원인 = 알림 생산자는 늘어나는데 출처표 분기는 손으로 따라가야 한다 → 생산자 id 리터럴(`id: 'sys:…'`) 자동 발견 ↔ `_rptSrc` 분기(`startsWith('sys:…')`) 접두 대조(세분화 분기 허용 · 정적 · 렌더·LLM 0 · 면책표 없이 하드 0 · 현행 6종 전건 커버). 새 `sys:` 알림 = 생산자와 분기를 같은 커밋에 넣는다(선택 아님).
- **[요약 실패 자동진단서 = `.github/scripts/ask_fail_probe.py` · 원장 `asks/fail_ledger.jsonl`(운영자 260805 "자동진단서 ㄱ")]** 요약 요청 실패 알림에 그 URL 의 **기계 실측**(받은 바이트·한글 자수·껍데기/프레임 판정·프레임 해제 결과·수확 이미지/OCR 층 발동 기록)을 자동 동봉 — 260804 사고 2건(보배드림·네이버 블로그) 다 「입력이 비었거나 불충분」 한 줄뿐이라 세션이 매번 원인 실측부터 다시 하던 축의 기계화. **같은 도메인 14일 창 2회 재발 = 인수인계 진단서 승격**(시도한 층·이번 실측·재현 명령·코드 위치·다음 확인 순서 — insta-thumb-miss 진단서 문법 계승 · 재발 문턱 2 = brk_misfire '같은 축 2건' 동축). 취득·해제는 `ask_srcimg` 정본 import 재사용(별도 fetch 창작 0) · 킬스위치 `ASK_FAIL_DIAG=0` · 전 경로 fail-soft(진단 실패가 실패 알림을 못 죽인다) · 원장 = 기계산출물 손편집 금지(상한 400줄 롤링 · news-ask.yml `git add queue asks` 동반 커밋 · 소비 글롭 asks/*.json 과 확장자 불충돌 실측). 강제 = `check_ask_srcimg_chain` ⑧축.
- **[구독 = 등록 계정만 · 작성자 검문 = `check_subs_author_scope`(운영자 260804 "내가 구독한 애들이 아닌데")]** 「스레드 - 구독」이 **등록한 적 없는 계정 20건**으로 통째 채워진 실사고(등록 5계정 0건 · 회차마다 명단이 통째로 갈림 = 추천 피드 롤링 · 전건 `region:"gl"` = 등록 사전 미매칭 폴백의 기계적 증거). 구조 = `threads_subs()`의 walk()가 응답 안 **모든** 포스트 노드를 걷는데, 게스트 응답은 프로필 주인 글만 담지만(260804 실측 = 등록 5계정 37건 전건 본인 · other 0) 로그인 상태(`THREADS_COOKIE`)·로그인월 리다이렉트 응답에는 **추천(For you) 피드**가 같이 실린다 → 무검문 채집 = 메타 알고리즘 피드가 '구독' 칸을 강탈. X·인스타·틱톡은 항목 `account`를 요청 계정으로 못박아 구조적으로 불가능했고 **스레드만 노드의 `user.username`을 신뢰**해서 갈렸다. 봉합 3층 = ⓐ `threads_subs()` 작성자 대조(요청 계정 ≠ 작성자 = 폐기 · username 결측도 폐기 = 주인 확인 불가) ⓑ 성공 도장 = **본인 글 확보** 기준(구판은 `posts` 유무로 찍어 추천 피드만 걷힌 회차를 `got 5/5 miss []` **전건 성공**으로 보고 = 화면은 남의 글인데 게이트 무경보 = 가장 조용한 실패) ⓒ 러너 채택 지점 등록 명단 화이트리스트(폰이 구 파서로 돌아도 러너가 거른다). 강제 = `check_subs_author_scope` 하드게이트(3층 심볼 + 구판 `or acc` 폴백 부활 차단 · 정적 · 렌더·LLM 0).
- **[스플래시→앱 배경 3값 정합 = `check_boot_bg_parity`(운영자 260805 "아이디어도 진행하구")]** 같은 색이 세 파일에 **손으로 복사**돼 있다(`viewer/manifest.json` `background_color`·`theme_color` · index `#bootveil` 배경 · index `meta[theme-color]`) — CSS·meta가 manifest를 못 읽어 `var()` 도달이 구조적으로 0이기 때문이다. 계약 = ⓐ `#bootveil` == manifest `background_color`(근거 = 그 선언 자신의 주석 "manifest background_color 단색을 첫 페인트가 그대로 이어받고") ⓑ `meta[theme-color]` == manifest `theme_color`. ⚠ 신설 사유 = 한쪽만 고치면 나머지가 조용히 낡는데 증상이 「스플래시에서 앱으로 넘어올 때 색이 한 번 튄다」뿐이라 눈으로 안 잡힌다 — 기존 게이트는 전부 다른 축이다(`check_design` = 토큰 raw **개수** · 팔레트 핀 = **뷰어 간** 동값 · 거울 정합 = `:root` ↔ `base.css`) → 「OS가 그리는 색과 우리가 그리는 색이 같은가」는 축 자체가 없었다. **ⓑ는 하드**(현행 일치 `#070a12`) · **ⓐ는 WARN·비차단** = 260805 실측이 이미 어긋나 있다(manifest `#000000` vs 베일 `#070a12` · 260724 아이콘 교체 때 background만 순흑으로 갈린 것으로 보이나 미확정) — 해소는 「둘 중 어느 값으로 통일하나」 = 색 결정 = [4-3] 운영자 승인 대기라 하드로 못 올린다. 승인 시 값 통일 → 하드 승격. 앵커 소실(선언 형태 변경) = fail-closed 하드.
- [셸캐시 put 절단 검문 · 260802 재발 봉합] `nm-shell-*` 캐시에 닿는 **모든** `.put(`은 꼬리 `</html>` intact 술어를 동반한다(청크 스트림 절단 = res.ok 그대로라 무검문 put = 잘린 셸 기기 감금). 1차 봉합이 sw.js put만 막고 index `applyShellUpdate` 페이지측 put을 놓쳐 3시간 만에 재발한 축 — 강제 = `check_shell_put_integrity` 하드게이트(put 상행 15줄 근접 검사 · 무검문 put = 커밋 차단).
- **[geni 어휘 전역 질의 금지 · 260803 6차 "게이트 ㄱㄱ"]** AI 생성 폼은 **홈이 둘**(팝업 `#genidlg` ↔ 스튜디오 이식 `#geniHost` · geniMount가 노드를 통째 옮긴다)이고, 설정▸다운로드 창(`#dlgrab`)이 그 문법을 계승해 `.geni-body`·`.geni-histfold` 클래스를 **공유**한다 → `document.querySelector('.geni-…')`는 문서순 첫 매치 = **남의 창**을 물고, 이식 중에는 구조적으로 그렇게 된다(#geniHost가 #dlgrab보다 뒤). 실사고 = ⓐ `geniOutPlace` 전역 `.geni-body` → 폰 1단에서 결과 레일이 다운로드 창 안으로 이사해 **화면에서 소멸**(rect 0×0 · 콘솔 에러 0 = 무증상 · 그 창이 본문 재구축하면 노드 영구 소멸) ⓑ `geniInit` 전역 `.geni-histfold` → 남의 창 폴드에 핸들러 덧바인딩(CDP 리스너 0→1) ⓒ 잠복 = 문서 말미 `cscrollAttach` 같은 질의. 통과 문법 = **id 앵커**(`#genidlg .geni-body`) 또는 **스코프 호출**(`geniRoot().querySelector`·`h.querySelector`). 강제 = `check_geni_scope` 하드게이트(`viewer/*.html`+`viewer/*.js` 정적 문자열 · 진입 = document{속성 체인 포함}·$·$$ 계열 · 판정 = 셀렉터를 콤마로 쪼갠 **각 항이 `geni-` 어휘를 담으면 `#`(id 앵커)로 시작해야 한다** · 주석 줄 제외 · 렌더·LLM 0 · 면책 `_GENI_SCOPE_BASE` = 현재 비어 있음 · 평의회3 실측 우회 10수법{백틱·`document.body` 경유·`dialog .geni-body`·`div.geni-body`·콤마 뒤쪽·`getElementsByClassName`·`[class*=geni-]`·점 주위 공백·`body` 앵커·무앵커} 전건 차단 확인 · 남은 한계 = 변수 경유{`const s=".geni-body"; qs(s)`}는 정적으로 못 잡는다).
- **[긴급 오발 신고 폐루프(운영자 260803 4차 "누적될 때 활용을 안 하면 소용이 없다 · 3개 적체되면 자동 로직이 알림메세지에 개선안 · 내가 알림메세지를 놓치진 않거든")]** 🚨긴급 배지를 **롱프레스 480ms**(수집함 `.sc-badge.brk` · 피드 `.urg.brk` 두 표면 공용 · PC 우클릭 동치)하면 「긴급 아님」 신고가 `reason` 예약키 `brkno`로 `scraper/ratings.jsonl`에 적재된다(전용 필드 불가 = `rate.yml` workflow_dispatch inputs가 이미 **10개 = GitHub 상한** · 11번째 = 디스패치 400 = 평점 레일 전체 사망). 소비 = `scraper/brk_misfire.py`(rate.yml 동행 + watchdog.yml 30분 타이머 = 운영자 행동과 무관하게 TTL 상시 갱신) → **발화 = ⓐ 같은 축 2건 재발(1순위 = 진짜 신호 · 오발은 축별로 오고 한 축이 패치되면 안 터지므로 '총 3건'은 대개 서로 다른 축 3개 = 잡음) ⓑ 총 3건 ⓒ 아사창 72h(1~2건이 죽은 데이터가 되는 것 차단)** → `shared/msg.py`로 알림메시지에 **개선안**(공통축 + 게이트 개정 제안 + 신고 전문). **알림 id = 건수 접미 회전**(`brk-misfire-N`) — 고정 id면 뷰어 unread가 id축이라 메시지함을 **한 번 열면 영영 재점등이 안 된다**(✓ dismiss면 전 기기 영구 실명 · 레포 관례 = `sys:quake:`+time 회전). 해소 = 회귀 원장 편입 **또는 RUBRIC 해시 변경**(제목 완전일치 단독은 원장 표기가 한 글자만 달라도 영구 미해소 = 영구 재발화) · 오신고 기각 = 원장 `rejected`. ⚠️ **회귀 원장 자동 편입 금지** — 미검증 케이스가 들어가면 나중에 RUBRIC을 건드리는 **다른 세션**이 스탬프를 못 찍어 rc=1에 갇히는 지연 시한폭탄(그래서 `check_rubric_regress`가 케이스 **개수**까지 대조). ⚠️ 소비기는 **fail-open**(`|| echo ::warning::`) — `bash -e`라 rc≠0이면 커밋 전에 죽어 방금 적재한 별점이 증발한다. 강제 = `check_brk_misfire_chain` 하드게이트(뷰어 신고·예약키·워크플로 스텝·소비기·알림 5층 + inputs 10/10 상한 감시 — 한 층만 빠져도 신고가 조용히 죽는 원장이 된다).
- **[루브릭 회귀 원장 = `.github/scripts/rubric_regress_cases.json`(운영자 260803 "ㄱㄱ" 승인)]** breaking_judge RUBRIC(속보 판정 프롬프트) 개정 = `python3 .github/scripts/rubric_regress.py` 드라이런 필수 — 과거 실측 판정 케이스(실발송·실픽 이력 + 평의회 보호 케이스 22건)를 현행 RUBRIC으로 재판정해 정답 뒤집힘(오컷·오발 회귀)이 0이어야 스탬프(rubric_regress_stamp.json)가 찍힌다. 강제 = `check_rubric_regress` 하드게이트(스탬프 해시 ≠ 현행 RUBRIC 해시 = 커밋 차단 · 게이트 자체는 정적 대조 = LLM 0 · LLM 1콜은 실행기 시점만 · RUBRIC 무변경 커밋 = 무비용 통과). 기대값(expect) 개정 = 운영 방침 변경 = 커밋 메시지에 사유 명기. ⚠ 신설 사유 = 260803 「대구 40.1도」 오발 — 프롬프트 수정의 회귀를 사람 눈으로만 보던 축의 기계화.
- **[Pages 빌드 코얼레싱 = 선별 [CF-Pages-Skip](운영자 260803 · 페이블 5인 평의회)]** CF Pages는 커밋당 1회 FIFO 풀빌드(46s·훅 빌드도 맨 뒤 = 추월 0 실측)라 봇 커밋 유입(피크 74/h)이 처리율(78/h·CF 감속일 ~33/h)에 육박하면 큐가 시간 단위로 밀린다(260803 실사고 = 4시간 → 대기열 무한 스피너·감지 0). 봉합 = 「화면에 수 분 늦게 떠도 되는 데이터 churn」 커밋에만 **[CF-Pages-Skip]** 접두(CF 전용 — GitHub [ci skip] 5종에 미포함 = push 발화 무손상 · 유일 안전 토큰) → 스킵분은 다음 비스킵 빌드(scrape 15분 메트로놈·코드·제작)가 tip 통째 배포 = 누적·유실 0 · 신선도 상한 ≈ 17분. 배선 = `git_land.sh` 헤더(sns-trends·insta·fb·lucy) + 인라인 5곳(social-scan·breaking-judge 경중 grade만·metrics-rollup·watchdog·rate) · 킬스위치 = repo variable `PAGES_COALESCE=0`(부재=ON) · **금지 축 = stamp(:14 명문)·제작 산출(체크런 게이트 5종)·news-analyze/ask(notify 240s 축)·scrape(메트로놈)·auto-pick·조기 반영** — 강제 = `check_pages_skip` 하드게이트(_PAGES_SKIP_ALLOW 등재 파일 밖 접두 = 커밋 차단) **+ 짝 강제 `check_coalesce_pair`(운영자 260803 6-4 승인 · Q1343 실사고 기계화)** = 코얼레싱 워크플로가 `git_land`로 착지시키는 `viewer/*.json` 중 **뷰어가 실제 fetch하는 표면**은 반드시 빌드 우회 라이브 서빙(`functions/api/*.js`)을 보유해야 한다(없으면 커밋 차단 · 전부 자동 발견 = 손 목록 0). ⚠ 없으면 스킵된 커밋의 산출이 **다음 비스킵 빌드까지 화면에서 정지** = 조용한 죽음 — 실측 = `tbs_data.json`(키워드 알림 국내 감시축 · 260720~26 "6일 빈 감시" 재발 축)이 그 상태였고, 게이트 첫 실행이 `threads_state.json`(lucy 화면)까지 **사고 전에** 선제 검출했다. ⚠ CF측 감속(μ 붕괴)은 레포로 못 막는다 — 감지 = watchdog ⑦ 배포 지연(90분·30분 주기). 잔여 미배선 = functions/api/seen.js(일 5건 · CF Function GitHub API 커밋 = env 게이트 불가 · 실익 미미).
- [워크플로 --amend 금지 · 260803] 러너 결과 커밋 재시도 루프의 `git commit --amend`는 자기 커밋이 rebase에서 드랍되는 순간 **남의 푸시된 커밋을 개서** → non-ff 영구 거절 = 산출 유실(filltest 동시 3발사 실측 · 런 30779914067 — 원조 imggen의 안전한 새-커밋 방식을 사본(resize→upscale)이 --amend로 개악한 복제 사고). 재병합 = **새 커밋으로만**(`git diff --cached --quiet || git commit -m …`) · 강제 = `check_workflow_amend` 하드게이트(.github/workflows 전수 · 실행줄 --amend = 커밋 차단 · 현행 0건 청정).
- **[프롬프트 리터럴 인용 무결성 = `check_prompt_literal_quoting`(260805 실사고 `fail-2026-08-04-{1528,2211}` 봉합)]** ask.sh 프롬프트 1-3) 블록에 **이스케이프 안 된 큰따옴표**가 들어갔다(`실패 사례 제목 = "이런걸 재능이라고 하는구나"`) → 첫 `"`가 `prompt="`를 닫아 뒤 텍스트가 셸 토큰이 되고(`재능이라고: command not found`), `prompt=…`는 그 명령의 **환경변수 프리픽스**로 흡수돼 변수 자체가 안 잡힌다 → `set -u` unbound → `claude -p`에 **빈 stdin** → "Input must be provided…" = **요약 요청 전건 100% 실패**(요청 내용과 무관한 고정 리터럴이라 뭘 넣어도 죽는다 · 실측 = 15:28 네이버·22:11 코리아탑뉴스 두 건이 **같은 줄에서** 죽었다). ⚠ **가장 조용한 실패의 전형** = 문법적으로 완전히 유효해 `bash -n`·커밋 게이트 전건 통과하고, 화면 문구는 「내용 분석 결함(입력이 비었거나 불충분)」이라 **입력 탓으로 보인다** — 그래서 260804 세션이 그 문구를 믿고 엉뚱한 축(네이버 프레임 셸)을 봉합했고 진범은 6시간 더 살아 다음 요청도 죽였다(프레임 해제 자체는 유효한 개선이나 이 사고의 원인이 아니었다). 같은 줄의 백틱은 `\``로 이스케이프돼 있었다 = **사람 눈이 따옴표만 놓치는 축** → 기계 검사가 유일한 해. 강제 = 하드게이트(`.github/scripts/*.sh`+`shared/*.sh` 자동 발견 = 새 스크립트가 조용히 못 빠진다 · 판정 = `_shell_literal_leak` bash 인용 문맥 정적 훑기{`$(…)`·`${…}`·heredoc·작은따옴표 스킵} · 렌더·LLM·네트워크 0 · 면책표 없음 = 현행 위반 0). 정상 관용구 4종은 갈라낸다{구분자·줄연속 `\`·다음 할당·한 줄 env-프리픽스 체인의 종점 명령} — 실측으로 위양성 200→70→10→**0**까지 좁힌 경계다(`METER_SRC=ask METER_REF="$base" … claude_meter` 형태가 레포 전역 관용구라 안 갈라내면 게이트가 레포를 통째로 얼린다). 실증 = 킬테스트 5종 전건 검출{사고 원복·줄중간 `case )` 할당·LINK_BLOCK·SRCIMG 블록·영문 본문} + 위양성 대조 3종 무경보{정상 이스케이프·주석 속 따옴표·무변경}.
- **[실패 사유 = 「코드 결함」 축(운영자 260805 "아이디어 ㄱㄱ" · 위 사고의 짝)]** 그 사고가 6시간 더 산 진짜 이유는 **알림 문구가 원인을 반대로 가리켰기 때문**이다 — 사유 분류가 {timeout·congest·source} 3종뿐이고 `source`(내용 결함)가 **else 분기**라, 모델이 입도 뻥긋 못한 파이프라인 사고까지 「입력이 비었거나 불충분」이라고 말했다(진단서가 붙어도 분류가 `source`면 그 진단서마저 소스 축으로 읽힌다 = 이번 실사고는 URL이 멀쩡한데 `prompt` 변수가 안 잡혀 죽은 건). → **`_fk=code` 신설** · 술어 = `rc≠0 ∧ 출력 0 ∧ ANALYSIS_FAILED 없음`(= 모델이 답한 적이 없다 · 앞 3분기 통과 뒤라 timeout·congest와 겹침 0 · 비용 0 문자열 검사). 문구 = 「**코드 결함** — 네 입력 문제가 아니야 / 첫 오류: <stderr 첫 줄> / 재시도해도 같은 자리에서 죽어」 = **stderr 첫 줄 동봉**이 핵심(실측상 그 한 줄이 진범을 그대로 가리켰다 · 자르기는 문자 단위 = analyze `_why` 관용구 계승, `head -c`는 한글을 쪼개 U+FFFD를 박는다). **두 경로 동시 보유가 계약**(ask·analyze) — 한쪽만 고치면 나머지가 조용히 구 문구로 남는 게 가장 흔한 미러 드리프트라 `check_ask_srcimg_chain` ⑧-b가 두 파일을 함께 본다(킬테스트 = analyze 축 제거 시 검출 실증). 실증 = 분류 6형 실측{실사고 rc1·빈출력 → code · ANALYSIS_FAILED → source · rc124 → timeout · Overloaded → congest · 형식불량 → source · ANALYSIS_FAILED+rc≠0 → source} = 기존 3축 회귀 0.
- **[썸네일 화풍 투표 폐루프 = `scraper/thumb_vote_report.py` · 게이트 `check_thumb_vote_chain`(운영자 260805 "좋으면 thumb up · 별로면 thumb down · 그게 남게끔 해서 나중에 한번 보자" → "ㄱㄱ")]** **정답지가 없어서** 260805 프롬프트 5축 봉합이 실제 발사분 236건 **손 재판독**으로 나왔다 — 「운영자가 어느 썸네일을 좋다고 봤나」가 시스템 어디에도 안 남았기 때문. 봉합 = 뷰어 AI 생성 슬롯에 👍/👎(부품 = 카드뉴스 투표 정본 100% 사본 = `.fbup`/`.fbdown`·`THUMBUP/THUMBDOWN_SVG`·`.voted`·2탭 취소 · 새 창작 0 · gen 한정 = 검색 이미지는 우리가 만든 게 아니라 정답지가 못 된다) → `postRate(action='thumb', reason='<up|down|clear>:<sid>')` → `rate_record.py`가 **별도 원장** `scraper/thumb_votes.jsonl`로 분기 → `thumb_vote_report.py`(rate.yml 동행 · fail-open)가 임계 20건·아사창 168h에 **프롬프트 개선 리포트**를 알림메시지로 발화. ⚠ **inputs 신설 0이 실효 조건** — `rate.yml` workflow_dispatch inputs가 이미 10/10 = GitHub 상한이라 11번째 = 디스패치 400 = **평점 레일 전체 사망**(brkno 예약키 선례와 같은 축) → 전용 필드 대신 기존 `action`·`reason`에 태운다. ⚠ **원장 분리가 실효 조건** — `ratings.jsonl`은 「이 기사를 볼 만한가」(자동픽·취향 학습 입력)이고 이건 「이 그림이 잘 나왔나」다. 섞으면 score=0 레코드가 취향 통계에 잡음으로 들어간다. 리포트 4축 = ⓐ 화풍별 승률 ⓑ 👎 몰린 연출코드(`thumb_dispatch` 역참조 = **다음 개선의 착수점**) ⓒ SUBJECT 유무 승률(260805 봉합 축의 사후 검증) ⓓ 👎 실물 발췌(SCENE·연출 = 원장을 안 열어도 알림 안에서 판단 = [9] 축) · **라운드 소비형**(발화분은 `seen` 커서에 박고 다음 라운드는 새 표만 = 같은 결론 재발화 0·최근 경향 매몰 0) · 표본 `MIN_CELL` 미만 셀은 「표본부족」으로 표기(없는 결론을 지어내지 않는다 = [1] 정직) · LLM·네트워크 0. ⚠ 신설 사유(게이트) = 투표는 **두 가지 서로 다른 방식으로 조용히 죽는다** — ⓐ 적재는 되는데 커밋이 안 됨(260805 실사고 = `rate.yml`이 `ratings.jsonl`만 add해 다음 체크아웃에서 증발할 뻔) ⓑ 쌓이는데 아무도 안 읽음(죽은 원장 = brk_misfire가 막으려던 바로 그 축). **화면은 둘 다 멀쩡해 보인다**(버튼이 눌리고 초록이 켜진다) → 정적 층별 생존 강제가 유일한 검출기(정적 · 렌더·LLM 0 · 면책표 없이 하드 0). ⚠ **`_has_exec_line` 헬퍼 신설 = 기존 게이트 공통 구멍 봉합** — 「주석 아닌 실행줄인가」를 `^[ \t]*(?!#)[^\n]*needle` 정규식으로 판정하던 관용구가 **주석 처리를 못 막는다**(`[ \t]*`가 들여쓰기를 한 칸 덜 먹고 백트래킹하면 `(?!#)`가 공백에서 통과하고 `[^\n]*`가 `# `를 삼킨다 · 260805 킬테스트 K1 실측 — `brk_misfire`·`srcimg` 게이트가 전부 이 구멍을 갖고 있었다) → 줄 단위 판정으로 교체하고 brk_misfire도 함께 봉합. 실증 = 소비기 5형 실측{발화·라운드 소비·취소(clear) 무효화·임계 미달 대기·원장 부재} + 킬테스트 7종 전건 검출{소비 스텝 주석·원장 커밋 줄 제거·러너 분기 제거·뷰어 전송 제거·라운드 커서 제거·brk 공통 구멍·위양성 대조}.
- **[뉴스 픽 AI 썸네일 프롬프트 = 자기모순 0 = `check_thumb_prompt_sanity`(운영자 260805 "아이디어 ㄱㄱ")]** 픽 → `thumb_gen.py`가 조립해 발사하는 프롬프트가 **스스로를 무효화하는 두 문장**을 품고 있었다 — 실측(실제 발사분 `cards/*/thumbs/prompts.json` 236건 재판독) = ① **CAMERA 자기모순 6%**(한 줄 안에서 `front-on … symmetrical` ∧ `not a flat head-on` = 「평면 탈피」 꼬리절이 **무조건** 붙어 AG 정면 코드와 충돌 → 모델이 둘을 평균내 어중간한 각도로 도망간다) ② **EXPRESSION 시선 중복 10%**(EM 코드가 CAMERA가 이미 말한 응시를 재지시 → 같은 지시 2회에 표정 축이 밀린다) ③ **MOOD 메타어 59%**(emotion frontmatter가 작성 지시 「1순위 감정 + …」를 받아 값에 'N순위·포지셔닝·독자·스크롤'이 그대로 붙어 온다 — 라벨이 `the reader's dominant emotion`이라 더 헷갈리는 순수 노이즈) ④ **SUBJECT 문장 통째 98%**(likeness 화풍의 「이 공인 얼굴을 그려라」 자리에 **한줄요약 전문**이 실려 그 문장의 날짜·장소·발언이 **제2의 장면**으로 SCENE과 경합 — 실측 「청와대 영빈관」 vs SCENE 「창 없는 대형 홀」) ⑤ **SCENE 위계 부재**(연출 줄이 6~8개 붙어 각자 장면을 암시하면 모델이 '평균'을 그린다 — STAGING·FOCUS엔 `adapt this motif` 래핑이 있었는데 CAMERA·LIGHT·EXPRESSION엔 없었다). 봉합 = `_FRONTAL_RE` 조건부 꼬리절(거리 병기 `_SHOT_RE`와 동일 문법 · 정면 건은 그 정면을 굳히는 **긍정문** 교체 = 부정 프라이밍 0) · CAMERA 정면일 때만 EM 시선 절 절삭(표정은 보존) · `_mood_clean` · `_subject_name`(「인명+직함」만 · **4겹 게이트** = 성씨 사전·조사꼬리·조직접미·지역명 · 못 찾으면 **줄 자체 생략** = 문장 통째 주입 금지) · `SCENE_PRIME` 1줄 신설. ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다 — `check_refs` 계열 = **정적 문자열**, `smoke_*` = **화면 렌더** → 「러너가 실제로 조립해 발사하는 프롬프트가 스스로 모순인가」는 축 자체가 없었고, 운영자에게는 「이미지가 별로다」로만 보여 원인 판별이 사람 눈에만 의존했다(insta-thumb-miss·brk_misfire 동축 = 조용히 나빠지는 것을 숫자로 드러낸다). 판정 = **정본 함수 재판정**(`check_disaster_landmark_sign` 문법 계승 · `build_prompt`를 import해 대표 5케이스(정면 AG-08·응시 EM-12 등 **실측 사용빈도 상위 코드**) × 전 화풍으로 실제 조립 후 5술어 검사 · 사전·정규식 사본 0 · 네트워크·LLM·렌더 0 · **면책표 없이 하드 0**) — 케이스는 **라이브러리 실코드(AG/LGT/SG/EM TSV)** 를 그대로 태우므로 새 연출 코드가 들어와도 같은 술어로 재판정된다(「메뉴만 늘리고 조립을 안 본」 회귀 차단). ⚠ **짝 축 = 구 발사분 잔류는 WARN(비차단)** — `prompts.json`은 기계산출물·과거 발사분이라 세션이 커밋으로 못 씻는다 → 하드로 두면 봉합 커밋 자신이 막히는 자기모순(`check_disaster_lm_stale` ② 선례) → 「씻길 때까지 보이게」가 정확한 역할(260805 최근 120기사 실측 = MOOD 132 · SUBJECT 120 · EM 28 · CAMERA 14 · 새 픽이 돌면 자연 감소 · 옛 기사는 뷰어 「다시 만들기」). ⚠ **남는 한계** = 희귀 성씨·조사 글자로 끝나는 인명(‘김지은’)은 `_subject_name`이 놓쳐 SUBJECT 줄이 생략된다 = **안전측 실패**(참조 얼굴 첨부는 불변). 실증 = 재조립 실측(현행 queue) ①②③④ 잔존 0 · ⑤ 전건 주입 · SUBJECT 53건 전건 실인명(1차 시도의 오탐 22건 「개표에서 선관위가 경기·전북 교육감」 류는 4겹 게이트가 전건 제거).
- **[게이트 실효성 원장 = `shared/gate_hits.jsonl` · 게이트 `check_gate_hits`(운영자 260805 "돌리고 머지 ㄱㄱ" · `check_contract_anchors`의 짝)]** ⚠ 신설 사유 = **85개 게이트가 도는데 「어느 게이트가 실제로 무언가를 잡은 적이 있는지」를 아무도 몰랐다.** 260805 하루만 봐도 `check_image_format`은 켜자마자 track q82 2건을, `check_contract_anchors`는 **자기 설계 오류 2건**을 잡았다 — 반면 **한 번도 아무것도 못 잡은 게이트**가 몇 개인지는 미확인이었다. 그런 게이트는 ⓐ 진짜 청정한 축이거나 ⓑ **판정이 헐거워 아무것도 안 걸리는 죽은 게이트**인데, 계측이 없으면 둘을 구분할 방법이 없다 = **게이트 수가 늘수록 pre-commit만 느려지고 안심은 가짜가 된다**(260804 실측 = 총 11.4s의 근거 있는 감량 수단이 없었다). 3게이트 삼각 = `check_gate_docs`(문서에 적혔나) · `check_contract_anchors`(배선됐나) · **이것(일을 하나)**. 동작 = **전 `check_*`를 모듈 레벨에서 자동 래핑**(`_gate_hits_install()` = main 진입 첫 줄 1회)해 rc≠0 순간을 원장에 1줄 적재 → 관측 `_GATE_HITS_MIN_DAYS`(90일) 초과 시 **0회 발화 게이트 목록**을 커밋 출력에 띄운다. ⚠ **호출부를 한 줄도 안 고치는 게 요점** — 러너의 `if check_x() != 0: rc = 1` 이 85번 반복되는 구조라 손으로 감싸면 diff 85곳이고 **새 게이트가 계측을 빼먹는 게 기본값**이 된다(= 이 원장이 막으려는 병과 같은 축) · `globals()` 치환이라 신규 게이트도 자동 편입 · 소스 텍스트를 읽는 게이트(`check_gate_docs`·`check_contract_anchors`)는 `def` 선언 정규식이라 래핑 무관(`functools.wraps`로 `__name__` 보존). ⚠ **WARN·비차단**이 정확한 역할 — 미발화가 곧 결함은 아니다(청정한 축일 수 있다) · 하드로 두면 레포가 언다(`check_component_lock`·`check_disaster_lm_stale` ② 선례). ⚠ **관측 90일 미만이면 미발화를 보고하지 않는다** — 원장 신설 직후엔 전건이 미발화라 그대로 띄우면 「85개 전부 죽었다」는 **거짓 신호**가 된다(정직한 유예). 킬스위치 = `GATE_HITS=0` · 전 경로 fail-soft(계측 실패·손상 줄이 게이트를 못 죽인다) · 롤링 2000줄(`_meta` 첫 줄 항상 보존) · ⚠ 원장 = **기계산출물 손편집 금지**. ⚠ 적재 타이밍 = pre-commit 훅이 발화를 적재하는 시점은 이미 `git add` 뒤라 **그 회차 커밋엔 안 실리고 다음 커밋에 딸려간다**(무해 · 표본 통계축) · 병렬 세션 동시 쓰기는 충돌 가능하나 유실돼도 무해(어느 쪽이든 표본 1건). 실증 = 킬테스트 6종 전건{관측 유예 10일차 미보고 · 120일 경과 0회 발화 목록 WARN · 킬스위치 · 손상 줄 fail-soft · 원장 없음=관측 시작 · 롤링 상한+`_meta` 보존} + 적재·읽기 라운드트립 실측.
- **[계약 앵커 = 「강제 없는 선언」 차단 = `check_contract_anchors`(운영자 260805 "머지 ㄱ" · `check_image_format`의 짝)]** 문법 = 주석에 **`CONTRACT: <게이트명>`**(다중은 콤마) → 그 계약을 강제하는 게이트가 `shared/check_refs.py`에 **실존**하고 **러너에 배선**돼 있어야 통과. ⚠ 신설 사유 = **이 레포의 계약은 주석으로 선언되는데 주석엔 강제력이 0이라 조용히 낡는다** — 실사고(260805 실측) = `gen_image.py`가 260710에 「전 JPEG 저장 경로 통일」을 **주석으로 선언**해 놓고 `resize_image`(92)·`upscale_image`(94)·`recompose_card`(95)·`card_news`(95)·`img_mosaic`(92)가 **6주 동안 안 따라왔다**. 선언도 있었고 위반도 있었는데 **그 사이를 잇는 것이 없어서** 운영자가 "모두 jpg 90으로"라고 말해줄 때까지 아무도 안 울렸다. 이 레포엔 그런 선언형 주석이 수백 개인데(260710·260716·260802…) 어느 게 살아있고 어느 게 죽었는지 **기계로 아는 방법이 없었다**. 2축 = ① **앵커 → 게이트 실존·배선**(고아 앵커 = 계약을 만들 게이트가 없다 · 죽은 앵커 = 게이트는 있는데 안 돈다 → 둘 다 차단 · 게이트를 지우면 그 앵커들이 고아로 떠서 「폐기됐다」가 드러난다) ② **게이트 → 배선**(정의만 되고 호출 0건 = 「만들어놓고 안 돎」 → 차단). ⚠ 짝 `check_gate_docs`는 **문서 등재**만 본다(이름이 CLAUDE.md에 적혔나) — 「그래서 **실제로 도는가**」는 축 자체가 없었다(문서에만 있고 안 도는 게이트 = 가장 비싼 거짓 안심). ⚠ **구분자는 콤마 하나뿐** — `·`를 허용했더니 이 레포의 일반 구분자(`CONTRACT: check_x · q90 = …`)를 먹어 뒤 낱말까지 이름으로 읽었다(**게이트 첫 실행이 자기 설계 오류를 `q90` 고아 앵커로 검출** = 실측 봉합). 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동 발견(`git ls-files`) · **면책표 없이 하드 0**(260805 = 앵커 6건 전건 유효 · 미배선 게이트 0 · 정의 85개 전부 가동 · 원장 증가 0). ⚠ 스코프 밖 = `_versions/`·`docs/`·`cards/`·`.claude/` · `shared/check_refs.py` 자신(처방문·정규식 리터럴이 곧 앵커 = `check_ytdlp_aac` 선례) · **`.md` 전량**(문서의 처방문 예시가 곧 앵커 — 이 항목 자신이 첫 실행에서 고아 앵커로 검출됐다 · 앵커의 거처는 **강제할 코드 옆**이고 문서 축은 `check_gate_docs` 전담). ⚠ **남는 한계 = 앵커를 「안 다는 것」은 못 막는다**(주석 문법 채택은 자발) — 막는 건 「달아놓고 강제가 없는 것」·「강제가 사라졌는데 선언만 남은 것」 뿐. 씨앗 6건 = 6주 드리프트가 **실제로 났던 그 줄들**(gen_image 진범 줄·thumb_gen `to_jpg90`·tr `TR_JPGQ`·card_news·img_mosaic 누끼·track-make yt-dlp 셀렉터) · 새 계약을 주석으로 선언할 땐 앵커를 같이 단다. 실증 = 킬테스트 6종 전건{고아 앵커·러너 배선 제거(죽은 앵커+미배선 동시)·미배선 신규 게이트 = 3종 검출 / 콤마 다중 앵커·앵커 뒤 `·` 산문·무변경 = 3종 무경보}.
- **[이미지 산출 포맷 2갈래 = `check_image_format`(운영자 260805 "아이디어 ㄱ" · 같은 날 JPG q90 통일의 짝)]** 계약 = **투명·무손실이 정당 사유인 축만 PNG · 나머지 산출은 JPEG q90 단일**(정본 = `gen_image.post_process` `quality=90, subsampling=0, optimize=True`). 2축 = ⓐ **PNG로 굽는 곳은 왜 그래야 하는지가 코드 옆에 있어야 한다** — 사유 어휘(투명·RGBA·마스크·오버레이·누끼·클립보드·아이콘·무손실·도먼트)가 호출 줄 **상행 5줄** 안에 **긍정문으로** 있어야 통과 ⓑ **JPEG 품질 = 90 단일** — 산출이 아닌 인코딩(화면 뒤 블러 장식·LLM 첨부 압축)은 그 줄에 `q-ok: 사유` 마커(CSS `raw-ok`/`reuse-ok` 관례 계승). ⚠ 신설 사유 = **「우리가 굽는 바이트가 무슨 포맷·무슨 품질인가」를 보는 축이 하나도 없었다** — 실측 2종: ⓐ `thumb_gen.py`가 R2 키를 항상 `.png`로 굽는데 실물은 Gemini가 준 JPEG = **거짓 확장자가 6주 넘게 무증상**(Content-Type은 매직바이트로 맞게 나가 화면이 멀쩡했다 = 운영자 눈에도 안 보인다) ⓑ JPEG 품질이 **90·92·94·95 4갈래**로 갈렸는데 `gen_image.py` 주석은 260710에 이미 "전 JPEG 저장 경로 통일"을 선언한 상태 = 선언과 실물의 6주 드리프트. 기존 게이트는 전부 다른 축(`check_refs` 계열 = 정적 **문자열·경로 실존** · `smoke_*` = **화면 렌더** · `check_ytdlp_aac` = **받는 쪽** 코덱). **첫 실행이 곧바로 `apps/track/track_analyze.py` q82 2건을 검출** — 같은 날 손 전수감사가 `quality=`·`jpg_bytes` 어휘만 훑어 `cv2.IMWRITE_JPEG_QUALITY, 82`를 통째로 놓친 자리다(사람 눈의 한계 실증 · insta-thumb-miss·brk_misfire 동축 = 조용히 나빠지는 것을 숫자로 드러낸다). 판정 = 정적(렌더·LLM·네트워크 0) · 표면 자동 발견(`git ls-files` = 새 생성기가 조용히 못 빠진다) · **면책표 없이 하드 0**(원장 증가 0 · 현행 인코딩 호출 35건 전건 정합). ⚠ 스코프 밖(구조적) = `_versions/`·`docs/`·`cards/`·`.claude/`(스냅샷·산출 자산) · `shared/smoke_*`·`*shot*`(테스트 하네스가 굽는 프로브는 산출물이 아니다) · `shared/check_refs.py` 자신(처방문 예시가 곧 위반 = `check_ytdlp_aac` 선례) · **주석 줄 전량**(py `#` · js `//`{`https://` 배제} · 블록 주석). ⚠ 남는 한계 = 변수 경유 품질값은 **같은 파일 1회 추적**까지만(다른 모듈 import 상수는 못 따라간다) = **fail-open**(위양성으로 레포를 얼리는 것보다 놓치는 쪽이 싸다). 실증 = 킬테스트 9종 전건{사유 없는 새 PNG 생성기·리터럴 q95·**변수 기본값 q94**·뷰어 toBlob PNG·유일 사유 주석 제거 = 5종 검출 / q-ok 마커·주석 속 예시(py·js)·무변경 = 4종 무경보}. ⚠ **부정문 무효화**가 실효 조건 — 「번역 카드는 투명 영역이 **없다**」 같은 JPG 쪽 설명이 PNG 사유로 통과하던 구멍을 킬테스트 K4가 실측으로 잡았다(어휘 존재만 보면 반대 뜻 문장이 면죄부가 된다).
- **[다운로드 오디오 코덱 = AAC 강제 = `check_ytdlp_aac`(운영자 260805 "유튜브를 편집가능한 자료까지 받아오게")]** 받은 mp4를 프리미어(·베가스·다빈치 일부)에 임포트하면 **영상만 올라오고 오디오 트랙이 아예 안 붙었다 · 에러도 안 뜬다.** 2단이 겹쳐야 터진다 = ⓐ yt-dlp 기본 정렬의 acodec 우선순위가 `flac>wav>opus>vorbis>aac>mp4a`라 알몸 `ba`가 유튜브 **251(Opus 141k)** 을 **140(AAC 129k)** 보다 먼저 집는다 ⓑ `--merge-output-format mp4`를 주면 `yt_dlp/utils/_utils.py` `get_compatible_ext()`의 `return 'mkv' if allow_mkv else preferences[-1]`이 걸려 **allow_mkv=False → mp4가 Opus와 비호환인 걸 알면서도 mp4를 반환**한다(실측 = `get_compatible_ext(vcodecs=['avc1'],acodecs=['opus'],vexts=['mp4'],aexts=['webm'],preferences=['mp4'])` → `'mp4'` · `preferences=None` → `'mkv'`) → 어도비 공식 지원 오디오 목록(AAC/AIFF/AIF/ASND/BWF/M4A/MP3/WAV/OMF)에 Opus가 없어 **컨테이너가 mp4라 임포트는 되는데 오디오 스트림만 조용히 무시**된다. ⚠ **가장 조용한 실패** = 옵션을 안 주면 mkv로 떨어져 바로 눈치채는데 mp4로 나오니 정상 파일로 보이고, 팟플·VLC·인스타 업로드는 전부 멀쩡하다 = 운영자가 편집 툴을 열어야만 발견. 처방 = **셀렉터에서 알몸 `+ba` 앞에 같은 가지의 `+ba[ext=m4a]`(=AAC) 사본을 세운다**(`bv*+ba/b/best` → `bv*+ba[ext=m4a]/bv*+ba[ext=mp4]/bv*+ba/b/best` · 해상도 제한판도 동일 · **비디오 선택자 `bv*`는 무접촉 = 화질 무변** · 실측 `401+251 acodec=opus 3840x2160` → `401+140 acodec=mp4a.40.2 3840x2160`). ⚠ **금지 2종(운영자 실측)** = `-S "acodec:aac"`는 acodec 정렬이 res를 앞질러 **format 18(640x360) 통합포맷이 이겨 화질이 통째로 망가진다** · `--postprocessor-args "Merger:-c:a aac"`는 이미 AAC인 파일까지 매번 재인코딩(시간·음질 손해). ⚠ 신설 사유 = 기존 게이트가 전부 다른 축이다(`check_workflow_yaml` = 문법 · `check_paths` = 경로 실존 · `smoke_*` = 화면 렌더) → 「받은 파일이 편집 툴에 물리는가」는 축 자체가 없었고, 호출부가 4문법(yml·py·bat·command) 10곳에 흩어져 새 호출부가 조용히 빠질 수 있었다. 판정 = **가지 단위**(`/`로 쪼갠 각 가지 중 알몸 `+ba`를 쓰는 가지는 `+ba`만 `[ext=m4a]`로 바꾼 쌍둥이 가지가 **앞에** 서 있어야 한다) — 「어딘가 앞에 m4a가 있으면 통과」로 두면 **다른 가지**의 m4a가 뒤 가지의 알몸 ba를 가려 미검출이 난다(실측 킬테스트 K4·K7이 그 구멍을 잡았다 = 앞 가지는 mp4 비디오가 없으면 통째로 실패하고 뒤 가지가 실제 착지점이 된다). 표면 자동 발견(`git ls-files` 중 `bv*`∧`+ba` 보유 · 정적 · 렌더·LLM·네트워크 0 · **면책표 없이 하드 0**) · 예외 2종은 **구조적**(주석·독스트링{`ast` 기반} / `--skip-download`·`--print` = 미디어를 0바이트도 안 받는 호출) · 자기참조 배제 = `shared/check_refs.py` 자신(처방문의 「수정 전」 예시가 곧 위반 문자열 · `check_clip_coverage` 템플릿 배제 선례). 파이썬 암묵 문자열 연결은 병합해 판정한다(2줄로 쪼갠 셀렉터의 위양성 차단 · 별개 할당은 안 잇는다 = 위반 은폐 0). 검증 = `ffprobe -hide_banner -i <파일>.mp4 2>&1 | grep Audio` → `Audio: aac` 정상 / `Audio: opus` = 미적용. ⚠ **남는 한계** = 소스에 AAC 자체가 없는 사이트(Opus/Vorbis만 제공)는 폴백으로 종전대로 Opus → 받은 뒤 수동 변환 `ffmpeg -i in.mp4 -c:v copy -c:a aac -b:a 320k -ar 48000 out.mp4`(비디오 copy = 화질 손실 0). 실증 = 킬테스트 9종 전건 검출(4문법·2줄분할·가지별) + 위양성 대조 8종 무경보.
- **[리베이스 후 재검증 = 푸시 게이트(운영자 260802 승인)]** 커밋 때 통과한 게이트가 **리베이스 자동병합에서 되돌아간 채 main에 들어가는 사고**가 실재한다(260802 실측 = thumb 빈 캡슐 술어 1줄 구판 복귀 · 원인 = 커밋→리베이스→푸시에서 **리베이스 뒤에 게이트가 없었다**). 이 레포는 봇·병렬 세션 커밋이 초당급이라 [7-5] 재시도 리베이스가 상시 = 구조적 재발. → **`.githooks/pre-push`가 push 직전 `check_refs`를 다시 돌린다**(CI 러너 제외 = pre-commit 동축 · 우회 = `git push --no-verify`). 셋업 = `git config core.hooksPath .githooks` 1회(pre-commit과 공용). ⚠ 훅은 최후 방어선이지 면죄부가 아니다 — **머지 절차 [7-5]는 ②③(fetch·rebase) 다음에 게이트 재실행을 넣은 순서로 수행한다.**
- **[상태 의존 화면 = 픽스처로 재현(절대규칙 · 운영자 260803 "그 부분까지 해줘")]** 수집함 카드처럼 **지금 라이브 데이터에 없는 상태**(긴급<4h·Failed·Picking…·PICKED)를 검증할 땐 가짜 DOM을 손으로 심지 말고 `node shared/fixture_shot.js` 를 쓴다(픽스처 = `shared/fixtures/scrap_states.json` · 산출 = `docs/_shots/fixture/scrap_<상태>.png` + 상태별 렌더 판정표 · rc=1 = 미렌더). 동작 = viewer/ 를 임시폴더로 복사해 `candidates.json`·`picks-failed.json` 교체 + `api/pending` 라우트 스텁 + `nm_ratings` 시드 → **뷰어 자신의 렌더 경로**(makeScItem·failBotHtml·wireFailBot)로 띄운다 = 프로덕션 코드에 픽스처 분기 0 · 라이브 viewer/ 는 읽기만. 수리 전/후 대조 = `--viewer=<구 사본>`. ⚠ 신설 사유 = 260803 실사고 — Failed 칩 검증에 임시 span 을 심어 캡처했더니 정본(폭 전체 빗금 Failed + ✓)과 **생김새가 달라 운영자가 지적**했다(증빙이 실물과 어긋남). 첫 실행이 곧바로 함정 3종을 잡았다{ⓐ Failed 카드는 `.scrap-list` 가 아니라 **실패 트레이**(`.sc-failtray-list`)로 빠진다 ⓑ `api/pending` 응답 봉투는 `{items:[…]}` — 배열을 주면 전건 무시돼 succ 가 조용히 Picking… 으로 보인다 ⓒ succ 여도 픽 시각이 무응답 타임아웃을 넘기면 pickState 가 'failed' 를 **먼저** 반환한다}. 픽스처는 기계산출물이 아니라 **사람이 쓰는 시나리오 자산** = 손편집 정상(시각은 `_ageH`·`pickedAt` 상대값으로 두어 날짜가 지나도 안 썩는다).
- [절대규칙 · 운영자 260802] UI를 만지면 **로컬 렌더로 눈과 숫자를 먼저 본다** — 손대기 전 `node shared/preview_shot.js base` → 고친 뒤 `node shared/preview_shot.js diff`(로컬 서버 자동 기동 → **스튜디오 2셸 10탭**{이미지 5 + 영상 5} 스샷 + DOM 실측 + 기존 대비 변화표 · 한 셸만 = `--shell=thumb|cap`). 눈대중 "정렬됨" 선언 금지 · 보고 6-2 전/후 이미지도 여기서 나온 캡처를 쓴다. **영상 셸 편입 = 260802 3차**(구판은 이미지 5탭만 봐서 영상이 뒤처진 걸 한 번도 못 잡았다 — 전체창 미적용·도크 순흑 잔류가 실측기를 그냥 통과했다) · 산출 = 셸별 잉크선 + **2셸 교차 한 세로선**(§3-5) + 전체창·도크 골격표.
- [광학-잉크 판정 · 운영자 260802] 정렬 판정 축 = **박스가 아니라 글자 잉크**(Range 사각형). 박스 x가 같아도 padding이 갈리면 눈에 보이는 위치가 어긋난다(260802 사고 = 박스 4탭 동일·잉크 5px 어긋남 → 롤백) · 절대 x 금지(창 폭 따라 위양성). **기준선 = 캡슐 중심축**(운영자 260802 6차 "중앙정렬로 할게" — 구 좌변 시작선 판정은 좌측정렬 시절 축·개정) = 전 칩 |잉크 중심 − 캡슐 중심| ≤ 0.5px. 기계 판정 = `shared/smoke_parity.js` C15(이미지 4탭 한 중앙축) **+ `shared/smoke_studioshell.js` C3(2셸 10탭 교차 한 중앙축) + C7(미리보기 모듈 등가 = 창 w/h·발사 h/r/fs·돋보기 캡슐 상대좌표 · 260802 6차 신설 — 게이트가 안 재던 축이 조용히 갈라진 사고의 편입)** · baseline 초과·신규 어긋남 = FAIL · `smoke_all` 자동발견으로 상시 동행. **면책표(INK_BASE)는 해소되면 그 자리에서 비운다** — 남겨두면 같은 회귀가 조용히 재통과한다(260802 3차 = `{편집:2.6}` 회수).
- [알고리즘 인사이트 회차 원장 · 운영자 260802] AI 채널요약(chan_brief·fb_brief) 회차마다 {요약 전문 + 요약이 쓴 데이터(계정·게시물 25 절대값·ctx 집계) + 그 시점 실검·커뮤니티 키워드}를 `apps/insta/data/algo_runs/YYYY-MM.jsonl`에 1줄 누적(IG+FB 한 줄 · 월샤딩 · 기록자 = insta-fetch 단독). 정본 = `.github/scripts/algo_ledger.py` · 일괄 집계 = `apps/insta/algo_insight.py`(원장 40줄+ 도달 후 ISO주 1회 자동 → algo_insight.{json,md}). Δ·트렌드 매칭은 원장에 굽지 않는다(절대값+타임스탬프만 — 분석기 몫 · 평의회 260802). 게이트 = `check_algo_ledger`(네트워크·LLM 0 심볼 · 스텝 3중 fail-soft · 기록자 유일성 하드) · 롤백 = repo variable `ALGO_LEDGER=0`.
- 기계산출물(거울·데이터)은 손편집 금지 — 생성 코드를 고쳐 재생성으로 반영한다.
- [9-1 납품] 운영자 PC에서 돌 산출물 = 자기완결 1파일로 묶어 첨부한다. 번들러 정본 = `scripts/build_drive_move_bundle.py`(감시기 ps1 전체를 base64로 실어 더블클릭 .bat 1개로 뽑는 방식 · `--check`로 최신 여부 확인). 드리프트 차단 게이트 = `check_drive_move_bundle`(ps1만 고치고 재생성을 잊으면 운영자 PC에 옛 코드가 깔린다 = 조용한 라이브 낡음).
- 디자인 계약 전문 = `AGENTS.md` §🎨.
- 뉴스 파이프라인 = `scraper/`(①수집·선정) → `prompts/news-analysis.md`(②분석·요약) → `apps/news/`(지침·fact_guard 검증) → queue/ → 뷰어.
- **[재난 3축 · 운영자 260803]** ⓞ **지진 규모 문턱 = 국내 3.5 / 해외 6.0**(운영자 260803 5차 "해외건, 국내건 차이가 많이 나야해 · 3도 좀 애매해") — 해외 판정 = `sns_trends.DIS_INTL_RE`(본문 국가·해역명 · `intl` 필드로 배송 · area는 수신 지역이라 못 가른다) · **뷰어 `QUAKE_MIN_KR/INTL` = fire_watch 동값**(화면에 경보가 뜬 지진만 추적기에 들어간다) · 규모 미상 = fail-open(울린다). ⓐ **중대재난 문턱 = `sns_trends.DIS_CRIT_MIN`(77 = 산불 이상) 단일 원천** — 지진·화재급 골드 표기(`.trend-q.dis-crit`)와 화재 추적기 등록이 이 한 값을 공유(뷰어 `disCrit()` = crit 1순위 · 구데이터 sev 역산 폴백). ⓑ **랜드마크 즉시 긴급알림** = `DIS_LANDMARK`(문화유산·고위공직처·유명건물 99종) + `DIS_GOV_RE`(«OO시청»류 접미 결합 · `(?!역)` 오탐컷) → 수집기가 `lm` 필드로 실어보내고 뷰어 `fireMsgs()`가 quakeMsgs 문법 그대로 경고+기기알림(tag 축별 분리 = 지진·화재 동시 발생 시 교체 소실 방지). ⚠ **판정 대상 = 발신 서명을 걷어낸 본문뿐 = `DIS_SIGN_RE`·`_dis_body` · 게이트 `check_disaster_landmark_sign`(260805 실사고 봉합)** — 한국 재난문자는 관례상 전건이 «[○○구청]» 서명으로 닫히는데 구판은 그 서명을 「그 건물에 사고」로 읽었다(실측 = 라이브 재난 10건 중 lm 매치 4건이 **전건 말미 서명**{서해구청·계양구청×2·행정안전부} = 정확도 **0/4**) → 지자체 발신 화재 문자 **전건**이 즉시 긴급알림 = ⑭-e 취지("작아도 전국 뉴스가 되는 곳")의 정반대. 실사고 = 「도로차단은 **유지**되오니 우회」 교통 후속 안내가 기기 긴급알림으로 발사(260805 07:03 인천)되고 fire_watch가 「랜드마크=30」을 얹어 grade HI(3시간 추적)로 하드 승격까지 연쇄. ⚠ 신설 사유 = 기존 게이트는 전부 **정적 문자열·심볼**(check_refs 계열)이거나 **화면 렌더**(smoke_*)라 「판정 함수가 실제로 무엇을 잡는가」가 축 자체로 없었다 — 오탐이 알림·추적·자동픽까지 번지는 동안 무경보였고 운영자 눈이 유일한 검출기(insta-thumb-miss·brk_misfire 동축). 판정 = 정본 함수 import 재판정 8케이스(사전 복제 0 · 네트워크·LLM·렌더 0 · 면책표 없이 하드 0) · 놓침은 안 는다(진짜 사고는 건물명이 **본문**에 실린다 = 「숭례문 화재 발생」·「강남구청 청사 화재」 전건 보존 실증). ⚠ **판정은 데이터에 박제된다 = 코드 봉합 ≠ 라이브 봉합 = 뷰어 2층 검문 + 게이트 `check_disaster_lm_stale`(260805 2차 실사고 봉합)** — `lm`은 **수집 시점 1회** 계산해 항목에 박고(`"lm": disaster_landmark(msg)` · 생산자 2곳 = `disaster()`·`disaster_km()`) 뷰어 `fireMsgs()`는 매 렌더 그 **저장값**을 다시 읽는다 → 위 서명 봉합(10363a4)이 **09:37 KST main 착지**했는데도 07:03에 이미 구워진 항목이 `viewer/sns_trends.json`에 `lm:"서해구청"`으로 살아남아 **10:20 알림 리포트에 그대로 재등장**했다(TTL `FIRE_TTL_H` 12h = 19:03까지 유령 재점등). 데이터는 기계산출물이라 손으로 못 씻고 다음 수집 회차를 기다려야 하므로 **소비 지점 방어가 유일한 즉시 해** = 뷰어가 「서버가 준 lm이 **서명을 걷어낸 본문에 실제로 있는가**」를 한 번 더 본다(`DIS_SIGN_TAIL`·`disBody()` · **사전 복제 0** = 목록·정규식 사전을 뷰어가 들지 않고 형태 규칙 1줄뿐 = 정본 1곳 계약 유지 · fail-open 2종 = text 결손·300자 컷 도달 → 종전 동작 = 놓침 0{실측 라이브 재난 10건 본문 75~90자}). ⚠ 신설 사유 = 짝 게이트 `check_disaster_landmark_sign`은 정본 함수를 케이스로 재판정해 **「코드가 옳은가」만** 본다 — 「**라이브 데이터에 구판 판정이 남아 있는가**」는 축 자체가 없었고, 그래서 봉합 세션이 코드만 고치고 완료로 닫아도 아무 게이트가 안 울렸다(운영자가 리포트를 열어야만 발견 = insta-thumb-miss 동축). 판정 2축 = ① 뷰어 2층 검문 실존(심볼 + `fireMsgs` 본문 배선) = **하드** ② 라이브 데이터 재판정 불일치 = **WARN(비차단)** — 하드로 두면 판정 코드를 고치는 **바로 그 커밋**이 반드시 불일치를 띄워 게이트가 봉합 자체를 막는 자기모순이 되고, 데이터는 봇 소유라 세션이 커밋으로 못 씻으며, ①이 살아 있으면 불일치는 화면에 안 뜨므로 「씻길 때까지 보이게」가 정확한 역할(`check_component_lock` WARN 선례 계승) · 실증 = 킬테스트(`fireMsgs` 검문 줄 제거 → rc=1 검출) + 사고 당일 라이브 재판정이 그 1건을 정확히 지목. ⓒ **화재 후속 추적 = `scraper/fire_watch.py`**(원장 `push/fire_watch.json` · scrape.yml 15분 타이머 편승) — 화재 계열 발령을 사건 단위로 묶어(고정 슬롯 금지 = 근접 매칭) **발령 후 3시간 동안 매 런(15분)** 수집함→네이버 순 검색(운영자 260803 3차 "3시간이면 무조건 보도가 뜸" · 구 15·30분 2회차는 08:20 사건의 10:00 보도를 못 잡는 구멍이었다) → 3축 히트(지역∧화재어∧[**사상자어** ∨ 규모신호])면 **즉시 알림**(push_send `nomute-fire` + msg.py 점등 · 큐잉보다 먼저 = 상한에 막혀도 알림은 간다) + `pick_pending` 큐 적재. ⚠️ 규모신호(대응 2단계↑)는 **사상자어의 대체재가 아니라 병렬 OR**(운영자 "대응단계가 낮아도 사상자는 발생할수있음") — 그 줄을 지워도 사상자 축은 무손상. ⚠️ 자동 과금 = 픽 시점만(되짚기는 LLM 0콜·800건×12회 89ms 실측) · 사건당 1픽 영구 + 일 상한 `FIRE_DAY_CAP`. ⓔ **추적 상태 화면 표기(⑭-i · 260803 6차)** = 원장이 `viewer/` 밖(Pages 출력 루트 아님)이라 뷰어가 못 읽는다 → `build-viewer.mjs`가 표시용 사본 `viewer/fire_watch.json`으로 굽는다(messages.json 선례 · 노출 = 표시 필드만 · `.gitignore` 등재 = 빌드 산출물). 재난문자 행 우측 = `.fw-chip`(부품·값 = `.tcard-ago(.fresh24)` 사본) — 「추적 중 N분」(무채) / 「사상자 확인·큐 넘김」(골드) · 매칭 = 광역∧유형∧45분(서버 `EVENT_GAP_MIN` 동값) · 미매칭 = 칩 없음(추측 표시 금지). ⓓ **위험 등급 학습(⑭-g · 260803 4차)** = 추적이 끝난 사건을 `push/fire_outcomes.jsonl`(정답지 · 특징+결과+소요분)에 1줄 적재 → 다음 발령을 **문자 시점에** 채점(특징 = 시설어·심야·랜드마크 · 추가 수집 0 · 표본 3건↑ 특징은 seed 대신 **실측 적중률** 채택 = 자기교정) → HI 3h / LO 1h 창 배분. ⚠ **「모르는 건 HI」가 제1 원칙** — LO 강등은 {전체 표본 ≥`LEARN_MIN` ∧ 랜드마크 아님 ∧ 점수<`RISK_HI` ∧ **전 특징이 실측으로 뒷받침**} 전부 충족 시만(260803 실측 사고 = ④ 없으면 「야산 들불」 20건만 쌓인 상태에서 한 번도 학습된 적 없는 「심야 요양원」 창이 1h로 깎였다) · 표본 미달이면 전건 HI = 현 동작 100% 불변.
