1. [의도] 사용자의 말에서 진짜 목적을 파악해 그걸 해결한다. 간단한 일은 빠르게, 큰 일은 시작 전에 계획 한 줄 공유한다.
2. [확인] 불확실하면 파일이나 웹으로 확인한다. 추측 금지. 못 확인하면 「미확인」이라고 말한다.
3. [디자인] 새 디자인을 만들지 않는다. 같은 리포지토리 안의 기존 디자인·컴포넌트를 쓴다.
4. [전후] UI 변경은 전과 후를 보여준다. 시각 변경은 이미지나 html로.
5. [완료] **작업 끝 = 머지 = 라이브 반영.** 머지했는데 운영자 화면이 그대로면 그건 완료가 아니라 미완이다. 시안·문서·계획만 머지하고 「완료」라고 부르지 않는다 — 라이브 표면 파일까지 같은 작업 안에서 바꾼다. 단 충돌·체크 실패·비가역 작업이면 멈추고 묻는다.
5-1. [라이브 정의] 라이브 = 운영자가 실제로 여는 화면(`viewer/**` 등 산출 표면)과 그걸 만드는 코드. `docs/reports/**`(시안·플레이그라운드·감사·보고)만 바뀐 머지 = **라이브 0** → 보고에 「시안 머지 · 라이브 미반영」이라고 못박고, 라이브까지 갈 다음 한 걸음을 같은 답에 붙인다.
5-2. [시안은 경유지] 플레이그라운드는 목적지가 아니라 결정 수단이다. 운영자가 선택값을 주면 그 턴에 라이브 배선까지 간다. 시안을 또 뽑는 건 ⓐ 선택값이 아직 없거나 ⓑ 라이브 반영이 규격 개정·승인에 막혔을 때만 — 이때는 막힌 게이트 이름과 필요한 승인 한 줄을 반드시 같이 낸다.
5-3. [막히면 멈추되 노출] 게이트(check_refs·스모크)·규격 동결·8표면 공용 계약처럼 승인 없이는 못 넘는 벽에 막히면 진행을 멈추는 건 맞다. 대신 「완료」라고 부르지 않고 ⚠로 표시한다 — 무엇이·어느 파일 어느 행이 막았고·운영자가 뭘 승인하면 풀리는지.
6. [보고] 딱 3줄: ①결과(실패는 실패라고) ②바뀐 것 ③**라이브 반영 여부**(머지만 = 미반영이라고 명시) + 다음 액션.

## 이 레포 전용 (nomute-editor)
- 디자인 계약 = `AGENTS.md` §🎨 — 값 SSOT = `viewer/index.html` `:root` · 거울 재생성 = `python3 shared/build_design_mirror.py build` · 컴포넌트 계승 = `디자인기틀/CII_컴포넌트계승인덱스.md` 정본 셀렉터.
- 새 색·px·blur·radius·scale 창작 금지 — 기틀에 없는 값이 필요하면 멈추고 운영자에게 묻는다. 승인분은 기틀 편입(토큰 추가 → 거울 재생성 → CII 행).
- 커밋 전 `python3 shared/check_refs.py` 필수(`.githooks/pre-commit`이 강제). UI 표면(뷰어 html) 변경 커밋 전 = `bash shared/smoke_all.sh` rc=0.
- [5 라이브 반영] 이 레포의 라이브 = `viewer/**` · `apps/**` · `functions/**` · `shared/**`(생성 코드) · `scraper/**`. 머지 diff가 `docs/**`에만 닿았으면 라이브 0 = 미완 — 보고 ③에 그대로 쓴다. 기계산출물(거울·데이터)은 손편집이 아니라 생성 코드를 고쳐 재생성으로 반영한다.
- 뉴스 파이프라인 = `scraper/`(①수집·선정) → `prompts/news-analysis.md`(②분석·요약) → `apps/news/`(지침·fact_guard 검증) → queue/ → 뷰어.
