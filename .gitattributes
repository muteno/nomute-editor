# 원장(append-only) union merge — 세션 간 동시 append 충돌을 git이 양쪽 자동 이어붙임(운영자 260716 승인).
# git 내장 union 드라이버(설정 불요) · 데이터 json은 rewrite라 대상 제외(union = JSON 손상).
docs/작업이력.md merge=union
docs/요구사항_큐.md merge=union
